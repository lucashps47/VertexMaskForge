# Vertex Mask Forge Architecture

## Metadata

- **Document status:** Living source of truth
- **Baseline commit:** `9e98800227447f164669719b6c4e45489b1b936a`
- **Baseline subject:** `feat: add mesh paint texture transfer workflow`
- **Validated suite:** 482/482
- **Last architectural checkpoint:** M20-E.1

See also: [Decision Log](VertexMaskForgeDecisionLog.md).

---

## 1. Purpose and Scope

**Current Contract.** Vertex Mask Forge is an Editor-only Unreal Engine 5.8 plugin that generates and edits vertex colors on Static Meshes, so that other systems (materials, shaders, tools) can consume artist-authored per-vertex masks. It solves the production problem of needing procedurally-derived, hand-tunable vertex masks (occlusion, curvature, position, normal-direction, thickness, material-slot membership, noise) without a DCC round-trip.

The tool operates entirely inside the Editor: a Slate panel (`SVertexMaskForgePanel`) lets an artist select one or more eligible components in the viewport, generate one or more scalar masks per selection, compose them into a working color buffer, preview the result live on the real components, and either **Accept** (persist into the Static Mesh's source vertex colors) or **Cancel** (discard the session). Nothing in this plugin ships into a packaged game — it has no Runtime module and no gameplay-facing API.

---

## 2. Architectural Overview

**Current Contract**, for the legacy/production path (the only path with a live UI caller and a real Accept target):

```
Selection (viewport components)
  → Working Mesh (per-asset geometry + identity)
  → Generator Result (7 fixed scalar masks, GeneratorState)
  → Layer Stack (fixed GeneratorLayerOrder + Channel Filter)
  → Composition (GeneratorLayerBridge, pure)
  → Working Colors (WorkingStateOwner, applied)
  → Display Colors (Preview Mode reduction, display-only)
  → Accept (writes to the Static Mesh source) / Cancel (discards the session)
```

A second subsystem exists in parallel — the **Dynamic Layer** model (`FVertexMaskForgeDynamicLayerStack`, `VertexMaskForgeDynamicLayerEvaluator`, `VertexMaskForgeDynamicLayerBatchCompositor`, `VertexMaskForgeDynamicMaskGeneration`) — whose composition core (stack identity, per-index evaluation, batch composition) is structurally complete and tested, but **not yet connected** to any panel output. It has a real, editable Slate panel section, real generation for one generator (Material Slot), and a proven, tested composition chain (see [§8 Composition](#8-composition) and [§15 Current Integration Status](#15-current-integration-status)), but it does not feed any preview, `WorkingColors`, or Accept target today. It is shown here deliberately isolated:

```
[Dynamic subsystem, proven but isolated]
DynamicLayerStack identity → real generation → InstanceResults → ComposeColors → local output only
(no arrow into Working Colors / Accept — none exists yet)
```

**Contract established, implementation pending (M16-K.6D-1, ADR-011):** `SVertexMaskForgePanel` now owns an explicit `EVertexMaskForgePreviewSource PreviewSource` member (`Legacy`/`Dynamic`, default `Legacy`) — the authority that will eventually determine which of the two pipelines above `ApplyPreviewToEntry` shows. As of this checkpoint the member exists and defaults to `Legacy`, but **nothing reads it to select behavior and nothing writes it to `Dynamic`** — no UI control exists yet, and the diagram above remains accurate: Dynamic still has no arrow into Working Colors or Accept. See [Decision Log ADR-011](VertexMaskForgeDecisionLog.md#adr-011--authoritative-preview-source-and-non-persistible-dynamic-preview) for the full future contract (a non-persistible transitory buffer for Dynamic, structural — not conditional — isolation from Accept, and the revised checkpoint sequence) this member's *type* now codifies, none of which is implemented yet.

**Contract established (M16-K.6D-2, corrected):** a real Source-Topology visual-only preview seam now exists — `VertexMaskForgePanel::ApplySuppliedSourceTopologyPreviewColors(State, SourceMesh, const TArray<FColor>& SemanticComposedColors, EVertexMaskForgePreviewMode PreviewMode, DebugMaterial, bUseOriginalMaterials, ActorHideStates) -> bool`, a `static` free function private to `SVertexMaskForgePanel.cpp`. It receives colors that are semantically composed (i.e. equivalent to what `WorkingColors` holds immediately after `ApplyComposedColorsRGB`), **before** any Preview Mode display reduction — the M16-K.6D-2 checkpoint's first attempt was rejected specifically because its own "seam" received colors the caller had already reduced through `DeriveDisplayColors`, so its `ComposedColors` parameter was not actually semantically composed. This corrected version validates `SemanticComposedColors.Num()` against the Source Topology corner count (`SourceMesh.TriangleCount() * 3`) completely before any visual mutation — an exact mismatch returns `false` immediately, with no padding, no truncation, no partial application — then derives display colors itself via `VertexMaskForgeDisplayColorDerivation::DeriveDisplayColors` (delegated to the new, directly-testable, non-static `VertexMaskForgePanel::DeriveValidatedSourceTopologyPreviewColors`, declared in `VertexMaskForgeMaskTypes.h`), and applies the result through the existing `EnsureSourceTopologyPreviewComponent`/`ApplySourceTopologyColorsToPreviewComponent` helpers, unchanged. It reads no `FVertexMaskForgeWorkingStateOwner`/`WorkingMesh.InstanceResults`/`FVertexMaskForgePreviewComponentState::InstanceResults` state, never calls `ApplyComposedColorsRGB`/`RestoreFromBaseline`/`RestoreFromCommitted`, and never writes `WorkingColors`/`CommittedColors`/`BaselineColors` (or their `SourceTopology*` siblings) or any Accept-facing store. Today's only caller remains `ApplyPreviewToEntry`'s Legacy Source-Topology branch, which supplies its own just-written `StateOwner->GetWorkingColors()` (the exact semantic composition `ApplyComposedColorsRGB` just applied) and `CurrentPreviewMode`, and explicitly calls `RestorePreviewVisualOnly` if the seam returns `false` — **no Dynamic caller exists yet**, no buffer was created, and `PreviewSource` is still never read anywhere in production code (fixed at its `Legacy` default in practice). The render-vertex sibling (`ApplyPreviewColorsToPreviewComponent`) was deliberately left untouched — this checkpoint's seam is Source-Topology only, matching the M16-K.6D sequence's own Material-Slot-plus-Source-Topology scope; a Render Vertex seam remains **future checkpoint**. M16-K.6D-3 through M16-K.6D-5 (Material Slot API adaptation, testable Dynamic orchestrator, functional UI/wiring) remain **planned**, not implemented.

**Contract established (M16-K.6D-3):** the Material Slot generator's existing, unmodified Source-Topology entry point — `VertexMaskForgeMaterialSlotGenerator::GenerateMaterialSlotMaskFromDynamicMesh(const FVertexMaskForgeWorkingMesh& WorkingMesh, int32 SelectedSlotIndex, bool bInvert) -> FVertexMaskForgeScalarMask` — is now formally documented as THE caller-owned Source-Topology Material Slot API a future Dynamic orchestrator (M16-K.6D-4) is meant to call directly. Auditing this checkpoint found the function already satisfied every required property without any signature or math change: it takes only read-only inputs (no store, no `WorkingColors`, no `FVertexMaskForgeWorkingStateOwner`), returns its result entirely by value (domain: Source Topology corners, cardinality exactly `Mesh.TriangleCount() * 3`), and reports success/failure unambiguously via `Mask.State` (`Ready`, including a legitimate all-`0.0` result when `SelectedSlotIndex` matches no triangle, versus `Unavailable` for a structural failure — the two are never confused). Three existing call sites already treat it as the single source of truth, each writing to its own store only after confirming `Ready`: `SVertexMaskForgePanel`'s own legacy inline call (`GeneratorState.MaterialSlotMask`), `VertexMaskForgeMaterialSlotGenerator::GenerateMaterialSlotMaskInstanceResult` (`WorkingMesh.InstanceResults`, Recipe/M16-E identity), and `VertexMaskForgeDynamicMaskGeneration::GenerateStoredResultForMaterialSlotInstance` (`WorkingMesh.InstanceResults`, Dynamic Layer identity) — none of the three were altered by this checkpoint. Nine new direct tests (`VertexMaskForge.MaterialSlotCallerOwned.*`) prove this contract, including byte-for-byte equivalence against `GenerateMaterialSlotMaskInstanceResult`'s own stored result for identical inputs. **No orchestrator was created** (`ComputeComposedColorsRGBSourceTopology`-equivalent remains reserved for M16-K.6D-4), **no buffer was created**, the result is **not connected to the K.6D-2 preview seam**, and `PreviewSource` is still never read anywhere in production code.

**Contract established (M16-K.6D-4):** a new, testable Dynamic Source-Topology composition orchestrator now exists — `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology(const FVertexMaskForgeWorkingMesh& WorkingMesh, const FVertexMaskForgeDynamicLayerStack& Stack, TConstArrayView<FColor> BaseColors, TArray<FColor>& OutComposedColors) -> bool`, in its own new file pair (`VertexMaskForgeDynamicSourceTopologyComposition.h/.cpp`) and own new namespace, deliberately outside `SVertexMaskForgePanel.cpp` and distinct from the pre-existing Legacy `VertexMaskForgePanel::ComputeComposedColorsRGBSourceTopology` (same reserved name, different namespace — no ambiguity at any call site, and this new function has no production caller of any kind yet). It folds `Stack`'s layers strictly in array order over `BaseColors`, mirroring `VertexMaskForgeDynamicLayerEvaluator::EvaluateColor`'s own per-layer semantics (Fill resolution via the same `TryResolveFillValue`, masked-layer `EffectiveMask` multiplication, `BlendMode`/`Opacity` fold via the same `VertexMaskForgeSequentialEvaluator::EvaluateFillLayerStep`, per-layer Channel Filter, one final `Clamp01`, Alpha carried unconditionally from `BaseColors`) — but resolves a masked layer's `EffectiveMask` from a freshly, directly generated `VertexMaskForgeMaterialSlotGenerator::GenerateMaterialSlotMaskFromDynamicMesh` array (the M16-K.6D-3 caller-owned API, called directly, never through any stored wrapper) rather than a `FVertexMaskForgeInstanceResultStore` lookup — this orchestrator never receives, constructs, or touches a `FVertexMaskForgeInstanceResultStore` of any kind. Scope is deliberately restricted to a Material-Slot-only vertical slice: an **enabled** layer whose `Mask` is set to any generator type other than `MaterialSlot` fails the **whole call** explicitly (never silently skipped, never treated as Fill-only); a **disabled** layer's `Mask` is never even inspected, exactly mirroring `EvaluateColor`'s own `bEnabled==false` no-op contract. Validation is all-or-nothing: `WorkingMesh.Mesh` must be valid, `BaseColors.Num()` must equal exactly `Mesh.TriangleCount() * 3` (no resize, no partial output), and every enabled masked layer's generation call must return `Ready` at the expected cardinality — any failure leaves `OutComposedColors` **completely untouched** (whatever it held before the call), never partially written. An empty `Stack` succeeds trivially as a byte-exact passthrough of `BaseColors`. Proven by 13 new direct tests (`VertexMaskForge.DynamicSourceTopologyComposition.*`), including an order-of-composition proof (two Fill-only layers with opposite values, added in each of the two possible orders, independently-computed expected results) and structural non-mutation/no-store proofs. **This orchestrator is not connected to preview, to the K.6D-2 visual seam (`ApplySuppliedSourceTopologyPreviewColors`), or to `PreviewSource`** — it has no production caller of any kind; only its own Automation tests call it. M16-K.6D-5 (functional wiring) remains **planned**, not implemented.

**Contract established (M16-K.6D-5):** `PreviewSource` now has its first functional read/write anywhere in production code, and `EVertexMaskForgePreviewSource::Dynamic` is, for the first time, actually shown in the viewport (Source Topology domain, Material Slot only). A minimal `SComboBox<TSharedPtr<EVertexMaskForgePreviewSource>>` (label "Preview Source", options "Legacy"/"Dynamic", default `Legacy`, mirroring the pre-existing Preview Mode combo's own shape) is `PreviewSource`'s sole writer (`SVertexMaskForgePanel::OnPreviewSourceSelectionChanged`); selecting a new value immediately calls the panel's existing `RecomposeWorkingColors()` choke point. `ApplyPreviewToEntry`'s Source-Topology branch reads `PreviewSource` live, once per component, at the very top of the domain block — **before** any Legacy per-component generator work (Bounding Box/AO/Directional Normal World-Space re-evaluation), so a degenerate/failed Legacy generator can never block or affect the Dynamic branch and vice versa:
- **`PreviewSource == Legacy`** (default): the pre-existing composition code is **byte-for-byte unchanged** — same `ComputeComposedColorsRGBSourceTopology` (Legacy, `VertexMaskForgePanel` namespace), same `ApplyComposedColorsRGB`, same seam call supplying `StateOwner->GetWorkingColors()`.
- **`PreviewSource == Dynamic`**: a structurally separate branch calls `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` (the M16-K.6D-4 orchestrator, unmodified) directly, with `WorkingMesh`, the panel's own `DynamicLayerStack`, and `StateOwner->GetBaselineColors()` (never `GetWorkingColors()`/`GetCommittedColors()`, which belong to the Legacy lifecycle only) as inputs. On success, the orchestrator's fresh, local `TArray<FColor>` is fed directly to the unmodified M16-K.6D-2 seam (`ApplySuppliedSourceTopologyPreviewColors`) — never `ApplyComposedColorsRGB`, never any color-state array, never any store. On failure (unsupported masked generator, invalid input), `PreviewSource` is **never** silently reverted to `Legacy` (no automatic fallback, per ADR-011) — the affected component's real, original appearance is restored via the existing `RestorePreviewVisualOnly`, and `LastOperationErrorText` reports the failure factually so the status line communicates it while `Dynamic` remains selected.

Every `FVertexMaskForgeDynamicLayerStack` mutator the panel's own Dynamic Layers UI already exposed (`SetLayerFill`/`SetLayerBlendMode`/`SetLayerOpacity`/`SetLayerEnabled`/`SetLayerChannelFilter`/`SetLayerMaskGeneratorType`/`ClearLayerMask`/`SetLayerMaskParams`, plus reorder/add/remove) now additionally calls a new single choke point, `OnDynamicLayerStackMutated()`, which requests a refresh (`RecomposeWorkingColors()`) **only** when `PreviewSource == Dynamic` — editing the Dynamic stack while Legacy is being previewed triggers no recomposition at all, and every one of those edits now updates the Dynamic presentation live while it is selected.

**Accept was Legacy-only through M16-K.6D-6; superseded by M16-K.6D-7B (see below) — Dynamic Accept now exists.** Through M16-K.6D-6, `CanAcceptChanges()` additionally required `PreviewSource == Legacy` and `AcceptPendingChanges()` had a matching defensive early-out for Dynamic. As of M16-K.6D-7B, both are source-aware instead of Dynamic-blocking: see the M16-K.6D-7A/7B paragraph below for the current contract.

**Not covered by any Automation test in this codebase:** the panel-level routing above (`PreviewSource` default/read/write, the `ApplyPreviewToEntry` branch, `OnDynamicLayerStackMutated`, `CanAcceptChanges`/`AcceptPendingChanges`'s Dynamic gate) is entirely private `SVertexMaskForgePanel` code, and no test anywhere in this codebase constructs `SVertexMaskForgePanel` or a live Preview Component (same boundary M16-K.6D-2's own tests already documented). The M16-K.6D-4 orchestrator (13 tests) and the M16-K.6D-2 seam (6 tests) — both unmodified by this checkpoint and both re-confirmed still passing — remain the full extent of this feature's automated proof; the panel-level wiring itself is deferred to M16-K.6D-6's manual validation, per this checkpoint's own explicit scope.

M16-K.6D-5 remains scoped exactly as before: Material Slot is still the only Dynamic generator; the Render Vertex domain has no Dynamic seam; Global Channel Filter/Fill remain Legacy-only and are not yet gated off in the UI while `PreviewSource == Dynamic` (still **planned**, per ADR-011).

**Contract established (M16-K.6D-6, manual validation and defect correction):** the M16-K.6D-1 through -5 vertical slice was independently audited and manually validated end-to-end in the Editor (Groups A–D of the checkpoint's own protocol: session/default behavior, Dynamic Fill, Dynamic Material Slot mask, and stack mutation/sequential order — all PASS). Manual validation surfaced two real defects and one false alarm, all in panel-only UI code, none in the K.6D-4 orchestrator or the K.6D-2 seam (both remain unmodified since M16-K.6D-4/-2 respectively, and both remain fully covered by their own existing automated tests):

- **Reported Opacity "plateau" — investigated, found NOT to be a defect.** A numeric regression test (`VertexMaskForge.DynamicSourceTopologyComposition.CopyBlendOpacityIsContinuousAndMonotonicAcrossFullRange`) proved the orchestrator's own Copy-blend output is continuous and strictly monotonic across the full `[0,1]` Opacity range (independently-computed expected byte values, exact match). The original visual report was a misinterpretation, confirmed by the reporter's own retest. No production code was changed; the regression test is retained permanently.
- **Generator Type dropdown had no reachable "None" row.** Root cause (confirmed directly against UE 5.8 engine source, `Widgets/Views/SListView.h`): `SListView`'s own row-generation loop unconditionally skips any option that fails `TListTypeTraits::IsPtrValid` — a null `TSharedPtr`, the representation the M16-K.6B checkpoint originally chose for "None", can **never** render as a selectable row in any `SComboBox`, in any circumstance. Fixed by changing `DynamicLayerGeneratorTypeOptions`' element type to `TSharedPtr<TOptional<EVertexMaskForgeGeneratorType>>` — "None" is now a *valid* pointer to an *unset* `TOptional`. `EVertexMaskForgeGeneratorType` (no None enumerator) and `FVertexMaskForgeDynamicLayerStack`/`ClearLayerMask` are unchanged — this is a Slate-list-rendering fix local to one combo's option representation. A second, related defect (the combo's `InitiallySelectedItem` was hardcoded to "None" on every row rebuild, desyncing Slate's own internal selection tracking from the layer's real state per `SComboBox::OnSelectionChanged_Internal`'s reselection-suppression rule in `Widgets/Input/SComboBox.h`) was fixed the same way for both the Generator Type and Fill combos: `InitiallySelectedItem` is now resolved from the layer's actual current state at row-construction time, never hardcoded to array index 0.
- **Dynamic Layers panel showed inverted composition priority.** Root cause: `RebuildDynamicLayersList` displayed `DynamicLayerStack.GetLayers()` in forward array order (index 0 at the top), while `VertexMaskForgeDynamicLayerEvaluator::EvaluateColor` and the K.6D-4 orchestrator both fold strictly in that same array order (index 0 first, so the *highest* array index — composited last — wins Copy-mode conflicts) — an established, tested, unmodified contract. The layer shown at the top of the panel therefore had the *lowest* composition priority, opposite of standard painter/layer-stack convention. Fixed by reversing only the panel's own display order (`RebuildDynamicLayersList` now walks the array from the last index to the first) — `FVertexMaskForgeDynamicLayerStack`'s stored array order and the evaluator/orchestrator's fold direction are unchanged.
- **UI corrections requested alongside the above, applied in the same pass:** (1) the Fill combo no longer offers `EVertexMaskForgeLayerFill::None` as a user-facing option (Enabled is the sole disable mechanism now) — `AddLayer`'s own domain default (`Fill=None`, protected by `VertexMaskForgeDynamicLayerStackTests.cpp`) is unchanged; `OnAddDynamicLayerClicked` now calls the existing `SetLayerFill` mutator to resolve a newly added layer to `White` immediately. (2) The per-layer Up/Down buttons were replaced with vertical drag-and-drop reordering, implemented via UE 5.8's own `SDragAndDropVerticalBox`/`FDragAndDropVerticalBoxOp` (SlateCore, `Widgets/SBoxPanel.h` — the same mechanism the Editor's own Landscape Edit Layers stack uses), translating the visually-dropped position through the same reversed-display mapping into a single `FVertexMaskForgeDynamicLayerStack::MoveLayer(LayerId, NewIndex)` call (the existing general "move to an arbitrary final index" seam — `MoveLayerUp`/`MoveLayerDown`, single-step-only, cannot express an arbitrary-distance drag in one action). `MoveLayerUp`/`MoveLayerDown` themselves remain in `FVertexMaskForgeDynamicLayerStack` and fully tested; only their now-unused panel-side UI-glue wrappers were removed.

**Automated coverage:** one new orchestrator-level regression test (14 total in the `DynamicSourceTopologyComposition` group). The panel-level fixes above (combo option representation, display order, drag-and-drop) are entirely `SVertexMaskForgePanel`-private UI code with no non-panel public seam to test against, and drag-and-drop is pure Slate interaction — consistent with every prior K.6D checkpoint's own established automation boundary (no test in this codebase constructs `SVertexMaskForgePanel` or a live Preview Component), these were validated **manually** instead, per this checkpoint's own protocol.

**Still not implemented (unchanged from M16-K.6D-5), independent of Dynamic Accept below:** Global Channel Filter/Fill gating while `PreviewSource == Dynamic` and the Render Vertex domain. Dynamic generator support beyond Material Slot, Bounding Box, Directional Normal, and Curvature — see the M16-K.6D-8B/8C/8D/8E paragraphs below.

**Contract established (M16-K.6D-7A, non-panel builder seam):** a new, testable Dynamic Source-Topology accept-target builder now exists — `VertexMaskForgeDynamicAcceptTargetBuilder::BuildSourceTopologyAcceptTargets(const TArray<TSharedPtr<FVertexMaskForgeSelectedMesh>>& SelectedMeshes, const FVertexMaskForgeDynamicLayerStack& DynamicLayerStack, TArray<VertexMaskForgeAcceptTargetBuilder::FSourceTopologyAcceptTarget>& OutTargets, FText& OutErrorText) -> bool`, in its own new file pair (`VertexMaskForgeDynamicAcceptTargetBuilder.h/.cpp`). It mirrors `VertexMaskForgeAcceptTargetBuilder::BuildSourceTopologyAcceptTargets`'s own output type and all-or-nothing/per-component-conflict contract, but sources its per-component result from the M16-K.6D-4 orchestrator (`VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology`, called directly, no reimplemented blend/mask math) fed by each component's own `GetBaselineColors()` — **never** `WorkingColors`/`SourceTopologyWorkingColors`/`CommittedColors`, satisfying ADR-011's structural-isolation requirement (this builder is a separate function from, and never calls, either Legacy `VertexMaskForgeAcceptTargetBuilder` builder). An empty or all-disabled `DynamicLayerStack` is a *valid computation* here (byte-exact Baseline passthrough) — this is a computational contract only, distinct from the UI-eligibility contract M16-K.6D-7B adds below. On failure, `OutTargets` is left **completely unchanged** from its caller-supplied value (built into a local array, published only on full success — unlike the Legacy builder, which unconditionally `Reset()`s its output first). Reuses `VertexMaskForgeAcceptTargetBuilder::ValidateSourceTopologyCorrespondence` verbatim for triangle/corner correspondence. Proven by 14 new direct tests (`VertexMaskForge.DynamicAcceptTargetBuilder.*`), including byte-exact parity against a direct orchestrator call, same-asset agreement/divergence, and non-mutation of `DynamicLayerStack`/Baseline/Working/Committed colors. **No production caller existed as of M16-K.6D-7A** — `SVertexMaskForgePanel` was untouched, and `CanAcceptChanges()`/`AcceptPendingChanges()` still unconditionally rejected Dynamic.

**Contract established (M16-K.6D-7B, panel wiring — Dynamic Accept is now production-reachable; manual Editor validation still pending):** `RecomputeOperationState()` is now source-aware. Legacy's branch is byte-for-byte unchanged (the same `GeneratorState.*.Ready` scan). Dynamic's branch computes pending-ness from `bHasSelection && DynamicLayerStack.HasAnyEnabledLayer()` — a live `PreviewComponent` on at least one selected entry, **and** `FVertexMaskForgeDynamicLayerStack::HasAnyEnabledLayer()` (new, M16-K.6D-7B), a small query distinct from `!IsEmpty()`: an empty stack, or a non-empty stack with every layer disabled, both report no enabled layer and therefore never produce Dynamic pending changes. `CanAcceptChanges()` no longer special-cases `PreviewSource` at all (`OperationState == PendingChanges` is sufficient, since `OperationState` is itself now source-aware). `AcceptPendingChanges()`'s target-construction step is now a source branch: Legacy calls its original two builders unchanged; Dynamic calls **only** `VertexMaskForgeDynamicAcceptTargetBuilder::BuildSourceTopologyAcceptTargets` (passing the live `DynamicLayerStack`), with `Targets` (render-vertex) left empty since Dynamic is Source-Topology only. Both branches fully validate before the shared confirmation dialog, the shared single `FScopedTransaction`, and the shared `VertexMaskForgeAcceptWriter::WriteSourceTopologyAcceptTargets`/`BuildModifiedMeshes`/success-teardown path — all reused completely unmodified and source-agnostic, so a Dynamic Accept's write, transaction, Undo/Redo, dirtying, render rebuild, and session-end behavior are identical in mechanism to Legacy's. The confirmation and status text were already source-neutral and required no wording change. No Dynamic Instance Override path exists or was added — this codebase has no distinct Instance Override control at all (removed entirely by an earlier checkpoint); the single Accept path now covers both sources via `FSourceTopologyAcceptTarget` only. **Automated coverage** is necessarily partial: `HasAnyEnabledLayer()` (the new decision primitive) has 3 new direct tests; the M16-K.6D-7A builder's own 14 tests and the Legacy writer/transaction machinery are unmodified and re-confirmed passing; `RecomputeOperationState`/`CanAcceptChanges`/`AcceptPendingChanges` themselves remain entirely private `SVertexMaskForgePanel` code with no automatable seam in this codebase (same established boundary as every prior K.6D checkpoint — no test anywhere constructs `SVertexMaskForgePanel` or a live Preview Component). Dynamic Accept's own manual Editor validation subsequently passed (M16-K.6D-7B).

**Contract established (M16-K.6D-8B, orchestrator support for Local-space Bounding Box):** `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` now accepts a second masked-generator type, `BoundingBox`, alongside `MaterialSlot` — Local-space only. Its Pass 1 dispatches by `GeneratorType`: `MaterialSlot` unchanged (corner-domain, via `GenerateMaterialSlotMaskFromDynamicMesh`); `BoundingBox` validates `bUseUnifiedBounds == false` and no enabled axis has `bWorldSpace == true` *before* calling the existing, unmodified `VertexMaskForgeBoundingBoxGenerator::GenerateBoundingBoxMaskFromDynamicMesh` — either unsupported flag fails the whole call deterministically (never silently reinterpreted as Local Space or per-mesh bounds). Because that generator's output is indexed by Dynamic Mesh VertexID (not corner), each generated mask is now tagged with its own domain (`Corner` or `DynamicMeshVertex`, a private orchestrator-local enum) and vertex-domain masks are resolved per corner through that corner's own triangle (`Mesh.GetTriangle(TriangleID)[Corner]`), read only via `FVertexMaskForgeScalarMask::TryGetValue` — mirroring, but never calling, the same per-generator domain distinction Legacy's own Source-Topology composition already makes. World Space and Unified Bounds remain unsupported by design (no per-component transform, no full-selection context reaches this orchestrator) — proven by two dedicated rejection tests. 9 new orchestrator tests total; `VertexMaskForgeDynamicAcceptTargetBuilder` required zero changes (already fully generator-agnostic).

**Contract established (M16-K.6D-8C-B, obsolete panel removal):** the standalone `Layers` presentation section (a fixed Legacy-generator list with Up/Down reorder buttons, built during an earlier, since-superseded UI attempt — never the same as `Dynamic Layers`) was removed from `SVertexMaskForgePanel::Construct()`, along with its exclusively-owned helpers (`RebuildGeneratorLayerList`, `BuildGeneratorLayerRow`, `CanMoveGeneratorLayerUp/Down`, `OnMoveGeneratorLayerUp/Down`, `GeneratorLayerListContainer`). **`GeneratorLayerOrder` itself was retained unchanged** — it remains the real order Legacy composition reads at both of its production call sites; only its now-obsolete UI wrapper was deleted. The `Dynamic Layers` panel and every Legacy generator expander were unaffected.

**Contract established (M16-K.6D-8C-C, Local-space Bounding Box is user-accessible in Dynamic Layers; manually validated PASS):** `Bounding Box` is now a selectable entry in the Dynamic Generator Type combo (`DynamicLayerGeneratorTypeOptions`), alongside `MaterialSlot`. Selecting it uses the existing `SetLayerMaskGeneratorType` contract unchanged — a fresh default `FVertexMaskForgeBoundingBoxParams` (all axes disabled, Local-space, Unified Bounds off), discarding any prior variant, exactly like every other generator switch. Each Dynamic Layer row now has its own **`Generator Parameters`** `SExpandableArea`, visible only when the layer's generator actually has Dynamic-exposed parameters (`MaterialSlot` or `BoundingBox` today; a Fill-only/`None` layer shows no expander) — belonging to that one layer, never a shared panel-level control, and never the panel removed in M16-K.6D-8C-B. The Material Slot editor moved inside this expander with byte-for-byte unchanged mutation semantics. Bounding Box exposes, per axis (X/Y/Z): Enabled, Invert, Position (`[0,1]`), Falloff (label only — the underlying field remains `TransitionWidth`, `[0.001,1]`), Mirror — visually mirroring the Legacy `BuildBoundingBoxAxisRow` layout/ranges, but every control reads/writes only that specific `LayerId`'s own stored parameters via a new `MutateDynamicBoundingBoxAxisParam` helper, which mirrors the Material Slot picker's own six-step identity-validated write path (mask exists, `GeneratorType == BoundingBox`, `Params` is the Bounding Box payload type, `MaskInstanceId` matches the id this row was built for) before copying-mutating-one-field-and-writing back through `SetLayerMaskParams` + `OnDynamicLayerStackMutated()`. No World Space or Unified Bounds control exists anywhere in this UI. If a layer's *already-stored* data requests either (unreachable through this UI, but not otherwise precluded), a concise warning appears and every axis control disables itself — `IsDynamicBoundingBoxLayerLocalSpaceCompatible` is a read-only echo of the orchestrator's own rejection predicate, never a second enforcement point, never a silent normalization. No shared/panel-level Bounding Box state was added — only four new private `SVertexMaskForgePanel` methods. Manually validated end-to-end: independent two-layer parameter state, reorder/deletion preserving correct layer identity, generator-switch reset behavior, live preview, Accept/Cancel, and no Legacy regression.

**Contract established (M16-K.6D-8D-B, orchestrator support for Local-space Directional Normal):** `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` now accepts a third masked-generator type, `DirectionalNormal`, alongside `MaterialSlot` and `BoundingBox` — Local-space only. Its Pass 1 dispatch validates `Space == EVertexMaskForgeNormalSpace::Local` *before* calling the existing, unmodified `VertexMaskForgeDirectionalNormalGenerator::GenerateDirectionalNormalMaskFromDynamicMesh` with all six authoritative fields (`Direction`/`Angle`/`Falloff`/`Blur`/`bInvert` unchanged, `FTransform::Identity` supplied only after Local-space is confirmed, since Local-space evaluation never reads it) — World Space fails the whole call deterministically, never silently reinterpreted as Local. The generator's output is already corner-domain, like Material Slot's, so it is tagged `ELayerMaskDomain::Corner` and resolved through the existing direct Pass 2 corner-index read, unchanged — no new domain, no vertex remapping. Because an individual corner may legitimately be left unwritten by the generator (missing Normal Overlay element or degenerate normal) while the mask as a whole still reports `Ready`, Pass 1 additionally requires `NumValidValues` to equal the full expected corner count before accepting the mask, since the corner-domain read is a direct, unconditional index rather than a `TryGetValue` lookup. 5 new orchestrator tests total; `VertexMaskForgeDynamicAcceptTargetBuilder` required zero changes (already fully generator-agnostic).

**Contract established (M16-K.6D-8D-C, Local-space Directional Normal is user-accessible in Dynamic Layers; manually validated PASS):** `Directional Normal` is now a selectable entry in the Dynamic Generator Type combo, alongside `MaterialSlot` and `BoundingBox`. Selecting it uses the existing `SetLayerMaskGeneratorType` contract unchanged — a fresh default `FVertexMaskForgeDirectionalNormalParams` (Local-space, `PositiveZ`, `Angle=90`, `Falloff=45`, `Blur=0`, not inverted), discarding any prior variant. The layer's own `Generator Parameters` expander gained a third sibling block (`BuildDynamicDirectionalNormalLayerParamsBlock`) exposing exactly five artistic controls — Direction (combo, reusing the Legacy `NormalDirectionOptions`/label), Angle (`[0,180]`), Falloff (`[0,Angle]`, its live maximum bound to the layer's own current Angle), Blur (`[0,10]`), Invert — with `Falloff <= Angle` maintained at the UI mutation boundary only (reducing Angle clamps a larger stored Falloff down to match; a newly requested Falloff is clamped to the current Angle). `Space` is deliberately not exposed anywhere in this block — it remains model-owned and hidden, since this checkpoint supports Local Space only. Every control reads/writes only that specific `LayerId`'s own stored parameters via a new `MutateDynamicDirectionalNormalParam` helper, mirroring the Bounding Box axis helper's own six-step identity-validated write path (mask exists, `GeneratorType == DirectionalNormal`, `Params` is the Directional Normal payload type, `MaskInstanceId` matches the id this row was built for) before copying-mutating-and-writing back through `SetLayerMaskParams` + `OnDynamicLayerStackMutated()`. If a layer's *already-stored* data requests World Space (unreachable through this UI, but not otherwise precluded), a concise warning appears and every control in the block disables itself — `IsDynamicDirectionalNormalLayerLocalSpaceCompatible` is a read-only echo of the orchestrator's own rejection predicate, never a second enforcement point, never a silent normalization. No shared/panel-level Directional Normal state was added — only three new private `SVertexMaskForgePanel` methods. No evaluator math was copied into Slate; every edit still flows through the same orchestrator preview and Accept both already share. Manually validated end-to-end (items A–G): selector/defaults, live parameter behavior including the Angle/Falloff invariant and genuinely-nonzero Blur, independent two-layer parameter state with reorder, stable-`LayerId` deletion safety, generator switching, preview/Accept parity, and no Legacy regression.

**Contract established (M16-K.6D-8E-B, orchestrator support for Dynamic Curvature with within-call raw-cache sharing):** `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` now accepts a fourth masked-generator type, `Curvature`, alongside `MaterialSlot`, `BoundingBox`, and `DirectionalNormal`. Curvature has no Local/World-space concept or `ComponentTransform` dependency at all, so its Pass 1 branch has no space-rejection check — it extracts `FVertexMaskForgeCurvatureParams` and forwards all six authoritative fields (`Type`/`Multiplier`/`Blur`/`LevelsMin`/`LevelsMax`/`bInvert`) unchanged to the existing, unmodified `VertexMaskForgeCurvatureGenerator::GenerateCurvatureMaskFromDynamicMesh`. Unlike the three generators above, that function requires a mutable `FVertexMaskForgeGeneratorState&` for its own internal raw Convex/Concave cache (`EnsureCurvatureRawCache`, gated on `WorkingMesh.GeometryFingerprint`) — the orchestrator satisfies this with **one function-local `FVertexMaskForgeGeneratorState CurvatureLocalGeneratorState`, declared once outside Pass 1's per-layer loop and reused by every enabled Curvature layer within that single orchestrator invocation**: the first such layer computes the raw arrays, every subsequent one in the same call reuses them via the generator's own existing fingerprint gate, while each layer still applies its own Type/Multiplier/Blur/Levels/Invert independently. `GenerateCurvatureMaskFromDynamicMesh` returns its `FVertexMaskForgeScalarMask` entirely by value (never aliased to the shared state's own cached arrays), so storing each layer's completed mask via `MoveTemp` is always safe — a later Curvature layer's call can never retroactively mutate an earlier layer's already-stored result. `CurvatureLocalGeneratorState` is never static, panel-owned, or retained beyond the single call — a later, separate orchestrator invocation (e.g. a subsequent Auto Update Preview recomposition) starts with a fresh, empty state and recomputes the raw arrays again, even if geometry has not changed; **persistent cross-call caching is explicitly deferred, not implemented**. Curvature's output is indexed by DynamicMesh Vertex ID (like Bounding Box's), so it is tagged `ELayerMaskDomain::DynamicMeshVertex` and resolved through the existing `CornerToVertexID`/`TryGetValue` Pass 2 path, unchanged — no new domain, no vertex remapping. 6 new orchestrator tests total; `VertexMaskForgeDynamicAcceptTargetBuilder` required zero changes (already fully generator-agnostic).

**Contract established (M16-K.6D-8E-C, Dynamic Curvature is user-accessible in Dynamic Layers; manually validated PASS):** `Curvature` is now a selectable entry in the Dynamic Generator Type combo, alongside `MaterialSlot`, `BoundingBox`, and `DirectionalNormal`. Selecting it uses the existing `SetLayerMaskGeneratorType` contract unchanged — a fresh default `FVertexMaskForgeCurvatureParams` (`Concave`, `Multiplier=1`, `Blur=0`, `Levels=[0,1]`, not inverted), discarding any prior variant. The layer's own `Generator Parameters` expander gained a fourth sibling block (`BuildDynamicCurvatureLayerParamsBlock`) exposing exactly six artistic controls — Type (combo, reusing the existing Legacy `CurvatureTypeOptions`/label as immutable presentation data), Multiplier (`[0,10]`), Blur (`[0,10]`, full float precision), Levels Min (`[0,1]`), Levels Max (`[0,1]`), Invert — with `LevelsMin <= LevelsMax` maintained at the UI mutation boundary only, inside a single `MutateDynamicCurvatureParam` call (editing Min above Max raises Max to match; editing Max below Min lowers Min to match; one `SetLayerMaskParams` write, one `OnDynamicLayerStackMutated()` notification per edit). Curvature has no Space concept, so unlike Bounding Box/Directional Normal this block has no incompatible-state warning and no per-control `IsEnabled_Lambda` gate. Every control reads/writes only that specific `LayerId`'s own stored parameters via `MutateDynamicCurvatureParam`, mirroring `MutateDynamicDirectionalNormalParam`'s own six-step identity-validated write path exactly. No shared/panel-level Curvature state was added — only two new private `SVertexMaskForgePanel` methods (the block builder and the mutator; no compatibility predicate was needed). No evaluator math or generator call was copied into Slate; every edit still flows through the same orchestrator preview and Accept both already share. Manually validated end-to-end (items A–G): selector/defaults, all three Type options, live parameter behavior including the Levels invariant and genuinely-nonzero fractional Blur, independent two-layer parameter state with reorder, stable-`LayerId` deletion safety, generator switching, preview/Accept parity, Auto Update Preview on/off behavior, and no Legacy/other-generator regression.

**Contract established (M16-K.6D-8F-B, orchestrator support for Dynamic Noise with independent per-layer transient state):** `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` now accepts a fifth masked-generator type, `Noise`, alongside `MaterialSlot`, `BoundingBox`, `DirectionalNormal`, and `Curvature`. Like Curvature, Noise has no Local/World-space concept or `ComponentTransform` dependency, so its Pass 1 branch has no space-rejection check — it extracts `FVertexMaskForgeNoiseParams` and forwards all thirteen generative fields (via a freshly-built `FVertexMaskForgeNoiseGenerativeParams`) plus the four artistic fields (`Multiplier`/`LevelsMin`/`LevelsMax`/`bInvert`) unchanged to the existing, unmodified `VertexMaskForgeNoiseGenerator::GenerateNoiseMaskFromDynamicMesh`. That function also requires a mutable `FVertexMaskForgeGeneratorState&` for its own raw-noise cache, but unlike Dynamic Curvature's **shared** `CurvatureLocalGeneratorState` (M16-K.6D-8E-B, ADR-013), the orchestrator gives **each enabled Noise layer its own independent, function-local `FVertexMaskForgeGeneratorState`, declared fresh inside Pass 1's per-layer loop body** — a deliberate divergence from the Curvature pattern, not an oversight: Noise's own raw-cache key (`FVertexMaskForgeNoiseGenerativeParams::operator==`) includes the full generative parameter set, not just mesh geometry, so two differently-configured Noise layers would gain nothing from sharing one cache slot (they would simply invalidate and recompute it for each other every layer) — only same-configuration reuse across calls would help, which no orchestrator invocation provides for any generator today. `GenerateNoiseMaskFromDynamicMesh` returns its `FVertexMaskForgeScalarMask` entirely by value, so storing each layer's completed mask via `MoveTemp` is always safe. Noise's output is indexed by DynamicMesh Vertex ID (like Bounding Box's and Curvature's), so it is tagged `ELayerMaskDomain::DynamicMeshVertex` and resolved through the existing `CornerToVertexID`/`TryGetValue` Pass 2 path, unchanged — no new domain, no vertex remapping. 7 new orchestrator tests total (`VertexMaskForge.DynamicSourceTopologyComposition.Noise*`, `TwoNoiseLayersRetainDistinctMasksAndReorderChangesResult`, `DisabledNoiseLayerIsNoOp`, `MultipleNoiseLayersUseIndependentGeneratorState`); `VertexMaskForgeDynamicAcceptTargetBuilder` required zero changes (already fully generator-agnostic).

**Contract established (M16-K.6D-8F-C/8F-C.1, Dynamic Noise/Grunge is user-accessible in Dynamic Layers, with a per-layer Lock Axes workflow convenience; manually validated PASS — "funcionou"):** `Noise` is now a selectable entry in the Dynamic Generator Type combo (artist-facing label **"Noise/Grunge"**; the underlying generator/type names remain `Noise` throughout the code, matching Legacy), alongside `MaterialSlot`, `BoundingBox`, `DirectionalNormal`, and `Curvature` — this is the sixth and, as of this checkpoint, final entry (`None` plus these five generators). Selecting it uses the existing `SetLayerMaskGeneratorType` contract unchanged — a fresh default `FVertexMaskForgeNoiseParams` (`FractalPerlin`, `Scale=1`/`Offset=0` per axis, `Seed=0`, `Octaves=4`, `Roughness=0.5`, `Lacunarity=2`, `TurbulenceStrength=0.5`, `Multiplier=1`, `Blur=0`, `Levels=[0,1]`, not inverted), discarding any prior variant. The layer's own `Generator Parameters` expander gained a fifth sibling block (`BuildDynamicNoiseLayerParamsBlock`) exposing all 17 authoritative fields — Type, Scale X/Y/Z, Offset X/Y/Z, Seed, Octaves, Roughness, Lacunarity, Turbulence Strength, Blur, Multiplier, Levels Min/Max, Invert — grouped "Noise Generation"/"Artistic Adjustment", with `LevelsMin <= LevelsMax` maintained the same way Curvature's own invariant is, inside a single `MutateDynamicNoiseParam` call mirroring `MutateDynamicCurvatureParam`'s own six-step identity-validated write path exactly. No shared/panel-level authoritative Noise state was added; every edit still flows through the same orchestrator preview and Accept both already share; no Generate button exists anywhere in this block.

Alongside the parameter block, the Scale row additionally exposes **"Lock Axes"** (tooltip "Use Scale X for all three axes.") — a Legacy-equivalent workflow convenience, reproducing Legacy's own `bNoiseScaleAxesLocked` scale-editing rule (Scale X is the sole master; enabling copies X absolutely into Y/Z, immediately, but only if they were not already equal — already-equal axes produce no payload write and no recomposition; disabling only flips the UI state and never touches Scale; while locked, editing X assigns the identical clamped value to X/Y/Z in one mutation, and Y/Z's own spin boxes are disabled, with a defensive guard against a stale/programmatic write) — but **not** its ownership model. Legacy's `bNoiseScaleAxesLocked` is a single panel-global bool, which is architecturally incompatible with multiple independent Dynamic Noise layers; Dynamic Lock Axes state instead is ephemeral, panel-owned, **per-layer** UI metadata (`SVertexMaskForgePanel::DynamicNoiseScaleAxesLockedByLayerId`, a `TMap<FGuid, bool>` keyed by stable `LayerId`, read only via a non-inserting lookup, an absent entry meaning unlocked) — never part of `FVertexMaskForgeNoiseParams`, never serialized, never touched by `SetLayerMaskParams`/the orchestrator/any generator, and therefore never affecting generator equality, cache identity, recipes, or asset state. It survives row/widget rebuilds, reorder, rename, and temporary generator switching away from and back to Noise (within the same panel session), and is removed on permanent layer removal; it resets entirely when the panel is destroyed/reopened, exactly like Legacy's own panel-member lifetime. See [Decision Log ADR-014](VertexMaskForgeDecisionLog.md#adr-014--layer-owned-noise-as-the-fifth-dynamic-generator-and-ephemeral-per-layer-lock-axes-metadata) for the full ownership rationale.

The Lock-enable-normalization and Scale-X-edit algorithms themselves are not duplicated inline in these two callbacks — they are two small, production-internal, non-`static` pure functions with external linkage, `VertexMaskForgePanel::NormalizeDynamicNoiseScaleForAxisLock(FVertexMaskForgeNoiseParams&) -> bool` and `VertexMaskForgePanel::ApplyDynamicNoiseScaleXEdit(FVertexMaskForgeNoiseParams&, float, bool) -> void`, declared in `SVertexMaskForgePanel.h` and defined in `SVertexMaskForgePanel.cpp`, operating only on `FVertexMaskForgeNoiseParams`/primitives with no Slate or panel-instance dependency — extracted specifically so this module's own automation tests can call the exact same functions the real callbacks call, rather than reconstructing the algorithm as test-local code. **Automated test coverage is honestly boundary-limited**, consistent with every prior Dynamic Layers UI checkpoint's own established boundary (no test in this codebase constructs `SVertexMaskForgePanel` or a live Preview Component): automation exercises the two production helpers' own contracts directly, and real `FVertexMaskForgeDynamicLayerStack` behavior via the same API shape the panel uses; it does **not** execute the real Slate callbacks, click the checkbox, or read/write `DynamicNoiseScaleAxesLockedByLayerId` itself — that callback-to-helper wiring, and the checkbox's exact label/tooltip/placement/enablement, are established by direct source inspection, and the integrated end-to-end workflow was confirmed manually in the Editor by Lucas ("funcionou").

**Contract established (M16-K.6D-8G-C/8G-D, Dynamic Ambient Occlusion backend — persistent per-component/per-`LayerId` cache ownership and Source-Topology dispatch):** Ambient Occlusion is exposed as the sixth masked-generator type `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` accepts, alongside `MaterialSlot`, `BoundingBox`, `DirectionalNormal`, `Curvature`, and `Noise` — the only generator whose evaluator is unconditionally **World-Space** (it has no Local-space mode at all, unlike Bounding Box/Directional Normal), so unlike those two there is no space-rejection check; instead the orchestrator's own signature grew a `const FTransform& ComponentTransform` parameter (M16-K.6D-8G-B), threaded by both production callers with the real per-component transform (never `FTransform::Identity`), which the AO branch is the first generator to actually consume. Pass 1 extracts `FVertexMaskForgeAmbientOcclusionParams` and translates its identically-named fields 1:1 into a `FVertexMaskForgeAOParams` (the pre-existing raw-generator-input struct), then calls the existing, unmodified `VertexMaskForgeAmbientOcclusionGenerator::GenerateAmbientOcclusionMaskFromDynamicMesh` — no AO math, sampling, or cache-comparison logic is duplicated in the orchestrator. AO's raw mask is indexed by **Normal Overlay Element ID**, a third Pass-2 domain (`ELayerMaskDomain::NormalOverlayElement`) distinct from both `Corner` and `DynamicMeshVertex`: a hard-edge corner shares its Dynamic Mesh VertexID with its neighbors but owns its own Normal Overlay element, so a Dynamic-Mesh-VertexID domain would silently average/smooth hard edges. Each corner's Element ID is resolved via a lazily-built `CornerToElementID` map (mirroring the existing `CornerToVertexID` lazy-build pattern) using `NormalOverlay->GetTriangle(TriangleID)[Corner]` — the exact same lookup Legacy's own `UpdateWorkingColorsSourceTopology` already uses for this generator, never a new/invented rule, and never a source-VertexID/Element-ID positional assumption.
  - **Persistent cache ownership (Model D):** unlike Dynamic Curvature's shared-within-call state (ADR-013) or Dynamic Noise's independent-per-layer-within-call state (ADR-014) — both function-local and discarded at the end of every orchestrator call — Dynamic AO's raw raycast pass is expensive enough that recomputing it on every recomposition (including one triggered by an unrelated layer's Fill/Blend/Opacity edit) would break the "composition-only edits cost nothing" contract Legacy AO's own cache already guarantees. `FVertexMaskForgePreviewComponentState` (M16-K.6D-8G-C) therefore owns a new, persistent, **additive** field — `TMap<FGuid, FVertexMaskForgeSourceTopologyAOCache> DynamicSourceTopologyAOCachesByLayerId` — alongside, never replacing, Legacy's own single-slot `SourceTopologyAOCache`. Each **stable `LayerId`** (never `MaskInstanceId`, never stack index, never a component pointer) owns one complete, independent `FVertexMaskForgeSourceTopologyAOCache`, including its own retained acceleration tree — tree sharing across AO layers on the same geometry is a deliberately deferred optimization, not implemented. See [Decision Log ADR-015](VertexMaskForgeDecisionLog.md#adr-015--persistent-per-componentper-layerid-cache-ownership-model-d-for-dynamic-ambient-occlusion) for the full ownership rationale and the models considered.
  - **Cache-adapter seam:** `GenerateAmbientOcclusionMaskFromDynamicMesh`'s own public interface (unmodified) takes a `TUniquePtr<FVertexMaskForgeSourceTopologyAOCache>&`, while the persistent map stores each cache **by value** — the orchestrator bridges this with a call-scoped adapter (move the map entry's current content into a transient `TUniquePtr`, call the generator, move the result back into the same map entry immediately after, on every reachable return path) rather than changing either the generator's interface or the map's own value type.
  - **Lifecycle:** the map is created only when a real, enabled AO layer's evaluation requires it (`FindOrAdd`, never during UI construction/refresh alone). Layer removal and a genuine generator-type change (both already-established, generic-over-generator-type mutation points since M16-K.6D-8G-C) erase exactly that `LayerId`'s own entry from every selected component's map, mirroring the pre-existing `DynamicNoiseScaleAxesLockedByLayerId.Remove(LayerId)` precedent; a same-type AO reselection is detected as a non-genuine transition and retains the entry; reorder never touches the map at all.
  - **Preview and Accept share the same map:** `ApplyPreviewToEntry`'s Dynamic branch passes `State.DynamicSourceTopologyAOCachesByLayerId` (the same `FVertexMaskForgePreviewComponentState&` already resolved for that component's loop iteration); `VertexMaskForgeDynamicAcceptTargetBuilder::BuildSourceTopologyAcceptTargets` passes `StateOwner->GetVisualSessionStateMutable().DynamicSourceTopologyAOCachesByLayerId` — the same owning component state's own map, never a fresh/temporary one, never Legacy's cache. Accept continues to recompose from `GetBaselineColors()` (never a preview shortcut) and computes correctly on a cache miss, since `FindOrAdd` transparently creates a cold entry the generator then populates.
  - 11 new orchestrator tests (`VertexMaskForge.DynamicSourceTopologyComposition.AmbientOcclusion*`, M16-K.6D-8G-E) prove real dispatch with oracle parity, hard-edge Element-ID domain resolution, cold/warm cache reuse, Levels/Invert raw reuse, composition-only-edit raw preservation, Samples/MaxDistance/Bias raw invalidation with tree retention, a non-identity transform, multi-layer and cross-component cache isolation, reorder retention, disabled-layer no-entry, and transactional-failure cache-adapter restoration; 2 new Accept tests prove cold/warm parity. Two pre-existing negative fixtures that used `AmbientOcclusion` as the "genuinely unsupported generator" sentinel were switched to `Thickness` at the time (later itself repurposed by M17-TH-DL-B once Thickness became a real, dispatched generator — see that paragraph below).

**Contract established (M16-K.6D-8G-F, Dynamic Ambient Occlusion is user-accessible in Dynamic Layers; manually validated PASS):** `Ambient Occlusion` is now a selectable entry in the Dynamic Generator Type combo, inserted immediately after `Bounding Box` — matching Legacy's own generator-library ordering (Bounding Box, Ambient Occlusion, Curvature, Directional Normal, Noise, Material Slot) — while the other five generators retain their prior relative selector order unchanged (as of M17-TH-DL-B, `Thickness` was added last — see its own paragraph below; every generator type is now available). Selecting it uses the existing `SetLayerMaskGeneratorType` contract unchanged — a fresh default `FVertexMaskForgeAmbientOcclusionParams` (`Samples=16`, `MaxDistance=100`, `Bias=0.1`, `Levels=[0,1]`, not inverted), discarding any prior variant. The layer's own `Generator Parameters` expander gained a sixth sibling block (`BuildDynamicAmbientOcclusionLayerParamsBlock`) exposing five controls — Samples (`[8,256]`, raw), Max Distance (`(0.01,10000]`, raw), Bias (`[0.001,10.0]`, raw), Levels Min/Levels Max (`[0,1]`, post-processing, same coupled-invariant pattern as Curvature's own Levels), Invert (post-processing) — using Legacy AO's own exact ranges. Every control reads/writes only that specific `LayerId`'s own stored parameters via `MutateDynamicAmbientOcclusionParam`, mirroring `MutateDynamicCurvatureParam`'s own six-step identity-validated write path exactly, and **never touches `DynamicSourceTopologyAOCachesByLayerId` directly** — raw-cache validity remains entirely the backend's own responsibility (M16-K.6D-8G-C/8G-D above), so this widget introduces no second invalidation policy. No Generate button exists anywhere in this block; every edit flows through the same orchestrator preview and Accept both already share. No shared/panel-level Ambient Occlusion state was added — only two new private `SVertexMaskForgePanel` methods (the block builder and the mutator). Manually validated end-to-end: selector position and defaults, live parameter edits (raw and post-processing) on concave/self-occluding geometry, ordinary composition controls (Fill/Blend/Opacity/Channel Filter/enable/reorder), independent multi-layer and multi-component (including non-identity transform) behavior, generator-switch reset behavior, and Accept (source mesh + instance override)/Cancel/Undo-Redo — no Legacy or other-generator regression.

**Contract established (M17-TH-DL-B, Dynamic Thickness — Source Topology only, reuses the existing production backend, persistent per-`LayerId` cache):** `Thickness` is exposed as the seventh and final masked-generator type `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` accepts — every `EVertexMaskForgeGeneratorType` enumerator is now dispatched, so the orchestrator's trailing `else` (whole-call failure for an unrecognized type) is retained only defensively, never structurally reachable through this UI. `EVertexMaskForgeGeneratorType::Thickness` and `FVertexMaskForgeThicknessParams` already existed before this checkpoint (used only as a data-model/test sentinel); M17-TH-DL-B added no new parameter struct, only the UI/evaluation/cache wiring. Pass 1 extracts `FVertexMaskForgeThicknessParams` and calls the existing, unmodified `VertexMaskForgeThicknessGenerator::GenerateThicknessMaskFromDynamicMesh(CachePtr, WorkingMesh, MinThickness, MaxThickness, SearchDistance, Bias, Blur, bInvert)` — the SAME public entry point the Legacy panel already uses, including that entry point's own internal M9 conservative raycast fallback and fixed confidence gate (committed separately, `75cf7bf6`); no raycasting, fallback, confidence-gate, normalization, or no-hit-detection logic is duplicated in the orchestrator or the UI. Thickness's raw mask is **Corner domain** (`ELayerMaskDomain::Corner`, `Mesh.TriangleCount()*3`), the same domain Material Slot and Directional Normal already use — Pass 2 reads `Values[CornerIndex]` directly and unconditionally (never `TryGetValue`), so, exactly like Directional Normal's own established precedent, an individual corner legitimately left unwritten (no opposing surface found, even after the backend's own internal fallback/gate) requires `NumValidValues == ExpectedCornerCount` or the WHOLE call fails — never a substituted zero/one/Fill Value, never a new validity policy.
  - **Persistent cache ownership (Model D, exact sibling of Dynamic Ambient Occlusion's own — M16-K.6D-8G-C):** `FVertexMaskForgePreviewComponentState` owns a new, persistent, additive field — `TMap<FGuid, FVertexMaskForgeSourceTopologyThicknessCache> DynamicSourceTopologyThicknessCachesByLayerId` — alongside, never replacing, anything Legacy owns. Each stable `LayerId` owns one complete, independent `FVertexMaskForgeSourceTopologyThicknessCache`, including its own retained acceleration tree; two Thickness layers with different Search Distance never cross-contaminate (proven directly by test). The orchestrator's own public signature grew a matching `TMap<FGuid, FVertexMaskForgeSourceTopologyThicknessCache>&` parameter (same position/ownership style as the AO map), threaded by both production callers (`SVertexMaskForgePanel`'s preview call site and `VertexMaskForgeDynamicAcceptTargetBuilder`'s Accept call site) with their own component's map. The generator's own two-layer freshness contract governs invalidation, reused verbatim: the acceleration tree rebuilds on mesh identity/geometry-fingerprint mismatch; raw distances rebuild only when Search Distance or Bias change; Min Thickness/Max Thickness/Invert are remapped fresh from valid raw distances on every call, never invalidating the raycast cache (proven directly by test: Tree pointer and raw-distance array both byte-identical across a Min/Max/Invert-only edit). Blend Mode/Opacity/Fill Value/channel filtering/reorder are — exactly like every other generator — purely Pass-2/recomposition concerns; none of them touch the cache (also proven directly by test).
  - **UI:** the layer's own `Generator Parameters` expander gained a seventh sibling block (`BuildDynamicThicknessLayerParamsBlock`) exposing four controls — Min Thickness (`[0,10000]`), Max Thickness (`[0,10000]`), Search Distance (`[0,10000]`), Invert — using the Legacy Thickness panel's own exact ranges/defaults. Bias and Blur are deliberately never exposed (kept internal, mirroring every M9 fallback/confidence-gate parameter — cone angle, ray count, support tolerance, minimum valid values, median support count, confidence threshold, support lead — none of which are UI or serialized layer parameters). No stronger Min≤Max or Search-Distance≥Max-Thickness coupling was introduced than the Legacy panel's own controls already enforce (neither pair is actually clamped together in Legacy today, despite one tooltip's claim to the contrary) — the backend remains tolerant of an inverted pair, exactly as it already was. Every control reads/writes only that specific `LayerId`'s own stored parameters via `MutateDynamicThicknessParam`, mirroring `MutateDynamicCurvatureParam`'s own six-step identity-validated write path exactly, and never touches `DynamicSourceTopologyThicknessCachesByLayerId` directly. Cache cleanup (layer removal; a genuine generator-type change away from Thickness) mirrors the pre-existing AO cache erasure exactly.
  - **Render Vertex domain:** explicitly out of scope for M17-TH-DL-B — no Render Vertex Dynamic path exists for any generator except Material Slot today (a pre-existing, generator-agnostic gap, not introduced or resolved by this checkpoint).
  - 8 new orchestrator tests (`VertexMaskForge.DynamicSourceTopologyComposition.Thickness*`) prove real dispatch versus Fill-only passthrough, the sparse-mask no-hit contract, two-layer independent cache identity, Search-Distance-invalidates/Min-Max-Invert-reuses classification, exact-complement Invert-applied-once, and Blend/Opacity/reorder recompose-only; the two former "Thickness is unsupported" sentinel tests were repurposed (same fixture/mechanism, updated rationale — Thickness's own backend still requires a Normal Overlay, so a mesh without one still fails the whole call, now for that reason instead of "unsupported type"). 6 new `VertexMaskForge.DynamicLayerStack.Thickness*`-area assertions (one new test) prove generator-type acceptance, exact default `FVertexMaskForgeThicknessParams`, params-update coherence, and fresh-`MaskInstanceId`-on-replacement with stale-instance-id rejection.

**Correction (M17-TH-DL-E, Dynamic Thickness partial-validity tolerance, Levels Min/Levels Max, root cause of the Editor RGB Vertex Color Preview Mode fallback):** real Editor artistic validation (M17-TH-DL-D) reproduced a defect: switching a Dynamic Layer's generator from Ambient Occlusion to Thickness while Preview Mode was `RGB Vertex Color` silently dropped the vertex-color override and fell back to showing the plain mesh material. Root cause: M17-TH-DL-B's own `NumValidValues == ExpectedCornerCount` requirement (described in the paragraph above) demands that **every single corner** resolve a valid Thickness hit, with zero tolerance for even one genuinely unmeasurable corner — but ordinary closed meshes routinely have at least one corner (a concave pocket, a boundary edge, a thin or self-occluding feature) where neither the center ray nor the backend's own internal M9 fallback/gate finds an opposing surface, which is a normal, expected per-corner outcome, not a total generator failure. That single missed corner failed the ENTIRE `ComputeComposedColorsRGBSourceTopology` call, which the panel's preview code (`ApplyPreviewToEntry`) then treated identically to "composition failed," calling `RestorePreviewVisualOnly` — destroying the RGB-Vertex-Color-carrying preview component and un-hiding the real static mesh component (i.e. showing its material). Ambient Occlusion never triggers this path because its own branch has no per-corner completeness requirement at all (`State == Ready` only, sparse-safe by construction via `NormalOverlayElement`/`TryGetValue`).
  - **Correction:** the orchestrator's Thickness branch now distinguishes a genuine **total** no-hit (`NumValidValues == 0` — every corner, center ray and fallback alike, missed; the whole call still fails, unchanged, matching `ThicknessNoHitFailsWholeCallTest`'s own established contract) from a **partial** result (`0 < NumValidValues < ExpectedCornerCount` — the normal real-mesh case). For a partial result, every corner the backend legitimately left unwritten is filled in, locally and only for that one Dynamic Layer evaluation, with a fixed, documented, non-fabricated substitute — `0.0f`, the darkest/"no measurable thickness" end of the mapped range — so Corner-domain Pass 2's own unconditional `Values[CornerIndex]` read (a GLOBAL contract, deliberately unchanged by this correction) always has a real value to read. This mirrors the *intent* (not the mechanism) of Legacy's own established Thickness behavior, where a per-vertex `TryGetValue` miss simply contributes nothing from that one layer at that one vertex (`VertexMaskForgeGeneratorLayerBridge.cpp`) — the dense Corner-domain architecture has no per-corner "skip this layer" primitive, so a fixed neutral substitute is the closest safe equivalent. The substitute is applied BEFORE Levels, so a missed corner always renders as the true darkest value regardless of the artist's own Levels setting.
  - **Levels Min / Levels Max:** two new fields on `FVertexMaskForgeThicknessParams` (`LevelsMin=0.0f`, `LevelsMax=1.0f`), mirroring `FVertexMaskForgeAmbientOcclusionParams`/`FVertexMaskForgeCurvatureParams`/`FVertexMaskForgeNoiseParams`'s own established Levels fields exactly (same names, defaults, `[0,1]` range, `[0.001` epsilon-guarded degenerate-range handling). Applied as a pure postprocess directly in the orchestrator (`(Value-LevelsMin)/max(LevelsMax-LevelsMin,Epsilon)`, clamped `[0,1]`) — never inside the protected backend, never rebuilding the raycast/acceleration structure, never touching `RawDistances` (proven directly by test). **Processing order differs from AO/Curvature/Noise by proven necessity, not by choice:** those three generators apply Levels BEFORE Invert because their own generator file owns both steps together; Thickness's Invert is already baked into `GeneratedMask.Values` by the protected `GenerateThicknessMaskFromDynamicMesh` call before the orchestrator ever sees the result, so Levels can only be applied AFTER Invert here — a proven, generator-specific architectural constraint (Levels lives in an unprotected file; Invert lives in a protected one), documented rather than silently diverging.
  - The layer's own `Generator Parameters` block gained two new controls — Levels Min/Levels Max (`[0,1]`, post-processing, same coupled-invariant/tooltip pattern as Ambient Occlusion's own Dynamic Levels widgets) — inserted between Search Distance and Invert. Bias, Blur, and every M9 internal remain hidden, unchanged.
  - 2 new orchestrator tests prove the corrected contract: `ThicknessPartialValidityToleratedNotWholeCallFailure` (a 24-valid/3-no-hit mixed fixture composes successfully; the 3 no-hit corners receive the documented substitute, the 24 valid corners keep their real measured values) and `ThicknessLevelsMinMaxContract` (identity no-op, formula-verified raised Levels Min / lowered Levels Max, deterministic degenerate-range hard step, and the proven Levels-after-Invert processing order). The existing `ThicknessParamInvalidationClassificationTest` gained a third block proving Levels Min/Max changes never touch the Tree, `CachedSearchDistance`, `CachedBias`, or `RawDistances`. All 45 authoritative `ThicknessDiagnostic` tests and all 17 `WorkingMeshDomainSplit` tests remain unchanged and passing — the production Thickness raycasting algorithm and the automatic M9 fallback were not modified.
  - **Interactive responsiveness:** investigated but NOT changed in this checkpoint. No parameter anywhere in this codebase (Thickness or otherwise) currently distinguishes drag-time (`OnValueChanged`) from commit-time (`OnValueCommitted`) evaluation — Ambient Occlusion's own expensive raycast-affecting controls (Samples, Max Distance) already recompute synchronously on every drag tick, identically to Thickness's Search Distance. The perceived "stall" reported alongside the preview-fallback defect is plausibly explained, at least in part, by the SAME root cause corrected above: every time a drag tick crossed into a corner-validity boundary, the whole composition call failed and the panel tore down and later recreated the entire RGB-Vertex-Color preview component (`RestorePreviewVisualOnly` → `EnsureSourceTopologyPreviewComponent`) — expensive component churn that no longer fires once partial results are tolerated. No new debounce/coalescing infrastructure was added; none exists anywhere in this codebase to reuse, and inventing one was explicitly out of this checkpoint's scope.

**Contract established (M18, single Layers workflow — Legacy/Dynamic consolidation):** `Layers` (artist-facing rename of "Dynamic Layers") is now the SOLE workflow the panel presents and operates through. The Legacy fixed 7-generator UI (`~2,336` lines of inline `SExpandableArea` sections built directly in `Construct()`) and the Legacy/Dynamic Preview Source combo (`EVertexMaskForgePreviewSource`'s own widget, `OnPreviewSourceSelectionChanged`, `PreviewSourceOptions`) were removed. `SVertexMaskForgePanel::PreviewSource` now defaults to (and has no remaining write site to become anything other than) `Dynamic`. Every production read of it except one was collapsed to its unconditional Layers-only body: `OnDynamicLayerStackMutated` always recomposes; `RecomputeOperationState` always uses the Layers-only pending-ness signal (`HasAnyEnabledLayer()` + at least one live PreviewComponent); `AcceptPendingChanges` always builds Accept targets via `VertexMaskForgeDynamicAcceptTargetBuilder::BuildSourceTopologyAcceptTargets` (its earlier Legacy-only World-Space Directional Normal pre-Accept resync guard was removed with it). The ONE remaining read, deep inside `ApplyPreviewToEntry`'s Source-Topology branch, guards a large (~340-line), deeply nested, dual-domain Legacy composition block that was deliberately NOT excised (removing it safely would require restructuring that one function beyond this checkpoint's own UI-consolidation scope) — see `EVertexMaskForgePreviewSource`'s own doc comment (`SVertexMaskForgePanel.h`) for the full technical-debt justification: the guarding condition is provably always true (no write site reaches `Legacy` anymore), so that block is unreachable dead code, not a second live workflow. The global Channel Filter (`bChannelFilterR/G/B`, checkboxes removed) and Fill White/Fill Black (`OnFillWhiteClicked`/`OnFillBlackClicked`/`RunConstantFill`/`CanRunFill`, all deleted) were removed the same way — `bChannelFilterR/G/B` are kept at their default (`true`) only because the same dead Legacy branch still reads them; Fill Black/White had no reachable caller left once their buttons were removed, so their functions were deleted outright. Per-layer R/G/B channel controls (`FVertexMaskForgeDynamicLayerStack::SetLayerChannelFilter`) and per-layer Fill Value are unaffected and remain the live mechanism. The Legacy-only "Active layers: ..." status readout (`GetActiveMaskSourceText`) was removed (it could only ever report "None" once its backing Legacy enable-flags had no UI left to set them). `Bounding Box` is now shown to the artist as `Position` (label change only — `GetDynamicLayerGeneratorTypeLabel`; the enum, generator, cache identity, and numerical output are all unchanged). The Dynamic Layers generator picker (`DynamicLayerGeneratorTypeOptions`, the single authoritative option list every picker reads) is now ordered alphabetically by artist-facing label: Ambient Occlusion, Curvature, Directional Normal, Material Slot, Noise/Grunge, Position, Thickness.
  - **Not removed, and why:** the ~1,239-line block of Legacy-only per-generator Slate callback functions (`OnAOEnableChanged`, `OnCurvatureTypeSelectionChanged`, `OnThicknessRaycastParamChanged`, etc., `SVertexMaskForgePanel.cpp` roughly lines 2380-3630) and the Legacy `BuildBoundingBoxAxisRow` row builder were investigated for removal but found to be INTERLEAVED with functions Layers still calls (`OnGenerateDynamicLayerFillRow`, `OnGenerateDynamicLayerGeneratorTypeRow`, `OnGenerateDynamicMaterialSlotPickerRow` all live inside the same contiguous region) — a clean, low-risk extraction was not achievable within this checkpoint's own audit-then-edit discipline, so they remain as orphaned-but-compiling dead code (confirmed via a successful build with every calling Legacy widget already removed) rather than risk a large, error-prone deletion. Their own artist-facing strings (e.g. one remaining `"Enable at least one Bounding Box axis, ..."` message inside `RunAutoUpdatePreview`) are consequently also unreachable, not corrected.
  - **Regression coverage:** no test file was modified in this checkpoint — the full, unmodified `VertexMaskForge` automation suite (454/454) was rerun and passes unchanged, proving the panel-only consolidation introduced no regression in any generator, composition, cache, or Accept-target-building backend (none of which this checkpoint touched). No new automated coverage was added for the panel-level routing/removal itself, consistent with this codebase's own long-established boundary (§16 below) that no test constructs `SVertexMaskForgePanel` or a live Preview Component — manual Editor validation (owned by the user) is the verification path for this checkpoint's own changes.

**Correction (M18.1, Material Slot Layers integration — missing resolution-validity feedback):** manual M18 validation found a blocking symptom: a Material Slot layer with a configured slot selection never visibly responded to Preview Mode — the mesh stayed on Original Material regardless of which Preview Mode was selected. Root cause, proven by direct inspection (the composition dispatch and generator files themselves have zero diff since before M18 and are structurally identical to every other working generator's own branch, confirmed by the pre-existing, still-passing `MaterialSlotGeneratorFailureFailsWholeCall`/`SingleMaterialSlotLayerWhiteFillCopyProducesExpectedCoverage` tests): the Dynamic Layers Material Slot per-layer widget's `IsEnabled_Lambda` for its slot picker and Invert checkbox checked only `WorkingMesh.MaterialSlotOptions.IsEmpty()`, never `WorkingMesh.bMaterialSlotResolutionValid` (set by `BuildMaterialSlotLookups` when a mesh's imported Material Slot names cannot be resolved unambiguously — duplicate or missing names). On such a mesh, the picker looked fully editable and let the user pick a slot, but `VertexMaskForgeMaterialSlotGenerator::GenerateMaterialSlotMaskFromDynamicMesh`'s own pre-existing, unchanged precheck always returns `Unavailable` for that same condition, so the orchestrator's Material Slot branch always returned `false`, and the panel's own established preview-fallback contract (any composition failure → `RestorePreviewVisualOnly`) kept the mesh on Original Material every time, independent of Preview Mode — exactly the reported symptom. The Legacy Material Slot panel (removed by M18) had its own diagnostic text for this exact case (`GetMaterialSlotMaskDiagnosticText`, now dead code); the Dynamic Layers widget never had an equivalent, so once M18 made Layers the sole path this pre-existing gap became fully blocking for the first time.
  - **Correction:** both the picker's and the Invert checkbox's `IsEnabled_Lambda` now also require `bMaterialSlotResolutionValid`; a third inline diagnostic message (mirroring Legacy's own wording: *"Material Slot unavailable: one or more Material Slots could not be resolved unambiguously (duplicate or missing slot names). Preview/Accept for this layer are blocked."*) is now shown, mutually exclusive with the existing "wrong asset count"/"no slots" messages. This is a UI-feedback correction only — it does not change, weaken, or add any new validity policy: the generator's own established unavailable/error contract (traced above) is unchanged and was already correctly enforced by the backend; the fix only makes an already-real failure visible instead of silent. Two stale "prototype -- assignment only; not yet generated, composed, or previewed" tooltips (picker and Invert, both factually wrong since M16-K.6C-2 actually wired this generator into real composition) were also corrected in passing.
  - **Not the fix, ruled out with evidence:** the composition dispatch branch (`VertexMaskForgeDynamicSourceTopologyComposition.cpp`, unmodified), the generator itself (`VertexMaskForgeMaterialSlotGenerator.cpp`, unmodified), the six-step stale-widget identity check on both the picker's and Invert's write paths (structurally identical to every other generator's own `MutateDynamicXParam` pattern), and the per-layer `FVertexMaskForgeMaterialSlotParams` storage/mutation path were all inspected and found correct and untouched by M18 — none of these were the defect.
  - **New regression test** (`VertexMaskForge.DynamicSourceTopologyComposition.MaterialSlotResolutionInvalidFailsWholeCallCleanly`): proves, at the real composition seam, that an otherwise-identical fixture succeeds when `bMaterialSlotResolutionValid` is left at its default `true` and fails cleanly (never corrupting the output array) when forced `false` — isolating this one field as the exact gate, confirming the backend contract the new UI message now surfaces was already correct.
  - **User validation: CONFIRMED.** The user validated Material Slot as functioning after this correction.

**Investigation (M18.2, layer priority / Copy composition — resolved as a false positive, no production defect):** the user reported an apparent defect: two enabled layers writing the same channel, both `Copy` at `Opacity=1`, where the visually upper layer did not appear to have complete authority and instead looked interpolated with the layer below. This checkpoint traced the complete production path (`FVertexMaskForgeDynamicLayerStack`'s stored array order and `MoveLayer`/`MoveLayerUp`/`MoveLayerDown` mutation; `RebuildDynamicLayersList`'s reversed-render mapping, already corrected in an earlier checkpoint, M16-K.6D-6; `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology`'s Pass 2 sequential fold, `Layers[0..N-1]` in order; `PaintValue = FillValue * EffectiveMask`, computed once; `VertexMaskForgeSequentialEvaluator::EvaluateFillLayerStep` → `BlendMaskValueUnclamped(Base, Mask, Copy, Opacity) = lerp(Base, Mask, Opacity)`, which reduces to exactly `Mask` — the upper layer's own `PaintValue` — at `Opacity=1`) and found no defect in any of it: array order, the reversed display mapping, the fold direction, the Copy formula, and per-channel gating are all internally consistent and mathematically correct for full-authority replacement at `Opacity=1`.
  - **Empirical proof, not just static reading:** two deterministic regression tests were added and both PASS on the production code, unmodified: `CopyFullOpacityUpperLayerCompletePriority` (Fill-only Black/White layers — proves upper-wins-completely at full Copy opacity, that `Stack.MoveLayer` immediately flips the result and moving back restores it, that partial opacity `0.5` still interpolates exactly once, that a three-layer stack yields the topmost ENABLED layer's value and that disabling/deleting it correctly reveals the next one, and that a channel a layer doesn't write is left untouched) and `CopyFullOpacityPriorityTwoMaterialSlotLayers` (two real Material Slot layers with different, genuinely distinct slot selections, proving the evaluator receives distinct lower/upper paint values from the SAME generator type and the upper one still wins completely per corner). Both are retained as permanent contract coverage for the sequential-fold/Copy/reorder/channel-gating behavior they exercise — not as "bug fix" tests, since no bug was found.
  - **Resolution:** the user subsequently confirmed the apparent interpolation was caused by the order of `Lerp` operations in the user's own material/shader, external to Vertex Mask Forge — not a defect in this plugin's evaluator, stack order, or `Copy` implementation. **No production correction was made or is required.** The sequential evaluator, `Copy` semantics, layer array order, and reorder mapping are byte-for-byte unchanged from before this investigation.

**Contract established (M19, Alpha as a fourth independently-gated Layers channel — backend M19-A, artist-facing controls M19-B; user manual validation: PASS):** Alpha is now a real, per-layer composable channel throughout the Layers workflow, reusing every existing composition primitive with zero Alpha-specific math anywhere. `FVertexMaskForgeLayer` gained a fourth Channel Filter bit, `bAffectAlpha` (default `false` — asymmetric and intentional; every pre-M19 layer, and every newly created layer until the artist explicitly opts in, leaves Alpha exactly as untouched as it was before this checkpoint). `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology`'s Pass 2 fold now seeds its running composite from all four `BaseColor` components (`FVector4f`, not `FVector3f`) and gates Alpha exactly like R/G/B: `Composite.W = Layer.bAffectAlpha ? LayerOutput.W : Composite.W`, using the identical `FillValue * EffectiveMask` `PaintValue` formula and the identical `VertexMaskForgeSequentialEvaluator::EvaluateFillLayerStep` scalar-blend primitive (a new `FVector4f` overload was added specifically so the pre-existing `FVector3f` overload — still used by the unrelated Legacy/Recipe `VertexMaskForgeGeneratorLayerBridge`/`VertexMaskForgeFillLayerResolution` callers — never needed to change; no independent or duplicated Blend Mode math exists for Alpha anywhere). Alpha is clamped once, at the same final point as R/G/B, and quantized through the same single `VertexMaskForgeColorConversion::ToDisplayFColor` call — the old unconditional `FinalColor.W = BaseColor.W` passthrough this section previously documented (see §9's prior wording, now corrected below) no longer exists as a separate step; disabled-Alpha preservation now falls out of the same seed-plus-gating mechanism R/G/B already used, not a special case. `WorkingColors`, `VertexMaskForgeDynamicAcceptTargetBuilder::BuildSourceTopologyAcceptTargets` (which re-invokes the identical orchestrator call from each component's own `GetBaselineColors()`), `VertexMaskForgeAcceptWriter`, Undo/Redo, and persistence required **zero changes** — all four already carried the full `FLinearColor`/`FColor` including Alpha with no channel-specific logic, so composed Alpha flows through them automatically. `FVertexMaskForgeDynamicLayerStack::SetLayerChannelFilter` and the pure UI decision helpers `EVertexMaskForgeDynamicLayerChannel`/`FVertexMaskForgeDynamicLayerChannelToggleResult`/`ResolveDynamicLayerChannelToggle` (all Slate-free, `VertexMaskForgeLayerTypes.h`) were extended from three booleans/channels to four, symmetrically — Alt+click Alpha isolates exactly like Alt+click Red/Green/Blue, and Alt+click Red/Green/Blue now also forces Alpha off (proven by test, not merely implied). No generator file was modified — every one of the seven generators already produced a channel-agnostic scalar mask; the only production surfaces that ever hardcoded three channels were the Pass 2 fold, the Layer struct, and the toggle helper, all listed above.

An artist-facing `A` checkbox was added to `SVertexMaskForgePanel::BuildDynamicLayerRow`, a structural copy of the existing `B` checkbox (same widget type/dimensions/spacing/style, same `LayerId`-resolved-fresh-per-call pattern, same two-step stale-widget guard, same `ResolveDynamicLayerChannelToggle`/`SetLayerChannelFilter`/`OnDynamicLayerStackMutated()` call chain) — no Alpha-specific Fill Value, Blend Mode, Opacity, or generator control was added. The existing layer-row tint mechanism (`EVertexMaskForgeDynamicLayerChannelTint`/`ResolveDynamicLayerChannelTint`/`GetDynamicLayerChannelTintColor`, all `VertexMaskForgeLayerTypes.h`) gained a `White` case, implementing that enum's own pre-existing "FUTURE DECISION" doc comment verbatim: exactly-one-of-four channels active resolves to that channel's own tint (Red/Green/Blue unchanged; Alpha-only now resolves to `White`), using the exact same shared `TintAlpha` opacity constant already used for Red/Green/Blue — no second opacity constant exists. Because the tint resolver is a pure function of the four channel booleans only, manually reaching Alpha-only ownership (three separate unchecks/checks, never Alt-click) and reaching it via Alt+click A are provably state-identical and therefore render the identical tint — proven directly by test, not merely asserted. The tint remains layer-row UI presentation only: it does not read or write `WorkingColors`, does not touch `EVertexMaskForgePreviewMode`/`VertexMaskForgeDisplayColorDerivation::DeriveDisplayColors`, and has no effect on mesh/viewport presentation or Original Material behavior — Preview Mode and per-layer channel ownership remain the same two independent, orthogonal mechanisms §9 already documents.

**User manual validation: PASS (11/11).** The artist confirmed: the `R G B A` controls appear as expected; a fresh layer shows `R/G/B` checked and `A` unchecked; normal-clicking `A` changes only Alpha; `Alt+click A` leaves only Alpha enabled; an Alpha-only layer receives the white row tint at the existing RGB tint opacity; manually reaching Alpha-only produces the identical tint; `Alt+click R/G/B` preserve their existing red/green/blue tints unchanged; none of these interactions changes Preview Mode or shows a white Alpha visualization on the mesh; reorder keeps the `A` control targeting the correct layer; all four channels disabled is accepted without corruption; and Alpha behaves correctly through Accept, Undo, Redo, and Cancel.

**Contract established (M20-D/D.1, Instance Override route investigated and removed):** an earlier interim checkpoint (M20-C) had added a transient "Send to Mesh Paint Texture" bridge alongside a separate, persistent "Instance Override" route that wrote Forge results directly into a component's own `OverrideVertexColors` as a standing (non-Accept) artifact. M20-D investigated extending that persistent route to Source Topology (Nanite) selections and found no exact, engine-backed mapping exists from Source-Topology corner data to a render-vertex `OverrideVertexColors` buffer that preserves correctness for an arbitrary mesh — the render-vertex domain and the Source-Topology (corner) domain are not interchangeable without a lossy or ambiguous remapping (see [§3](#3-domain-model) for why these two domains cannot be implicitly combined). Because no correct general mapping could be established, the entire persistent Instance Override route (`VertexMaskForgeInstanceOverrideWriter` and its own UI entry point) was removed rather than shipped with a silently-incorrect Source-Topology path. The M20-C transient bridge was deleted, uncommitted, in the same working period. Accept (writing consolidated colors into the Static Mesh asset's own source vertex colors) is unaffected and remains the plugin's sole persistent-authoring route. See [Decision Log ADR-016](VertexMaskForgeDecisionLog.md#adr-016--restoration-of-the-transient-send-to-mesh-paint-texture-bridge-removal-of-the-persistent-instance-override-route) for the full decision record.

**Contract established (M20-E, restoration of the transient "Send to Mesh Paint Texture" bridge):** `VertexMaskForgeComponentOverrideBridge` (`VertexMaskForgeComponentOverrideBridge.h/.cpp`, new files) reconstructs M20-C's own bridge from behavioral evidence only, narrowed to a well-defined contract: it reads vertex colors that have ALREADY been consolidated into the Static Mesh asset by a prior Accept — read verbatim from the asset's rebuilt LOD 0 `FStaticMeshLODResources::VertexBuffers.ColorVertexBuffer`, never from Forge's own live, possibly-unaccepted `WorkingColors` — and transfers them through Unreal's own native "From Vertex" Mesh Paint Texture import (`UMeshPaintModeSubsystem::ImportMeshPaintTextureFromVertexColors`, `MeshPaintEditorMode` module). A temporary per-component `OverrideVertexColors` buffer is installed only for the synchronous duration of that one native call (the native import sources from a component's existing override when present, falling back to the asset's own baked colors only when none exists — so a component with a stale pre-existing Legacy Vertex Paint override would otherwise have that stale data baked instead of the freshly-accepted Forge result), then the component's exact prior override state (presence, byte-for-byte RGBA, `PaintedVertices`) is restored unconditionally, on success or failure, before the call returns. The bridge never writes to, builds, or dirties the Static Mesh asset itself, never leaves a persistent Forge-owned override behind, never toggles Nanite settings, and never shares one `FColorVertexBuffer` between two components. It is exposed in the panel as a "Send to Mesh Paint Texture" button, positioned where the removed Instance Override control previously sat, with its own independent status line (`GetSendToMeshPaintTextureStatusText`) — deliberately separate from Accept's own `GetOperationStatusText`, since the two are independent operations that can each report their own last result. `BuildTransferTargets` performs read-only validation (asset resolves with valid LOD 0 render data and a non-empty `ColorVertexBuffer`; `UStaticMeshComponent::CanMeshPaintTextureColors()` is true) and is explicitly documented as unable to distinguish "genuinely Accepted colors" from an asset's own default-white buffer (a real Static Mesh LOD 0 essentially never has a zero-vertex `ColorVertexBuffer` after a normal build, Accepted or not) — this is a known, honestly-documented limitation, not a defect. `TransferToMeshPaintTexture` performs the capture/install-temporary-override/native-import/restore sequence per target, wrapped by the caller in one `FScopedTransaction` so the native import's own real, persistent side effect (the component's Mesh Paint Texture assignment) remains Undo/Redo-capable. 7 new automated tests (`VertexMaskForge.ComponentOverrideBridge.*`) prove: accepted-asset-colors-not-WorkingColors provenance, default-white-asset eligibility, session-independent candidate eligibility, byte-for-byte override restoration (both from a pre-existing override and from no override at all), restoration on a pre-native-call failure path, and no buffer sharing across multiple components — using a test-only injection seam (`InNativeImportOverride`, default-unbound in every production call site) that exercises the real capture/install/restore code around a simulated native result, never a reimplementation of the real GPU-touching bake itself.

**Contract established (M20-E.1, selection-independent enablement):** the M20-E button was initially gated on `SelectedMeshes` — the Edit Vertex Mask session's own live targets, populated only while a session is active and cleared the instant Accept/Cancel concludes one — which meant the button was incorrectly disabled whenever no Edit Vertex Mask session happened to be open, even though the bridge's real color authority (the accepted Static Mesh asset's own `ColorVertexBuffer`) has no dependency on session state at all. This checkpoint introduces a second, session-independent candidate list, `MeshPaintTransferCandidateMeshes`, refreshed via `RefreshMeshPaintTransferCandidates()` (the same `CollectViewportSelection`/`UpdateMeshDiagnostics` pair `RefreshCandidateSelection()` already uses) on every `OnEditorSelectionChanged()` call — unconditionally, regardless of `bIsEditingVertexMask` — and once more at panel construction so the button is correctly enabled from the very first paint. `CanSendToMeshPaintTexture()` now reads this list instead of `SelectedMeshes`; `OnSendToMeshPaintTextureClicked()` additionally re-resolves it fresh at the moment of the click, so a possibly one-frame-stale `IsEnabled` snapshot can never target a since-deselected component. This is a pure enablement/targeting fix — it does not change `CandidateMeshes`, `SelectedMeshes`, `WorkingMeshes`, the Edit Vertex Mask session lifecycle, Accept, or Cancel in any way.

**Incident (M20-E.2, read-only audit — non-blocking, unproven):** during manual bridge validation, a native UE 5.8 engine `ensure(!bAlreadyInSet)` was observed once in `MeshPaintVirtualTexture.cpp`, outside this plugin's own code. A source-level audit found no proven Forge defect and the incident did not reproduce under repeated manual use or under this checkpoint's own full automated suite run. No speculative workaround was added. See [Decision Log ADR-016](VertexMaskForgeDecisionLog.md#adr-016--restoration-of-the-transient-send-to-mesh-paint-texture-bridge-removal-of-the-persistent-instance-override-route) for the full disposition and the condition under which it would be reopened.

**Current Implemented Route vs. Planned Future Route.** As of M20-E.1, the only implemented path from an Accepted result to a Mesh Paint Texture is: `Accept` (writes consolidated colors into the Static Mesh asset's source vertex colors, via the existing `VertexMaskForgeAcceptWriter`/`VertexMaskForgeDynamicAcceptTargetBuilder` path, unchanged) → `Send to Mesh Paint Texture` (`VertexMaskForgeComponentOverrideBridge`, this checkpoint) → native `From Vertex` import → a `UMeshPaintVirtualTexture` Mesh Paint Texture the artist can continue painting manually in Unreal's own Mesh Paint mode. A **Direct Bake** architecture — composing and baking the Forge result straight into a target Mesh Paint texture without depending primarily on this transfer button or on Unreal's native "From Vertex" import — is a **planned future route only**, not implemented by this checkpoint or any prior one. It must not be described as implemented anywhere in this codebase's documentation. The next technical phase is a complete native Mesh Paint architecture audit (ownership, serialization, texture creation/component association, resource lifecycle/persistence, supported bake entry points) to determine how Direct Bake should actually be designed — see the Future Decision Candidates in the [Decision Log](VertexMaskForgeDecisionLog.md) for the specific open questions this audit must resolve before any Direct Bake implementation begins.

---

## 3. Domain Model

### Render Vertex Domain

- **Cardinality:** number of render vertices in the selected LOD (`FStaticMeshLODResources`, typically LOD0).
- **Identity:** a plain positional index into the LOD's vertex buffers — one entry per render-buffer vertex, not per unique geometric position; UV/normal/tangent/material-boundary splits routinely duplicate a single geometric position across several render-buffer vertices.
- **Storage:** `FVertexMaskForgeWorkingMesh` references the LOD's own render buffers (positions, normals, material-slot-per-triangle resolved to per-vertex where unambiguous).
- **Baseline:** `BaselineColors` (`FVertexMaskForgePreviewComponentState`).
- **Used when:** the source Static Mesh is a conventional (non-Nanite, or Nanite-with-fallback-editable) asset being edited via its render LOD.

### Source Topology Domain

- **Cardinality:** `TriangleCount * 3` — one entry per triangle **corner**, not per shared vertex.
- **Identity:** a corner index; two corners of two different triangles sharing the same vertex position are still distinct entries.
- **Storage:** `FVertexMaskForgeWorkingMesh::Mesh` (`UE::Geometry::FDynamicMesh3`), built for Nanite/dynamic-mesh editing paths.
- **Baseline:** `SourceTopologyBaselineColors` (`FVertexMaskForgePreviewComponentState`).
- **Used when:** the working geometry is represented as a `FDynamicMesh3` (Nanite meshes, or any path that chose corner-exact resolution over shared-vertex resolution).

### Why domains cannot be implicitly combined

**Known Limitation.** `FVertexMaskForgeInstanceMaskResult` (the payload stored per `MaskInstanceId` in `FVertexMaskForgeInstanceResultStore`) carries no domain tag, and `FVertexMaskForgeInstanceResultStore`'s key is a bare `FGuid`. Nothing in the type system prevents generating a source-topology result and a render-vertex result under the *same* `MaskInstanceId` — the second `StoreOrReplace` call would silently overwrite the first. This was identified and explicitly deferred during the M16-K.5I audit sequence; every checkpoint that touches this store must choose one domain per `MaskInstanceId` by convention, not by any structural guarantee.

### Domain comparison

| | Render Vertex | Source Topology |
|---|---|---|
| Cardinality | Render vertex count | `TriangleCount * 3` |
| Identity granularity | Per render-buffer vertex; geometric positions may be duplicated at attribute seams | Per triangle corner |
| Geometry source | `FStaticMeshLODResources` | `FDynamicMesh3` |
| Baseline array | `BaselineColors` | `SourceTopologyBaselineColors` |
| Committed array | `CommittedColors` | `SourceTopologyCommittedColors` |
| Working array | `WorkingColors` | `SourceTopologyWorkingColors` |
| Typical use case | Conventional render LOD editing | Nanite / dynamic-mesh editing |
| Cross-domain result reuse | Not supported (no domain tag on stored results) | Not supported (same reason) |

---

## 4. Ownership and State

**Current Contract**, confirmed by direct source read:

- **`FVertexMaskForgeWorkingMesh`** — the working geometry payload for one asset entry: the `FDynamicMesh3` (source-topology domain), material-slot-per-triangle mapping, and its own `InstanceResults` store (per-entry, shared across every component of that asset). Owned by exactly one `FVertexMaskForgeWorkingMeshOwner` at a time, installed via `InstallWorkingMesh`.
- **`FVertexMaskForgeWorkingMeshOwner`** — owns geometry **identity** (`ConfiguredStaticMesh`, `LODIndex`, `bUseSourceTopology`) and the currently-installed `FVertexMaskForgeWorkingMesh`, plus a `Provenance` record (`MeshOwnerId`, `Revision`/generation, `ExpectedCardinality`, domain flag) that every attached `FVertexMaskForgeWorkingStateOwner` authenticates against. `GetWorkingMesh()` is **const-only** — there is no mutable accessor; a new/regenerated `FVertexMaskForgeWorkingMesh` must be built standalone and moved in via `InstallWorkingMesh`.
- **`FVertexMaskForgeWorkingStateOwner`** — one instance per placed component (`FVertexMaskForgePreviewComponentState`). Owns that component's `BaselineColors`/`CommittedColors`/`WorkingColors` (and their `SourceTopology*` siblings) and the authoritative alpha metadata over `WorkingColors`. Attaches (weakly) to a `FVertexMaskForgeWorkingMeshOwner`; never owns geometry itself.
- **`FVertexMaskForgeDynamicLayerStack`** — a single, panel-global instance (`SVertexMaskForgePanel`'s own member), not per-entry and not per-component. Owns the ordered list of `FVertexMaskForgeLayer` and their optional `FVertexMaskForgeGeneratorMaskInstance`.
- **`InstanceResults`** (`FVertexMaskForgeInstanceResultStore`) — has **two** real owners: `FVertexMaskForgeWorkingMesh::InstanceResults` (per-entry/asset, intended for transform-independent generators) and `FVertexMaskForgePreviewComponentState::InstanceResults` (per-component, intended for transform-dependent generators). No generic dispatcher chooses between them today — a caller must know which one applies (see [§16 Known Limitations](#16-known-limitations-and-risks)).
- **`LayerId`** (`FVertexMaskForgeLayer::LayerId`, `FGuid`) — identifies a layer's position/configuration inside a `FVertexMaskForgeDynamicLayerStack`. Assigned once by `AddLayer`, never regenerated by rename/move.
- **`MaskInstanceId`** (`FVertexMaskForgeGeneratorMaskInstance::MaskInstanceId`, `FGuid`) — identifies one generator's stored result in `InstanceResults`, independent of `LayerId`. A layer's mask can be replaced with a different generator type, minting a fresh `MaskInstanceId` while `LayerId` stays the same.

### State ownership table

| State/Data | Owner | Domain | Mutability | Main consumer |
|---|---|---|---|---|
| `Mesh` (`FDynamicMesh3`) | `FVertexMaskForgeWorkingMesh` | Source Topology | Rebuilt wholesale on refresh | Source-topology generators, `ComposeColors` result-store domain |
| `InstanceResults` (per-entry) | `FVertexMaskForgeWorkingMesh` | Either (caller-chosen) | Replaced per `MaskInstanceId` via `StoreOrReplace` | `EvaluateColor` / `ComposeColors` |
| `InstanceResults` (per-component) | `FVertexMaskForgePreviewComponentState` | Either (caller-chosen) | Same as above | Same as above |
| `BaselineColors` / `SourceTopologyBaselineColors` | `FVertexMaskForgeWorkingStateOwner` | Matches its own domain flag | Captured once (`InitializeColors`/`EnsureBaselineCaptured`), invalidated on reattach/rebuild | Legacy composition base; Dynamic batch composition base |
| `CommittedColors` / `SourceTopologyCommittedColors` | `FVertexMaskForgeWorkingStateOwner` | Matches its own domain flag | Promoted from `WorkingColors` only via `ApplyComposedColorsRGB(..., Consolidate)` | Global Channel Filter fallback |
| `WorkingColors` / `SourceTopologyWorkingColors` | `FVertexMaskForgeWorkingStateOwner` | Matches its own domain flag | Written by `ApplyComposedColorsRGB` (explicit composed-output application), and by the restoration operations `RestoreFromBaseline`/`RestoreFromCommitted` — never by any pure composition function | Accept target; display derivation input |
| Batch-composed local output | Local to the caller (`TArray<FColor>&` out-parameter) | Caller-chosen | Freshly computed every call, never stored on any owner | Nothing yet in production (Dynamic path only) |
| Display colors | Local, transient | Matches `WorkingColors`' domain | Recomputed every call, never persisted | Viewport visualization only |

---

## 5. Color State Lifecycle

**Current Contract.**

- **Captured/Baseline** — captured once per component, from whichever source is authoritative first (an existing per-instance vertex color override, then the asset's own baked colors, then white). `BaselineColors`/`SourceTopologyBaselineColors` are immutable after capture until the owner is reset or reattached to a different/rebuilt mesh owner.
- **Committed** — still exists in the current model. `CommittedColors`/`SourceTopologyCommittedColors` serve as the fallback value for any channel the global Channel Filter has disabled (see [§9](#9-channel-semantics)), and are promoted to match `WorkingColors` only when `ApplyComposedColorsRGB` is called with `EColorCommitMode::Consolidate` (the real panel's own Fill Black/White path).
- **Working** — the live preview state. `FVertexMaskForgeWorkingStateOwner::ApplyComposedColorsRGB` is the explicit application point for a freshly composed output: it forces Alpha from the current `BaselineColors` regardless of what the caller supplied, validates cardinality, and optionally promotes `CommittedColors` (`Consolidate` mode) or leaves it untouched (`PreviewOnly` mode). `WorkingColors` is also written by the restoration operations `RestoreFromBaseline` (copies from `BaselineColors`, non-consolidating) and `RestoreFromCommitted` (copies from `CommittedColors`, non-consolidating) — these are owner-state mutations in their own right, distinct from composition. What never writes `WorkingColors` is any *pure composition function* (see below).
- **Composed output (local)** — both `VertexMaskForgeGeneratorLayerBridge::ComposeGeneratorLayersSequential` (legacy) and `VertexMaskForgeDynamicLayerBatchCompositor::ComposeColors` (Dynamic) are **pure**: they compute and return a result without mutating any owner state. Composition stays pure until one of the explicit owner-state mutation operations above (`ApplyComposedColorsRGB`, `RestoreFromBaseline`, `RestoreFromCommitted`) is called. `ComposeColors` in particular is explicitly proven (M16-K.5J) to never write `WorkingColors` and to leave `BaselineColors` untouched.
- **Display colors** — `VertexMaskForgeDisplayColorDerivation::DeriveDisplayColors(WorkingColors, PreviewMode)` reduces `WorkingColors` for on-screen visualization (e.g. isolating a single channel). It is stateless, never mutates `WorkingColors`, and its output is applied only to the viewport render state, never stored back into any owner.
- **Accept** — reads `WorkingColors`/`SourceTopologyWorkingColors` directly (never through `DeriveDisplayColors`) and writes into the real `UStaticMesh`'s source `FMeshDescription` vertex-instance colors, inside a single `FScopedTransaction`.
- **Cancel** — does not revert `WorkingColors` to any prior value; it discards the entire session's owning objects (see [§12](#12-persistence-boundaries)).

**Do not confuse** `ComposeColors`/`ComposeGeneratorLayersSequential` (pure computation, no owner-state mutation) with `ApplyComposedColorsRGB`, `RestoreFromBaseline`, and `RestoreFromCommitted` (the explicit owner-state mutation operations that do write `WorkingColors`/`CommittedColors`).

---

## 6. Layer Model

Two distinct layer models coexist (see [§15](#15-current-integration-status) for which is production-active):

**Current Contract — legacy fixed model.** `SVertexMaskForgePanel`'s `GeneratorState` holds exactly seven named `FVertexMaskForgeScalarMask` fields (Bounding Box, Ambient Occlusion, Curvature, Noise, Material Slot, Directional Normal, Thickness). Their composition order is a `GeneratorLayerOrder` permutation (`VertexMaskForgeLayerOrder`), not an arbitrary list. Each legacy layer, as seen by the bridge, is a `FVertexMaskForgeMaskLayerParams` (`Mask`, `BlendMode`, `Opacity`, `IndexOverride`) — **it has no per-layer channel-affect field**.

**Current Contract — Dynamic model.** `FVertexMaskForgeLayer` (owned by `FVertexMaskForgeDynamicLayerStack`) has: `LayerId` (stable identity), `Name`, `bEnabled`, `Fill` (`None`/`Black`/`White`), `BlendMode` (7 modes, shared enum with the legacy model), `Opacity`, `bAffectRed`/`bAffectGreen`/`bAffectBlue`/`bAffectAlpha` (per-layer channel participation — M19: `bAffectAlpha` added, default `false`, asymmetric with the other three's default `true`; see §2's M19 paragraph), and an optional `Mask` (`TOptional<FVertexMaskForgeGeneratorMaskInstance>`) carrying `MaskInstanceId`, `GeneratorType`, and `Params`.

A Dynamic layer resolves its per-index result without depending solely on `GeneratorType`: `VertexMaskForgeDynamicLayerEvaluator` looks up the layer's own `Mask->MaskInstanceId` in the supplied `FVertexMaskForgeInstanceResultStore`; a layer with no `Mask` never consults the store at all and contributes only its `Fill` value. Layer identity (`LayerId`) and mask-instance identity (`MaskInstanceId`) are independent: replacing a layer's generator type via `SetLayerMaskGeneratorType` mints a fresh `MaskInstanceId` while `LayerId` is untouched; re-assigning the *same* generator type is a no-op that preserves the existing `MaskInstanceId`.

---

## 7. Generator Result Model

**Current Contract.** `FVertexMaskForgeInstanceMaskResult` holds exactly `Values` (`TArray<float>`) and `bHasValue` (`TBitArray<>`) — no domain tag, no identity (the store's key already provides that), no generator parameters. `FVertexMaskForgeInstanceResultStore` maps `FGuid` (`MaskInstanceId`) → one such result, replacing in place on `StoreOrReplace`.

Four distinct absence/coverage cases exist and are handled differently in code, though all four currently resolve to **zero coverage** at the evaluator:

1. **`MaskInstanceId` absent from the store entirely.** `EvaluateColor`/`ComposeColors` treat this as zero coverage.
2. **Index out of `Values` range for a present instance.** Also zero coverage.
3. **Index within range but `bHasValue[index] == false`.** Also zero coverage.
4. **A structurally valid, `Ready` result whose actual value at that index is `0.0` with `bHasValue == true`** (e.g. Material Slot's real output for a corner/vertex not on the selected slot). This is a legitimate zero-*contribution* result, not an absent one — it is `Ready`, not `Missing`.

**Not frozen by the current code/tests:** a `Pending` (in-progress) generation status. No field, flag, or async contract exists anywhere in `FVertexMaskForgeInstanceMaskResult`/`FVertexMaskForgeInstanceResultStore`; every generation call observed in this codebase is synchronous.

**Current Contract — FailedAfterReady policy.** `VertexMaskForgeDynamicMaskGeneration::GenerateStoredResultForMaterialSlotInstance` only calls `StoreOrReplace` when the underlying generator returns `Ready`. A second, genuinely-failing call for the same `MaskInstanceId` never touches the store — the previous `Ready` result (and everything downstream that reads it) survives completely intact.

---

## 8. Composition

### Legacy/Current Composition Path

- **Entry point:** `VertexMaskForgeGeneratorLayerBridge::ComposeGeneratorLayersSequential`.
- **Input:** a Baseline `FVector4f`, a Committed `FVector4f`, an index, an array of `FVertexMaskForgeMaskLayerParams` (resolved from the seven fixed `GeneratorState` masks via `GeneratorLayerOrder`), and the panel's global `bFilterR`/`bFilterG`/`bFilterB`.
- **Store consumed:** none — each layer's `Mask` pointer is a direct `FVertexMaskForgeScalarMask*`, not a keyed store lookup.
- **Domain:** whichever domain the caller's Baseline/Committed/index already belong to (render-vertex or source-topology) — the function itself is domain-agnostic.
- **Output:** a single composed `FVector4f`.
- **Side effects:** none — pure function.
- **Panel integration:** wired into both real call sites of `ApplyPreviewToEntry` (`ComputeComposedColorsRGB` for render-vertex, `ComputeComposedColorsRGBSourceTopology` for source-topology).
- **Tests:** `GeneratorLayerBridgeTests.cpp` (`EmptyLayers`, `UnresolvedLayerSkipped`, `SingleLayerNumeric`, `StrictArrayOrder`, `ChannelFilter`, `IndexOverride`, `AlphaUntouched`).

### Dynamic Layer Evaluation

- **Entry point:** `VertexMaskForgeDynamicLayerEvaluator::EvaluateColor`.
- **Input:** a Base `FVector4f`, a `FVertexMaskForgeDynamicLayerStack`, a `FVertexMaskForgeInstanceResultStore`, an index.
- **Store consumed:** yes — each masked layer's contribution is looked up by `MaskInstanceId` in the supplied store.
- **Domain:** agnostic; determined entirely by what the caller's store/index represent.
- **Output:** a single composed `FVector4f`.
- **Side effects:** none — pure function; never mutates `Stack` or `ResultStore`.
- **Panel integration:** none.
- **Tests:** `DynamicLayerEvaluatorTests.cpp` (27 tests covering Fill, blend modes, per-layer channel filtering, alpha preservation, ordering, and all four absence/coverage cases from [§7](#7-generator-result-model)).

### Dynamic Batch Composition

- **Entry point:** `VertexMaskForgeDynamicLayerBatchCompositor::ComposeColors`.
- **Input:** `TConstArrayView<FColor> BaseColors`, a `FVertexMaskForgeDynamicLayerStack`, a `FVertexMaskForgeInstanceResultStore`, an `OutColors` out-parameter.
- **Store consumed:** the same `FVertexMaskForgeInstanceResultStore` as above, applied once per element of `BaseColors` via `EvaluateColor`.
- **Domain:** agnostic; determined entirely by what `BaseColors`/`ResultStore` represent.
- **Output:** `OutColors`, wholesale-substituted with exactly `BaseColors.Num()` elements. Safe to call with `BaseColors` and `OutColors` aliasing the same array.
- **Side effects:** none — pure; never mutates `Stack` or `ResultStore`; **never writes `WorkingColors` or `BaselineColors`**.
- **Panel integration:** none.
- **Tests:** `DynamicLayerBatchCompositorTests.cpp` (4 tests) plus `VertexMaskForge.DynamicCompositionSourceTopologyIntegration.RealSourceTopologyBaselineAndRealStoredMaterialSlotResultProduceExpectedColorsAcrossCoveredAndUncoveredCorners` (M16-K.5J).

**M16-K.5J proved, end to end, with real production objects (no hand-built store, no fixture-only shortcuts):**

```
WorkingStateOwner source-topology baseline
  → DynamicLayerStack identity
  → real Material Slot generation
  → WorkingMesh.InstanceResults
  → MoveTemp/InstallWorkingMesh preservation
  → batch ComposeColors
  → byte-exact local output
```

`ComposeColors` does not alter `BaselineColors` or `WorkingColors` — proven both structurally (no such call exists in its implementation) and by explicit before/after array comparison in the M16-K.5J test.

---

## 9. Channel Semantics

**Current Contract — three independent mechanisms, not interchangeable:**

- **Per-layer `bAffectRed`/`bAffectGreen`/`bAffectBlue`/`bAffectAlpha`** (`FVertexMaskForgeLayer`, Dynamic model only; `bAffectAlpha` added M19 — see §2's M19 paragraph). Applied inside `EvaluateColorInternal` (legacy-equivalent, dead-for-Layers evaluator) and, for the live Layers path, inside `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology`'s Pass 2 fold — per layer, per channel including Alpha: when false for a given channel, that one layer simply does not fold its value into that channel, leaving whatever the prior layers (or the corner's own Base value) already produced untouched. It never resets a channel to Base or Committed by force, and it has no legacy equivalent (`FVertexMaskForgeMaskLayerParams` carries no such field, for any of the four channels).
- **Global Channel Filter (`bChannelFilterR`/`bChannelFilterG`/`bChannelFilterB`)** — panel-level fields on `SVertexMaskForgePanel`, legacy-only (no UI reachable since M18 — see §2's M18 paragraph), passed into the now-unreachable Legacy composition call sites. Applied once, after the whole legacy layer stack has folded: a disabled channel's final value is replaced by the corresponding channel of `CommittedColors`; an enabled channel keeps the fold's own computed value. This mechanism has no Dynamic-model equivalent today, for R/G/B or Alpha.
- **Alpha, per composition path (corrected M19 — see §2's M19 paragraph for the full contract):** the Legacy composition path (`VertexMaskForgeGeneratorLayerBridge::ComposeGeneratorLayersSequential`, `VertexMaskForgeDynamicLayerEvaluator::EvaluateColor`) and `ApplyComposedColorsRGB` (which additionally forces Alpha from the live `BaselineColors` unconditionally, regardless of what a caller's `FinalRGB` happened to carry) are **unchanged** — Alpha remains untouched by the layer math on those paths, and neither is reachable in production for the Layers workflow (see §2's M18 paragraph on the one retained, provably-dead Legacy block; `ApplyComposedColorsRGB` is never called by the Layers preview or Accept paths, which write `WorkingColors`/Accept targets directly from the orchestrator's own output). The **live** Layers composition path (`VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology`) instead composes Alpha exactly like R/G/B, gated per layer by `bAffectAlpha` (default `false`) — a layer that does not affect Alpha leaves it exactly as it already was, which is why every pre-M19 layer and every fresh layer continues to leave Alpha byte-for-byte at its Base value.
- **Preview Mode / display-only derivation** — `EVertexMaskForgePreviewMode` (`OriginalMaterial`, `RGBVertexColor`, `RedChannel`, `GreenChannel`, `BlueChannel`, `AlphaChannel`) is consumed exclusively by `VertexMaskForgeDisplayColorDerivation::DeriveDisplayColors`, strictly downstream of `WorkingColors`. It never participates in composition and never mutates `WorkingColors` — unchanged by M19. The Layers UI's Alpha-only white row tint (§2's M19 paragraph) is a fourth, still-independent mechanism: pure layer-row presentation, never read by or written into Preview Mode, `DeriveDisplayColors`, or any mesh/viewport state.

---

## 10. Generators

**Current Contract**, all seven fixed generators exist and support both domains at the generator-function level:

| Generator | `MaskGeneratorType` | Domains supported | Result | Dynamic Stack integration | Known limitations |
|---|---|---|---|---|---|
| Bounding Box | `BoundingBox` | Render-vertex + Source-topology | Pure position/bounds scalar | Not yet represented | Compatibility gap only — legacy generator is fully functional |
| Ambient Occlusion | `AmbientOcclusion` | Render-vertex + Source-topology | World-space hemisphere-raycast scalar | **Implemented** (M16-K.6D-8G-C/8G-D/8G-F) | Legacy's own final mask value is still stored per-entry (asset) even though its raw raycast cache is per-component — an unresolved ownership tension specific to the **Legacy** path only (see [§16](#16-known-limitations-and-risks)); Dynamic AO uses a separate, fully-resolved per-component/per-`LayerId` cache (Model D — see [§13](#13-cache-and-invalidation-contracts), [Decision Log ADR-015](VertexMaskForgeDecisionLog.md#adr-015--persistent-per-componentper-layerid-cache-ownership-model-d-for-dynamic-ambient-occlusion)) |
| Curvature | `Curvature` | Render-vertex + Source-topology | Topology-derived scalar, no raycast | **Implemented** (M16-K.6D-8E-B/8E-C) | Raw Convex/Concave cache shared only within one orchestrator invocation, never across calls — see [§13](#13-cache-and-invalidation-contracts) |
| Noise | `Noise` | Render-vertex + Source-topology | Position-derived procedural scalar | **Implemented** (M16-K.6D-8F-B/8F-C) | Raw noise cache is independent per enabled Noise layer, given fresh per orchestrator call, never shared across layers or calls — see [§13](#13-cache-and-invalidation-contracts) |
| Directional Normal | `DirectionalNormal` | Render-vertex + Source-topology | Normal-vs-axis-direction scalar | **Implemented, Local-space only** (M16-K.6D-8D-B/8D-C) | World Space is authoritatively rejected, never silently reinterpreted as Local |
| Thickness | `Thickness` | Render-vertex + Source-topology | Asset-Local-Space raycast scalar (with internal M9 conservative fallback + confidence gate, committed `75cf7bf6`) | **Implemented, Source-topology only** (M17-TH-DL-B) | Deliberately never uses `ComponentTransform` (differently-scaled instances would otherwise produce incompatible persisted results); Dynamic Render-vertex generation path does not exist for this or any generator except Material Slot; Dynamic exposes only Min Thickness/Max Thickness/Search Distance/Invert, never Bias/Blur/M9 internals |
| Material Slot | `MaterialSlot` | Render-vertex + Source-topology | Per-vertex/per-corner slot-membership scalar | **Implemented and test-proven** (`GenerateStoredResultForMaterialSlotInstance`) | Render-vertex Dynamic generation path is untested — every existing test/fixture for this function uses the source-topology overload only |

A generator not yet represented by the Dynamic Stack is **still a fully functional part of the tool** through the legacy path — this is a Dynamic-model compatibility gap, not an absence of the capability.

---

## 11. Preview and Display

**Current Contract.**

- **Auto Update Preview** — the panel debounces recomposition via a timer handle (`AutoUpdateDebounceTimerHandle`), cleared explicitly on Cancel.
- **Preview Mode** — see [§9](#9-channel-semantics); a pure display-time reduction of `WorkingColors`, never affecting composition or persistence.
- **Channel Filter's role at composition, not just display** — the global Channel Filter affects the *composed* `WorkingColors` itself (it feeds `ComputeComposedColorsRGB[SourceTopology]`, upstream of `ApplyComposedColorsRGB`), not merely what is displayed. It is not a display-only mechanism.
- **`DisplayColorDerivation`** — stateless, operates only on an already-composed `WorkingColors` buffer; never reads generator/cache/UObject state.
- **Visual restoration vs. state restoration** — these are different operations: `RestoreComponentOriginal` (invoked from Cancel's `DestroyAllPreviews`) restores a real component's visibility/material state, with no color-array involvement at all (no vertex-color write ever happened that would need reverting). `RestoreFromBaseline`/`RestoreFromCommitted` (on `FVertexMaskForgeWorkingStateOwner`) instead restore the **color arrays** (`WorkingColors`), independent of any component visibility concern.
- **Generation-failure behavior** — confirmed only for Material Slot's Dynamic generation path: a failed regeneration leaves the previously stored `Ready` result, and therefore the previously-composed preview, untouched (see [§7](#7-generator-result-model)). Broader failure-preservation behavior for the legacy generators is **not frozen by the current code/tests** as a general cross-generator contract in this document's evidence base.

---

## 12. Persistence Boundaries

| Operation | Reads | Writes | Persistent target | Undo/transaction behavior | Protected by |
|---|---|---|---|---|---|
| Accept | `WorkingColors` / `SourceTopologyWorkingColors` | `UStaticMesh` source `FMeshDescription` vertex-instance colors | The Static Mesh asset itself | One `FScopedTransaction` wraps both the render-vertex and source-topology writers; `Build()`/`Modify()` sequencing keeps rebuild outside the transaction, guarded via a `GUndo` suppression | Not frozen by tests — no dedicated Accept test found in the current suite |
| Cancel | Panel's own session bookkeeping only | Nothing (no color write occurs) | None | No transaction opened; the entire session object graph (`SelectedMeshes`) is discarded, not reverted from a snapshot | Not frozen by tests |
| Instance Override | — | — | — | Confirmed fully removed from production code (only an unrelated, legitimately-named `InstanceOverrideColors` baseline-capture parameter remains) | N/A — historical feature, removed |
| Save to Source Mesh | — | — | — | No distinct feature by this name exists in the current codebase; the closest real operation is Accept itself | N/A — not a real distinct feature today |

---

## 13. Cache and Invalidation Contracts

**Current Contract.** There is no single global "generation counter" governing every cache. Each generator's raw cache instead stores the specific identity/content fields its expensive build depended on, and recomputes when the *current* inputs no longer match:

- **Ambient Occlusion** (`FVertexMaskForgeAOCache` / `FVertexMaskForgeSourceTopologyAOCache`): geometric rebuild triggers on mesh identity, derived-data key, vertex/index counts, and transform mismatch (render-vertex domain); on mesh pointer identity plus a full geometry fingerprint hash (source-topology domain). A second, independent layer of the cache (raw sample values) rebuilds only when sampling parameters change or the geometric layer itself rebuilt.
- **Thickness** (`FVertexMaskForgeThicknessCache` / `FVertexMaskForgeSourceTopologyThicknessCache`): the same identity-based fast-reject as AO, **plus** a full value-by-value geometry snapshot comparison performed immediately before Accept's first mesh modification — the fingerprint alone is explicitly documented as insufficient proof of freshness for an irreversible write.
- **Curvature** (`CurvatureRenderVertexToDynamicMeshVertex`): a static correspondence table rebuilt whenever the working mesh itself is rebuilt (e.g. on Refresh Selection); not independently versioned. This is the **Legacy** per-entry cache (`FVertexMaskForgeGeneratorState`, owned by each `FVertexMaskForgeSelectedMesh` entry) — see the Dynamic Curvature bullet immediately below for the separate, transient cache the Dynamic orchestrator uses instead.
- **Dynamic Curvature** (M16-K.6D-8E-B): `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` owns **one function-local `FVertexMaskForgeGeneratorState`, declared fresh on every call**, entirely separate from Legacy's own per-entry state above — it is never read from or written into Legacy's cache, and Legacy's cache is never read from or written into it. Every enabled Curvature layer evaluated *within that single orchestrator invocation* shares this one local state, so raw Convex/Concave computation happens once per call no matter how many Curvature layers are enabled — but the state does not persist **across** calls: the next orchestrator invocation (e.g. the next Auto Update Preview recomposition, triggered by editing *any* layer's property, not necessarily a Curvature layer's own) starts with a fresh, empty local state and recomputes the raw arrays from scratch, even if geometry has not changed. Artistic-only edits (Type/Multiplier/Blur/Levels/Invert) do **not** avoid this cross-call raw recomputation — only same-call sharing among multiple Curvature layers is provided. Persistent cross-call Dynamic Curvature caching is intentionally unimplemented (see [Decision Log ADR-013](VertexMaskForgeDecisionLog.md#adr-013--transient-within-call-generator-state-for-dynamic-curvature) and its own Future Decision Candidates entry).
- **Dynamic Noise** (M16-K.6D-8F-B): unlike Dynamic Curvature's one *shared* local state, the orchestrator gives **each enabled Noise layer its own independent, function-local `FVertexMaskForgeGeneratorState`, declared fresh inside the per-layer loop body** — deliberately not shared even within one orchestrator call. Noise's own raw-cache key (`FVertexMaskForgeNoiseGenerativeParams::operator==`) includes the full generative parameter set (Type/Scale/Offset/Seed/Octaves/Roughness/Lacunarity/Turbulence Strength/Blur), not just mesh geometry like Curvature's own key — two Noise layers commonly differ in these fields, so sharing one state across them would merely thrash (invalidate and recompute) rather than help. As with Dynamic Curvature, no state persists **across** orchestrator calls for any Noise layer — every invocation recomputes every enabled Noise layer's raw pattern from scratch. This state is never static, panel-owned, or keyed by `LayerId`; it never reads from or writes into Legacy's own separate Noise cache (`NoiseRawCache`, unaffected by this checkpoint). See [Decision Log ADR-014](VertexMaskForgeDecisionLog.md#adr-014--layer-owned-noise-as-the-fifth-dynamic-generator-and-ephemeral-per-layer-lock-axes-metadata).
- **Dynamic Ambient Occlusion** (M16-K.6D-8G-C/8G-D — Model D, the third and only *persistent* Dynamic generator-state pattern): unlike Curvature's shared-within-call and Noise's independent-per-layer-within-call states (both function-local, discarded when the orchestrator call returns), each eligible `FVertexMaskForgePreviewComponentState` owns a **persistent** `TMap<FGuid, FVertexMaskForgeSourceTopologyAOCache> DynamicSourceTopologyAOCachesByLayerId`, additive alongside Legacy's own single-slot `SourceTopologyAOCache` (never replacing or reinterpreting it). Each stable `LayerId` owns one complete, independent `FVertexMaskForgeSourceTopologyAOCache`, including its own retained acceleration tree — never shared across AO layers or across components. That cache struct's own existing two-layer contract governs invalidation, reused verbatim (never duplicated in the Dynamic orchestrator or UI): the acceleration tree rebuilds on mesh identity, geometry fingerprint, or component-transform mismatch; the raw sample values rebuild only when Samples/Max Distance/Bias change or the tree itself just rebuilt; Levels and Invert are applied fresh from valid raw values on every call, never cached, never invalidating anything. Because the map persists across orchestrator calls (unlike Curvature's/Noise's function-local state), composition-only edits (Fill/Blend/Opacity/Channel Filter/reorder/an unrelated layer's own edit) cost zero raycasts and zero tree rebuilds — matching Legacy AO's own established cost contract, which Curvature's/Noise's within-call-only patterns do not attempt to match. Tree sharing **across** AO layers on the same geometry is a deliberately deferred optimization (each layer currently owns a fully independent tree, even when several AO layers share identical geometry) — see [Decision Log ADR-015](VertexMaskForgeDecisionLog.md#adr-015--persistent-per-componentper-layerid-cache-ownership-model-d-for-dynamic-ambient-occlusion) for the models considered and the reasoning.
- **Dynamic Thickness** (M17-TH-DL-B — Model D, the exact sibling pattern of Dynamic Ambient Occlusion above): each eligible `FVertexMaskForgePreviewComponentState` owns a **persistent** `TMap<FGuid, FVertexMaskForgeSourceTopologyThicknessCache> DynamicSourceTopologyThicknessCachesByLayerId`, additive alongside every other field. Each stable `LayerId` owns one complete, independent `FVertexMaskForgeSourceTopologyThicknessCache`, including its own retained acceleration tree. That cache struct's own existing freshness contract governs invalidation, reused verbatim: the acceleration tree rebuilds on mesh identity/geometry-fingerprint mismatch; raw distances rebuild only when Search Distance or Bias change (or the tree itself just rebuilt); Min Thickness/Max Thickness/Invert are remapped fresh from valid raw distances on every call, never touching the raycast cache. Composition-only edits (Fill/Blend/Opacity/Channel Filter/reorder/an unrelated layer's own edit) cost zero raycasts and zero tree rebuilds, matching Dynamic AO's own established cost contract. Tree sharing across Thickness layers on the same geometry is likewise a deliberately deferred optimization, not implemented.

Generator parameters that only affect the final scalar mapping (e.g. blur, levels, invert) recompose without invalidating the raw geometric cache; parameters that change what geometry/sampling produced the cache (transform, LOD, mesh identity) force a rebuild.

---

## 14. Test-Protected Boundaries

| Boundary | Representative test(s) | What is frozen |
|---|---|---|
| Baseline/Committed/Working independence | `VertexMaskForge.WorkingStateOwner.Restore` | `RestoreFromBaseline`/`RestoreFromCommitted` never cross-touch the other array |
| Alpha forced from Baseline on apply | `VertexMaskForge.WorkingStateOwner.ComposedColorsControlled` | `ApplyComposedColorsRGB` ignores the caller's own Alpha |
| Fill Black/White consolidation | `VertexMaskForge.WorkingStateOwner.FillBlackWhite` | R/G/B forced, Alpha preserved, Committed synced |
| Multi-component independence | `VertexMaskForge.WorkingStateOwner.TwoComponentsSharedMesh` | One shared mesh owner, fully independent per-component color buffers |
| Global Channel Filter → Committed fallback | `VertexMaskForge.GeneratorLayerBridge.ChannelFilter` | Disabled global channel = `CommittedColor`; enabled = computed fold |
| Legacy `IndexOverride` resolution | `VertexMaskForge.GeneratorLayerBridge.IndexOverride` | Explicit override takes precedence over the shared index |
| Dynamic per-layer channel isolation | `VertexMaskForge.DynamicLayerEvaluator.ChannelFilterSingleChannel`, `...ChannelFilterAllDisabled` | Per-layer `bAffect*` never resets a channel to Base/Committed |
| Missing `MaskInstanceId` → zero coverage | `VertexMaskForge.DynamicLayerEvaluator.MissingMaskInstanceIdInStoreProducesZeroCoverage` | Absence Case A |
| Present instance, no value at index → zero coverage | `VertexMaskForge.DynamicLayerEvaluator.PresentInstanceWithoutValueAtVertexIndexProducesZeroCoverage` | Absence Case C |
| Out-of-range index → zero coverage | `VertexMaskForge.DynamicLayerBatchCompositor.MultipleIndicesWithDistinctCoveragesAndMissingIndexProduceExpectedColors` | Absence Case B |
| Valid coverage-0 result | `VertexMaskForge.DynamicMaskGeneration.GeneratedMaterialSlotResultDrivesEvaluatedColorAtBothCorners` | Absence Case D — Ready, not Missing |
| FailedAfterReady preservation | `VertexMaskForge.DynamicMaskGeneration.FailedRegenerationPreservesPreviousReadyResult` | A failed regeneration never overwrites a prior Ready result |
| Batch aliasing safety | `VertexMaskForge.DynamicLayerBatchCompositor.AliasedInputAndOutputProduceCorrectResult` | Same array safe as both source and destination |
| Empty stack passthrough | `VertexMaskForge.DynamicLayerBatchCompositor.EmptyStackPreservesAllBaseColorsByteExact` | An empty stack composes to a byte-exact passthrough |
| Real generation-to-composition integration | `VertexMaskForge.DynamicCompositionSourceTopologyIntegration.RealSourceTopologyBaselineAndRealStoredMaterialSlotResultProduceExpectedColorsAcrossCoveredAndUncoveredCorners` | The full real chain in [§8](#8-composition), including move-preservation and non-mutation of Baseline/Working colors |

---

## 15. Current Integration Status

### Integrated in the Production Panel

- Legacy fixed `GeneratorState` (all 7 generators) and `GeneratorLayerOrder`.
- `VertexMaskForgeGeneratorLayerBridge::ComposeGeneratorLayersSequential`.
- Global Channel Filter (`bChannelFilterR/G/B`) and `EVertexMaskForgePreviewMode` / `DisplayColorDerivation`.
- `FVertexMaskForgeWorkingStateOwner` and `FVertexMaskForgeWorkingMeshOwner` (both are the real state behind the panel's `ApplyPreviewToEntry`/Accept call sites).
- Accept and Cancel flows (Accept additionally gated Legacy-only as of M16-K.6D-5 — see below).
- **M16-K.6D-5:** `FVertexMaskForgeDynamicLayerStack` → `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` → the M16-K.6D-2 visual-only seam, selected live by `PreviewSource` inside `ApplyPreviewToEntry`'s Source-Topology branch. Presentation-only: never `WorkingColors`, never `ApplyComposedColorsRGB`, never Accept-eligible. See §2 for the full contract.
- **M16-K.6D-8C-C:** Dynamic Layers' Generator Type selector offers `Material Slot` and `Bounding Box` (Local-space only); each layer owns its own parameters and its own `Generator Parameters` expander. See §2 for the full contract.
- **M16-K.6D-8C-B:** the obsolete standalone `Layers` presentation panel (fixed Legacy-generator list, Up/Down buttons — distinct from `Dynamic Layers`) was removed; `GeneratorLayerOrder` and Legacy composition were unaffected. See §2.
- **M16-K.6D-8D-C:** Dynamic Layers' Generator Type selector additionally offers `Directional Normal` (Local-space only); each layer owns its own Direction/Angle/Falloff/Blur/Invert parameters in a third sibling `Generator Parameters` block, with `Space` model-owned and hidden. See §2 for the full contract.
- **M16-K.6D-8E-C:** Dynamic Layers' Generator Type selector additionally offers `Curvature`; each layer owns its own Type/Multiplier/Blur/Levels Min/Levels Max/Invert parameters in a fourth sibling `Generator Parameters` block. See §2 for the full contract, including the within-call-only raw-cache sharing behind it.
- **M16-K.6D-8F-C/8F-C.1:** Dynamic Layers' Generator Type selector additionally offers `Noise` (displayed "Noise/Grunge"); each layer owns all 17 authoritative Noise parameters in a fifth sibling `Generator Parameters` block, plus an ephemeral, panel-owned, per-layer `Lock Axes` Scale-editing convenience unrelated to the authoritative payload. See §2 for the full contract.
- **M16-K.6D-8G-F:** Dynamic Layers' Generator Type selector additionally offers `Ambient Occlusion`, inserted immediately after `Bounding Box` (matching Legacy's own generator-library ordering; the other five generators' relative order is unchanged); each layer owns its own Samples/Max Distance/Bias/Levels Min/Levels Max/Invert parameters in a sixth sibling `Generator Parameters` block, backed by the persistent per-component/per-`LayerId` cache established in M16-K.6D-8G-C/8G-D. See §2 for the full contract.
- **M17-TH-DL-B:** Dynamic Layers' Generator Type selector additionally offers `Thickness` (Source Topology only), placed last — the seventh and final entry (`None` plus all seven generators; no generator type remains unavailable to Dynamic Layers). Each layer owns its own Min Thickness/Max Thickness/Search Distance/Invert parameters in a seventh sibling `Generator Parameters` block; Bias/Blur and every M9 fallback/confidence-gate implementation detail (cone angle, ray count, support tolerance, minimum valid values, median support count, confidence threshold, support lead) remain internal, never UI or serialized layer parameters. Evaluation dispatches directly to the existing, unmodified production entry point `VertexMaskForgeThicknessGenerator::GenerateThicknessMaskFromDynamicMesh` (the same backend, including its own internal M9 fallback, the Legacy panel already uses) — no duplicated raycast/fallback/confidence-gate/normalization logic. Cache ownership is the exact Model D sibling of Dynamic Ambient Occlusion's own: `FVertexMaskForgePreviewComponentState::DynamicSourceTopologyThicknessCachesByLayerId` (`TMap<FGuid, FVertexMaskForgeSourceTopologyThicknessCache>`, one complete, independent cache per stable Dynamic `LayerId`, never shared/aliased across layers — proven directly by test for two Thickness layers with different Search Distance). Blend Mode/Opacity/Fill Value/channel filtering/reorder remain entirely Pass 2/generic-composition responsibilities, exactly as for every other generator — Thickness's own `bInvert` is applied once, inside the backend call itself, never a second time in composition (proven by an exact per-channel White-complement identity test). Render Vertex Dynamic support for Thickness is explicitly out of scope for this checkpoint (Render Vertex Dynamic support does not exist for ANY generator except Material Slot today — a pre-existing, generator-agnostic gap, not Thickness-specific). See §2 for the full contract.
- **M19:** Alpha is a real, per-layer composable fourth Channel Filter bit (`bAffectAlpha`, default `false`) throughout the live Layers composition/`WorkingColors`/Accept/persistence path, and an artist-facing `A` checkbox (normal click, Alt+click isolation, white Alpha-only row tint at the existing shared RGB tint opacity) is wired into `BuildDynamicLayerRow`. User manual validation: PASS (11/11). See §2 for the full contract.
- **M20-E/E.1:** `VertexMaskForgeComponentOverrideBridge::BuildTransferTargets`/`TransferToMeshPaintTexture`, invoked by `SVertexMaskForgePanel::OnSendToMeshPaintTextureClicked`, transfers already-Accepted Static Mesh vertex colors through Unreal's native "From Vertex" Mesh Paint Texture import, with session-independent enablement (`MeshPaintTransferCandidateMeshes`). Read-only relative to the Static Mesh asset; never a second Accept path. See §2 for the full contract.

### Implemented and Test-Proven but Not Yet Connected

- As of M16-K.6B/M16-K.6C-2, extended M16-K.6D-8C-C/8D-C/8E-C/8F-C/8G-F/M17-TH-DL-B, `FVertexMaskForgeDynamicLayerStack`'s UI supports generator-type assignment (`None`/`Material Slot`/`Bounding Box`/`Ambient Occlusion`/`Directional Normal`/`Curvature`/`Noise`/`Thickness`) and, under the ADR-010 single-asset gate, `FVertexMaskForgeMaterialSlotParams` editing (`SelectedSlotIndex`/`bInvert`) — both now feed the M16-K.6D-5 Dynamic preview live (via `OnDynamicLayerStackMutated()`) whenever `PreviewSource == Dynamic`, in addition to remaining directly editable at any time. As of M16-K.6D-6, the Generator Type combo's "None" option is represented as a valid pointer to an unset `TOptional<EVertexMaskForgeGeneratorType>` (never a null `TSharedPtr`, which UE 5.8's own `SListView` unconditionally excludes from rendering) — see §2's M16-K.6D-6 paragraph for the full root cause. As of M16-K.6D-8C-C, each layer's own `FVertexMaskForgeBoundingBoxParams` (Local-space only) is likewise directly editable per layer, inside that layer's own `Generator Parameters` expander — see §2's M16-K.6D-8C-C paragraph. As of M16-K.6D-8D-C, each layer's own `FVertexMaskForgeDirectionalNormalParams` (Local-space only; `Space` model-owned and hidden) is likewise directly editable per layer, inside a third sibling `Generator Parameters` block — see §2's M16-K.6D-8D-C paragraph. As of M16-K.6D-8E-C, each layer's own `FVertexMaskForgeCurvatureParams` is likewise directly editable per layer, inside a fourth sibling `Generator Parameters` block — see §2's M16-K.6D-8E-C paragraph. As of M16-K.6D-8F-C, each layer's own `FVertexMaskForgeNoiseParams` (all 17 fields) is likewise directly editable per layer, inside a fifth sibling `Generator Parameters` block, plus the layer's own ephemeral (non-authoritative) Lock Axes UI state — see §2's M16-K.6D-8F-C/8F-C.1 paragraphs. As of M16-K.6D-8G-F, each layer's own `FVertexMaskForgeAmbientOcclusionParams` is likewise directly editable per layer, inside a sixth sibling `Generator Parameters` block, backed by the persistent `DynamicSourceTopologyAOCachesByLayerId` cache — see §2's M16-K.6D-8G-C/8G-D/8G-F paragraphs. As of M17-TH-DL-B, each layer's own `FVertexMaskForgeThicknessParams` (Min Thickness/Max Thickness/Search Distance/Invert only — Bias/Blur/M9 internals never exposed) is likewise directly editable per layer, inside a seventh sibling `Generator Parameters` block, backed by the persistent `DynamicSourceTopologyThicknessCachesByLayerId` cache — see §2's M17-TH-DL-B paragraph.
- `VertexMaskForgeDynamicLayerEvaluator` / `VertexMaskForgeDynamicLayerBatchCompositor` — still have no production caller; the M16-K.6D-5 Dynamic branch calls `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` directly instead (see §2), which itself never uses these two or any `FVertexMaskForgeInstanceResultStore`.
- `VertexMaskForgeDynamicMaskGeneration::GenerateStoredResultForMaterialSlotInstance` (Material Slot only, source-topology domain proven) — still has no production caller; the M16-K.6D-5 Dynamic branch calls `VertexMaskForgeMaterialSlotGenerator::GenerateMaterialSlotMaskFromDynamicMesh` directly (via the orchestrator), never this stored wrapper.
- `FVertexMaskForgeInstanceResultStore` as a general mechanism — still only exercised by the Material Slot Dynamic *generation* path above (`GenerateStoredResultForMaterialSlotInstance`'s own tests); never touched by the M16-K.6D-5 preview path.
- The `FVertexMaskForgeRecipe`/`FVertexMaskForgeMaskInstance` type family — a separate, self-contained evaluation library (`VertexMaskForgeRecipeEvaluation`, `VertexMaskForgeRecipeInstanceGeneration`) with zero UI and zero production caller.

**Historical (superseded by M18):** the paragraph below described the dual Legacy/Dynamic era, when both paths remained fully independent and user-selectable via `PreviewSource`. As of M18, Layers (formerly "Dynamic Layers") IS the sole workflow — the Legacy UI and the `PreviewSource` selector were removed; see §2's own M18 paragraph for the current, authoritative contract. Retained here as history, not current state: "Do not presume the Dynamic Stack has replaced the Legacy path — both remain fully independent, user-selectable via `PreviewSource`. As of M16-K.6D-7B, Accept is reachable through **either** source (structurally isolated per-source target construction — see §2's M16-K.6D-7A/7B paragraph); Dynamic Accept's manual Editor validation is still pending. `EVertexMaskForgePreviewSource`/`PreviewSource` (M16-K.6D-1, ADR-011) now has its first real reader/writer (M16-K.6D-5), independently manually validated in the Editor (M16-K.6D-6) — see §2 for the exact contract — but Global Channel Filter/Fill are still Legacy-only and not yet gated off in the UI while `PreviewSource == Dynamic` (still planned, per ADR-011)."

### Planned

- A panel-level migration strategy from the legacy path to the Dynamic path (not designed).
- **Moot as of M18:** the item that used to appear here (Global Channel Filter and Fill Black/White becoming unavailable in the UI while `PreviewSource == Dynamic`) no longer applies — M18 removed the global Channel Filter and Fill Black/White entirely, and removed the Preview Source selector itself; see §2's own M18 paragraph.
- **Implementation pending:** Render Vertex domain support for Dynamic preview. World Space and Unified Bounds for Dynamic Bounding Box (see the M16-K.6D-8B paragraph in §2 for the exact rejection contract). World Space for Dynamic Directional Normal (see the M16-K.6D-8D-B paragraph in §2 for the exact rejection contract). Persistent cross-call raw-cache reuse for Dynamic Curvature (see the M16-K.6D-8E-B paragraph in §2 and [§13](#13-cache-and-invalidation-contracts) for the exact within-call-only sharing that IS implemented) and for Dynamic Noise (see the M16-K.6D-8F-B paragraph in §2 and [§13](#13-cache-and-invalidation-contracts) for its own independent-per-layer, within-call-only sharing). Tree sharing across multiple Dynamic Ambient Occlusion layers on the same geometry (see the M16-K.6D-8G-C paragraph in §2 and [§13](#13-cache-and-invalidation-contracts) — each AO layer currently owns a fully independent tree, a deliberately deferred optimization). Render Vertex domain support for Dynamic Thickness (M17-TH-DL-B delivered Source Topology only — Render Vertex Dynamic support does not exist for ANY generator except Material Slot today, a pre-existing, generator-agnostic gap). As of M17-TH-DL-B, all seven `EVertexMaskForgeGeneratorType` values are dispatched by the Source-Topology orchestrator — there is no longer a structurally-unsupported generator type to use as a negative-test sentinel (the orchestrator's own former "genuinely unsupported" test was repurposed; see its own updated comment in `VertexMaskForgeDynamicSourceTopologyCompositionTests.cpp`). Dynamic Accept exists as of M16-K.6D-7B (see §2) and its manual Editor validation has passed. No reusable Dynamic layer-stack serialization, presets, Duplicate Layer, or per-mutation Undo/Redo exist yet (none of these exist for any Dynamic generator, not specific to Thickness). No Paint Layers exist yet.

---

## 16. Known Limitations and Risks

- **Resolved by M18.** The Legacy/Dynamic coexistence risk noted through M16-K.6D-5 (Global Channel Filter and Fill Black/White remaining visible while `PreviewSource == Dynamic`) no longer applies — Legacy's UI, the Preview Source selector, the global Channel Filter, and Fill Black/White were all removed; Layers is the sole workflow. See the M18 paragraph above for the full contract and the one deliberately retained piece of dead Legacy code.
- **Domain-mixing risk.** `MaskInstanceId` carries no domain tag; generating a source-topology and a render-vertex result under the same id would silently overwrite one with the other (see [§3](#3-domain-model)).
- **Cardinality is caller-supplied, not independently verified.** `InstallWorkingMesh`'s `ExpectedCardinality` parameter is not cross-checked against the installed mesh's actual geometry.
- **Legacy AO's per-entry vs. per-component ownership is unresolved.** The final Legacy AO mask value is stored per-entry (asset-level) even though its raw raycast cache is per-component (transform-dependent) — a genuine tension between the legacy convention and the newer per-component-store design intent, specific to the **Legacy** path only. Dynamic AO (M16-K.6D-8G-C) does not share this tension — its cache is fully per-component and per-`LayerId` by design (Model D, see [§13](#13-cache-and-invalidation-contracts), [Decision Log ADR-015](VertexMaskForgeDecisionLog.md#adr-015--persistent-per-componentper-layerid-cache-ownership-model-d-for-dynamic-ambient-occlusion)).
- **Dynamic preview is Source-Topology only, across all seven generators (Material Slot/Bounding-Box(Local-space)/Ambient-Occlusion/Directional-Normal(Local-space)/Curvature/Noise/Thickness).** No Render Vertex Dynamic seam exists for any generator except Material Slot; `DynamicLayerEvaluator`/`DynamicLayerBatchCompositor`/`FVertexMaskForgeInstanceResultStore` still have no production caller (the M16-K.6D-5 preview path bypasses all three — see §2). Dynamic Bounding Box does not support World Space or Unified Bounds (M16-K.6D-8B/8C-C). Dynamic Directional Normal does not support World Space (M16-K.6D-8D-B/8D-C). Dynamic Thickness (M17-TH-DL-B; Levels Min/Levels Max added M17-TH-DL-E) exposes only Min Thickness/Max Thickness/Search Distance/Levels Min/Levels Max/Invert — Bias, Blur, and every M9 fallback/confidence-gate parameter remain internal to the backend, never reachable from Dynamic Layers.
- **Dynamic Ambient Occlusion does not share its acceleration tree across multiple AO layers on the same geometry (M16-K.6D-8G-C).** Each AO layer's cache entry owns a fully independent tree, even when several AO layers are enabled on the same component with identical mesh/transform — a deliberately deferred optimization (Model D over the alternative "shared tree, independent raw results" design), not a correctness issue. See [§13](#13-cache-and-invalidation-contracts), [Decision Log ADR-015](VertexMaskForgeDecisionLog.md#adr-015--persistent-per-componentper-layerid-cache-ownership-model-d-for-dynamic-ambient-occlusion).
- **Dynamic Curvature's raw Convex/Concave cache is transient and per-orchestrator-call only (M16-K.6D-8E-B).** It is shared among enabled Curvature layers *within one call*, but every new call (including one triggered by editing an unrelated layer's property while Curvature layers exist) starts fresh and recomputes the raw geometric pass — an accepted performance cost, not a correctness issue. Persistent cross-call caching remains future work (see [§13](#13-cache-and-invalidation-contracts), ADR-013).
- **Dynamic Noise's raw-noise cache is transient and per-layer-per-orchestrator-call only (M16-K.6D-8F-B).** Unlike Dynamic Curvature, it is not even shared *within* one call among multiple Noise layers — each enabled Noise layer recomputes its own raw pattern every call, since two Noise layers' cache keys commonly differ (Type/Scale/Offset/Seed/etc. all participate). An accepted performance cost, not a correctness issue. Persistent cross-call caching remains future work (see [§13](#13-cache-and-invalidation-contracts), ADR-014).
- **Dynamic Noise's per-layer `Lock Axes` UI convenience is ephemeral panel state, not part of the Dynamic Layer model (M16-K.6D-8F-C.1).** `SVertexMaskForgePanel::DynamicNoiseScaleAxesLockedByLayerId` is keyed by `LayerId` but lives only for the current panel session — it is not serialized, not part of `FVertexMaskForgeNoiseParams`, and not reachable by Accept, Cancel, composition, or any future recipe/preset mechanism (this is UI state, not a generator cache — see ADR-014).
- **The M16-K.6D-5/-6 panel-level routing and UI (`PreviewSource` default/read/write, `ApplyPreviewToEntry`'s branch, `OnDynamicLayerStackMutated`, the Accept gate, the Generator Type/Fill combos' option representation, the Dynamic Layers panel's display order, drag-and-drop reordering) is not covered by any Automation test and never will be under this codebase's own established boundary** — no test anywhere constructs `SVertexMaskForgePanel` or a live Preview Component. This was independently manually validated end-to-end in the Editor as part of M16-K.6D-6 instead (Groups A–D of that checkpoint's own protocol, all PASS). The orchestrator (14 tests, including a permanent Opacity-continuity regression added during M16-K.6D-6's investigation) and the seam (6 tests) it calls remain fully automated and unmodified.
- **Several boundaries are not frozen by any test today**, including: Accept's and Cancel's own top-level flows, Preview Mode/`DisplayColorDerivation`'s behavior beyond its own pure-function contract, and a general (non-Material-Slot) cross-generator FailedAfterReady contract.
- **The full native UE 5.8 Mesh Paint architecture has not yet been audited.** `VertexMaskForgeComponentOverrideBridge`'s Mesh Paint Texture transfer (M20-E/E.1) is a narrow, evidence-grounded use of one native entry point (`UMeshPaintModeSubsystem::ImportMeshPaintTextureFromVertexColors`) — it is not a general Mesh Paint integration. Open questions not yet resolved from source (Mesh Paint texture ownership and whether Static Mesh instances share one `UMeshPaintVirtualTexture`, promotion of instance paint to Static Mesh defaults, copy-on-write behavior, exact resource lifecycle/persistence/serialization, and which native entry points a future Direct Bake architecture could safely target) are listed in the [Decision Log](VertexMaskForgeDecisionLog.md)'s Future Decision Candidates and must be resolved by a dedicated audit before Direct Bake is designed or implemented. See §2's M20 paragraphs for the Current Implemented Route vs. Planned Future Route distinction.
- **A single non-reproducing UE 5.8 Mesh Paint Virtual Texture lifecycle `ensure` was observed once during M20-E manual validation (M20-E.2).** Classified non-blocking, unproven as a Forge defect — see [Decision Log ADR-016](VertexMaskForgeDecisionLog.md#adr-016--restoration-of-the-transient-send-to-mesh-paint-texture-bridge-removal-of-the-persistent-instance-override-route) for the full disposition.
- **No parameter anywhere in Dynamic Layers distinguishes drag-time from commit-time evaluation (investigated M17-TH-DL-E, unchanged).** Every `SSpinBox` in `SVertexMaskForgePanel.cpp` uses `OnValueChanged`, which fires and synchronously recomputes on every drag tick — including expensive raycast-affecting controls (Ambient Occlusion's Samples/Max Distance, Thickness's Search Distance). No `OnValueCommitted` usage or debounce/coalescing pattern exists anywhere in this codebase to reuse; this is an accepted, pre-existing, generator-agnostic tradeoff, not a Thickness-specific regression.

---

## 17. Architectural Maintenance Protocol

1. Read this document before starting a checkpoint.
2. Audit only the symbols the checkpoint actually touches — do not re-derive the whole architecture from scratch.
3. Implement the checkpoint.
4. Validate (build + full suite, or the scope the checkpoint actually requires).
5. Update this document in the **same commit** whenever a documented contract changes.
6. Update the [Decision Log](VertexMaskForgeDecisionLog.md) whenever an architectural decision is made or revisited.
7. Update the baseline metadata at the top of this document only **after** validation and commit, never before.
8. Never record a `Planned` item as a `Current Contract`.

### Checkpoint / PR checklist

- Architecture changed?
- Ownership changed?
- Domain changed?
- Identity changed?
- Persistence changed?
- Compatibility boundary changed?
- Tests protecting the contract changed?
- Documentation update required?

---

## 18. Symbol Index

| Concept | Primary symbol | File |
|---|---|---|
| Working geometry payload | `FVertexMaskForgeWorkingMesh` | `VertexMaskForgeWorkingMeshTypes.h` |
| Geometry identity + installed mesh owner | `FVertexMaskForgeWorkingMeshOwner` | `VertexMaskForgeWorkingMeshOwner.h` |
| Per-component color lifecycle owner | `FVertexMaskForgeWorkingStateOwner` | `VertexMaskForgeWorkingStateOwner.h` |
| Per-component color state | `FVertexMaskForgePreviewComponentState` | `VertexMaskForgeWorkingMeshTypes.h` |
| Legacy fixed generator state | `FVertexMaskForgeGeneratorState` | `VertexMaskForgeWorkingMeshTypes.h` |
| Legacy layer composition | `VertexMaskForgeGeneratorLayerBridge::ComposeGeneratorLayersSequential` | `VertexMaskForgeGeneratorLayerBridge.h/.cpp` |
| Legacy per-layer params | `FVertexMaskForgeMaskLayerParams` | `VertexMaskForgeMaskTypes.h` |
| Preview Mode enum | `EVertexMaskForgePreviewMode` | `VertexMaskForgeMaskTypes.h` |
| Preview Source (M18: TECHNICAL DEBT — no write site remains; always `Dynamic`; guards one deliberately-retained unreachable Legacy composition block only) | `EVertexMaskForgePreviewSource` / `SVertexMaskForgePanel::PreviewSource` | `SVertexMaskForgePanel.h` |
| Display-only reduction | `VertexMaskForgeDisplayColorDerivation::DeriveDisplayColors` | `VertexMaskForgeDisplayColorDerivation.h/.cpp` |
| Dynamic ordered layer stack | `FVertexMaskForgeDynamicLayerStack` | `VertexMaskForgeDynamicLayerStack.h` |
| Dynamic layer | `FVertexMaskForgeLayer` | `VertexMaskForgeLayerTypes.h` |
| Dynamic mask instance identity | `FVertexMaskForgeGeneratorMaskInstance` | `VertexMaskForgeLayerTypes.h` |
| Generator result payload | `FVertexMaskForgeInstanceMaskResult` | `VertexMaskForgeInstanceResultStore.h` |
| Generator result store | `FVertexMaskForgeInstanceResultStore` | `VertexMaskForgeInstanceResultStore.h` |
| Dynamic per-index composition | `VertexMaskForgeDynamicLayerEvaluator::EvaluateColor` | `VertexMaskForgeDynamicLayerEvaluator.h/.cpp` |
| Dynamic batch composition | `VertexMaskForgeDynamicLayerBatchCompositor::ComposeColors` | `VertexMaskForgeDynamicLayerBatchCompositor.h/.cpp` |
| Dynamic Material Slot generation | `VertexMaskForgeDynamicMaskGeneration::GenerateStoredResultForMaterialSlotInstance` | `VertexMaskForgeDynamicMaskGeneration.h/.cpp` |
| Isolated Recipe/library model | `FVertexMaskForgeRecipe`, `FVertexMaskForgeMaskInstance` | `VertexMaskForgeRecipeTypes.h` |
| Accept read + validation | `VertexMaskForgeAcceptTargetBuilder::BuildAcceptTargets` | `VertexMaskForgeAcceptTargetBuilder.h/.cpp` |
| Accept write | `VertexMaskForgeAcceptWriter::WriteAcceptTargets` | `VertexMaskForgeAcceptWriter.h/.cpp` |
| Panel orchestration | `SVertexMaskForgePanel` | `SVertexMaskForgePanel.h/.cpp` |
| Material Slot generator | `VertexMaskForgeMaterialSlotGenerator` | `VertexMaskForgeMaterialSlotGenerator.h/.cpp` |
| Dynamic Source-Topology composition orchestrator (production caller: `ApplyPreviewToEntry`'s Dynamic branch — M16-K.6D-5) | `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` | `VertexMaskForgeDynamicSourceTopologyComposition.h/.cpp` |
| Dynamic Accept target construction (production caller: `AcceptPendingChanges()`'s Dynamic branch — M16-K.6D-7B) | `VertexMaskForgeDynamicAcceptTargetBuilder::BuildSourceTopologyAcceptTargets` | `VertexMaskForgeDynamicAcceptTargetBuilder.h/.cpp` |
| Dynamic Accept UI-eligibility primitive (production caller: `RecomputeOperationState()` — M16-K.6D-7B) | `FVertexMaskForgeDynamicLayerStack::HasAnyEnabledLayer` | `VertexMaskForgeDynamicLayerStack.h` |
| Dynamic Bounding Box parameter type (layer-owned, Local-space only — M16-K.6D-8B/8C-C) | `FVertexMaskForgeBoundingBoxParams` / `FVertexMaskForgeAxisMaskParams` | `VertexMaskForgeRecipeTypes.h` / `VertexMaskForgeWorkingMeshTypes.h` |
| Dynamic Bounding Box per-layer UI (production caller: `BuildDynamicLayerRow` — M16-K.6D-8C-C) | `SVertexMaskForgePanel::BuildDynamicBoundingBoxLayerParamsBlock` / `BuildDynamicBoundingBoxAxisRow` / `MutateDynamicBoundingBoxAxisParam` | `SVertexMaskForgePanel.h/.cpp` |
| Directional Normal generator (existing, unmodified; production caller for Dynamic: the orchestrator's Pass 1 — M16-K.6D-8D-B) | `VertexMaskForgeDirectionalNormalGenerator::GenerateDirectionalNormalMaskFromDynamicMesh` | `VertexMaskForgeDirectionalNormalGenerator.h/.cpp` |
| Dynamic Directional Normal parameter type (layer-owned, Local-space only — M16-K.6D-8D-B/8D-C) | `FVertexMaskForgeDirectionalNormalParams` | `VertexMaskForgeRecipeTypes.h` |
| Dynamic Directional Normal per-layer UI (production caller: `BuildDynamicLayerRow` — M16-K.6D-8D-C) | `SVertexMaskForgePanel::BuildDynamicDirectionalNormalLayerParamsBlock` / `IsDynamicDirectionalNormalLayerLocalSpaceCompatible` / `MutateDynamicDirectionalNormalParam` | `SVertexMaskForgePanel.h/.cpp` |
| Curvature generator (existing, unmodified; production caller for Dynamic: the orchestrator's Pass 1 — M16-K.6D-8E-B) | `VertexMaskForgeCurvatureGenerator::GenerateCurvatureMaskFromDynamicMesh` | `VertexMaskForgeCurvatureGenerator.h/.cpp` |
| Dynamic Curvature parameter type (layer-owned — M16-K.6D-8E-B/8E-C) | `FVertexMaskForgeCurvatureParams` | `VertexMaskForgeRecipeTypes.h` |
| Dynamic Curvature transient within-call raw-cache owner (function-local, never persisted — M16-K.6D-8E-B) | `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology`'s own `CurvatureLocalGeneratorState` | `VertexMaskForgeDynamicSourceTopologyComposition.cpp` |
| Dynamic Curvature per-layer UI (production caller: `BuildDynamicLayerRow` — M16-K.6D-8E-C) | `SVertexMaskForgePanel::BuildDynamicCurvatureLayerParamsBlock` / `MutateDynamicCurvatureParam` | `SVertexMaskForgePanel.h/.cpp` |
| Noise generator (existing, unmodified; production caller for Dynamic: the orchestrator's Pass 1 — M16-K.6D-8F-B) | `VertexMaskForgeNoiseGenerator::GenerateNoiseMaskFromDynamicMesh` | `VertexMaskForgeNoiseGenerator.h/.cpp` |
| Dynamic Noise parameter type (layer-owned, 17 fields — M16-K.6D-8F-B/8F-C) | `FVertexMaskForgeNoiseParams` | `VertexMaskForgeRecipeTypes.h` |
| Dynamic Noise transient per-layer raw-cache owner (function-local, one per enabled Noise layer, never shared, never persisted — M16-K.6D-8F-B) | `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology`'s own `NoiseLocalGeneratorState` | `VertexMaskForgeDynamicSourceTopologyComposition.cpp` |
| Dynamic Noise per-layer UI (production caller: `BuildDynamicLayerRow` — M16-K.6D-8F-C; artist-facing label "Noise/Grunge") | `SVertexMaskForgePanel::BuildDynamicNoiseLayerParamsBlock` / `MutateDynamicNoiseParam` | `SVertexMaskForgePanel.h/.cpp` |
| Dynamic Noise Lock Axes UI metadata (ephemeral, panel-owned, per-`LayerId`, not part of `FVertexMaskForgeNoiseParams` — M16-K.6D-8F-C.1) | `SVertexMaskForgePanel::DynamicNoiseScaleAxesLockedByLayerId` / `IsDynamicNoiseScaleAxesLocked` / `OnDynamicNoiseScaleAxesLockChanged` | `SVertexMaskForgePanel.h/.cpp` |
| Dynamic Noise Lock Axes production-internal algorithm (external linkage, shared honestly by the real callbacks above and this module's own automation tests — M16-K.6D-8F-D.1) | `VertexMaskForgePanel::NormalizeDynamicNoiseScaleForAxisLock` / `VertexMaskForgePanel::ApplyDynamicNoiseScaleXEdit` | `SVertexMaskForgePanel.h/.cpp` |
| Ambient Occlusion generator (existing, unmodified; production caller for Dynamic: the orchestrator's Pass 1 — M16-K.6D-8G-D) | `VertexMaskForgeAmbientOcclusionGenerator::GenerateAmbientOcclusionMaskFromDynamicMesh` | `VertexMaskForgeAmbientOcclusionGenerator.h/.cpp` |
| Dynamic Ambient Occlusion parameter type (layer-owned, unconditionally World-Space — M16-K.6D-8G-D/8G-F) | `FVertexMaskForgeAmbientOcclusionParams` | `VertexMaskForgeRecipeTypes.h` |
| Dynamic Ambient Occlusion persistent per-component/per-`LayerId` cache map (Model D — M16-K.6D-8G-C) | `FVertexMaskForgePreviewComponentState::DynamicSourceTopologyAOCachesByLayerId` | `VertexMaskForgeWorkingMeshTypes.h` |
| Dynamic Ambient Occlusion Normal-Overlay-Element-ID domain resolution (function-local lazy map, mirrors `CornerToVertexID` — M16-K.6D-8G-D) | `ELayerMaskDomain::NormalOverlayElement` / `CornerToElementID` | `VertexMaskForgeDynamicSourceTopologyComposition.cpp` |
| Dynamic Ambient Occlusion per-layer UI (production caller: `BuildDynamicLayerRow` — M16-K.6D-8G-F) | `SVertexMaskForgePanel::BuildDynamicAmbientOcclusionLayerParamsBlock` / `MutateDynamicAmbientOcclusionParam` | `SVertexMaskForgePanel.h/.cpp` |
| Thickness generator (existing, unmodified; production caller for Dynamic: the orchestrator's Pass 1 — M17-TH-DL-B) | `VertexMaskForgeThicknessGenerator::GenerateThicknessMaskFromDynamicMesh` | `VertexMaskForgeThicknessGenerator.h/.cpp` |
| Dynamic Thickness parameter type (layer-owned, Source-Topology only — M17-TH-DL-B; struct itself pre-dates this checkpoint; `LevelsMin`/`LevelsMax` added M17-TH-DL-E) | `FVertexMaskForgeThicknessParams` | `VertexMaskForgeRecipeTypes.h` |
| Dynamic Thickness partial-validity substitute and Levels postprocess (M17-TH-DL-E, root-cause fix for the RGB Vertex Color preview fallback) | `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology`'s own Thickness branch | `VertexMaskForgeDynamicSourceTopologyComposition.cpp` |
| Dynamic Thickness persistent per-component/per-`LayerId` cache map (Model D — M17-TH-DL-B) | `FVertexMaskForgePreviewComponentState::DynamicSourceTopologyThicknessCachesByLayerId` | `VertexMaskForgeWorkingMeshTypes.h` |
| Dynamic Thickness per-layer UI (production caller: `BuildDynamicLayerRow` — M17-TH-DL-B) | `SVertexMaskForgePanel::BuildDynamicThicknessLayerParamsBlock` / `MutateDynamicThicknessParam` | `SVertexMaskForgePanel.h/.cpp` |
| Per-layer Alpha ownership, fourth Channel Filter bit (default `false` — M19) | `FVertexMaskForgeLayer::bAffectAlpha` | `VertexMaskForgeLayerTypes.h` |
| Pure four-channel toggle-isolation helper (Alpha added M19) | `EVertexMaskForgeDynamicLayerChannel` / `FVertexMaskForgeDynamicLayerChannelToggleResult` / `ResolveDynamicLayerChannelToggle` | `VertexMaskForgeLayerTypes.h` |
| Four-channel stack mutation (Alpha added M19) | `FVertexMaskForgeDynamicLayerStack::SetLayerChannelFilter` | `VertexMaskForgeDynamicLayerStack.h/.cpp` |
| Four-channel sequential evaluator primitive (new `FVector4f` overload, M19 — sibling of the pre-existing `FVector3f` overload, never widened in place, to avoid forcing changes onto the unrelated Legacy/Recipe `VertexMaskForgeGeneratorLayerBridge`/`VertexMaskForgeFillLayerResolution` callers) | `VertexMaskForgeSequentialEvaluator::EvaluateFillLayerStep` (`FVector4f` overload) | `VertexMaskForgeSequentialEvaluator.h/.cpp` |
| Live four-channel Layers composition (Alpha added M19; production caller: `ApplyPreviewToEntry`'s Dynamic branch and `BuildSourceTopologyAcceptTargets`) | `VertexMaskForgeDynamicSourceTopologyComposition::ComputeComposedColorsRGBSourceTopology` | `VertexMaskForgeDynamicSourceTopologyComposition.h/.cpp` |
| Alpha-only layer-row tint (White case added M19, implementing this enum's own pre-existing FUTURE DECISION doc comment) | `EVertexMaskForgeDynamicLayerChannelTint::White` / `ResolveDynamicLayerChannelTint` / `GetDynamicLayerChannelTintColor` | `VertexMaskForgeLayerTypes.h` |
| Artist-facing Alpha checkbox (production caller: `BuildDynamicLayerRow` — M19) | `SVertexMaskForgePanel::BuildDynamicLayerRow`'s `A` `SCheckBox` block | `SVertexMaskForgePanel.h/.cpp` |
| Send to Mesh Paint Texture bridge — reads Accepted asset colors, invokes native From Vertex import, restores temporary override state (M20-E) | `VertexMaskForgeComponentOverrideBridge::BuildTransferTargets` / `TransferToMeshPaintTexture` | `VertexMaskForgeComponentOverrideBridge.h/.cpp` |
| Session-independent transfer-button candidate list and enablement (M20-E.1) | `SVertexMaskForgePanel::MeshPaintTransferCandidateMeshes` / `RefreshMeshPaintTransferCandidates` / `CanSendToMeshPaintTexture` | `SVertexMaskForgePanel.h/.cpp` |
