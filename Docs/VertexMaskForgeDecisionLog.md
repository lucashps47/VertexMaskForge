# Vertex Mask Forge Architectural Decision Log

This file records architectural decisions and the reasoning behind them, as evidenced by the current code and test suite. [VertexMaskForgeArchitecture.md](VertexMaskForgeArchitecture.md) describes the state those decisions produced; this file records *why* and *when in the project's own checkpoint sequence* each decision was made. Entries here are historical — do not edit an ADR to match a later change; instead add a new ADR and mark supersession.

---

## ADR-001 — Separate Baseline and Working Color State

- **Status:** Accepted
- **Decision:** `BaselineColors`, `CommittedColors`, and `WorkingColors` (and their `SourceTopology*` siblings) are three independently-addressable arrays on `FVertexMaskForgePreviewComponentState`, not one array with implicit derivation.
- **Context:** Composition needs a stable, non-accumulating reference point (Baseline) to recompute from on every recomposition, a separate settled reference point for Channel-Filter fallback and Fill consolidation (Committed), and a live, display/Accept-facing result (Working).
- **Consequences:** Any recomposition can be re-derived cleanly from Baseline without accumulating error across repeated calls; Committed and Working can diverge intentionally (e.g. mid-edit preview vs. last consolidated state) without corrupting Baseline.
- **Protected by:** `VertexMaskForge.WorkingStateOwner.Restore`, `VertexMaskForge.WorkingStateOwner.ComposedColorsControlled`.
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-002 — Treat Render Vertices and Source Topology as Distinct Domains

- **Status:** Accepted
- **Decision:** Every color-state array (Baseline/Committed/Working) and every fixed generator's entry point exists in two parallel forms — a render-vertex form and a source-topology (`FDynamicMesh3` triangle-corner) form — rather than one unified representation. The Dynamic model's own result payload/store (`FVertexMaskForgeInstanceMaskResult`/`FVertexMaskForgeInstanceResultStore`) does **not** follow this pattern: it is domain-agnostic by construction (no domain tag, no typed variant), and relies entirely on caller discipline to stay within one domain per `MaskInstanceId` (see [Architecture §3](VertexMaskForgeArchitecture.md#3-domain-model)).
- **Context:** Render-buffer vertex identity (per render-buffer vertex index, with geometric positions routinely duplicated at attribute seams) and source-topology identity (per triangle corner, e.g. for Nanite) are not interchangeable; a shared geometric position can legitimately need different values across the render-buffer vertices or triangle corners that share it.
- **Consequences:** Every new color-state array or fixed-generator entry point must decide which domain it targets; nothing automatically bridges the two. The Dynamic result store's lack of a domain tag is a real, separately-tracked risk (see Known Limitations in the architecture doc), not something this decision itself resolves.
- **Protected by:** The `WorkingMeshDomainSplit` test family, and every generator's paired render-vertex/`FromDynamicMesh` entry points.
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-003 — Identify Dynamic Mask Results by MaskInstanceId

- **Status:** Accepted
- **Decision:** A Dynamic generator's result is keyed by a `FGuid` (`MaskInstanceId`) that is stable for the lifetime of the `FVertexMaskForgeGeneratorMaskInstance` that owns it — preserved across reorder, rename, and confirmed parameter edits (`SetLayerMaskParams`) — and independent of the layer's own `LayerId` and of array position. A **new** `MaskInstanceId` is minted only when `SetLayerMaskGeneratorType` actually changes a layer's generator type (assigning a first mask, or replacing one type with a different one); re-assigning the same type is a no-op that preserves the existing id. `LayerId` remains independent of `MaskInstanceId` throughout.
- **Context:** Layer order/position is mutable (reorder, remove, re-insert); a result keyed by index would silently become stale or misattributed after any reorder.
- **Consequences:** A layer and its mask's result can be correctly re-associated after any stack mutation or parameter edit that preserves the generator type, at the cost of the store having no domain awareness of its own (see ADR-002's consequence and the architecture doc's Known Limitations).
- **Protected by:** The `SetLayerMaskGeneratorType`/`SetLayerMaskParams` contract tests inside `DynamicLayerStackTests.cpp`, and the `MaskInstanceId`-preservation assertion in `VertexMaskForge.DynamicMaskGeneration.GeneratedMaterialSlotResultDrivesEvaluatedColorAtBothCorners`.
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-004 — Keep Composition Pure Until Explicit Application

- **Status:** Accepted
- **Decision:** Every composition function (`ComposeGeneratorLayersSequential`, `EvaluateColor`, `ComposeColors`) is a pure computation over its inputs, returning or writing only to caller-owned output — none of them mutate any owner's stored color state. Within the initialized color-state composition lifecycle, applying a freshly composed output requires `ApplyComposedColorsRGB` (gated by a chosen `EColorCommitMode`), while `RestoreFromBaseline` and `RestoreFromCommitted` are explicit restoration mutations. This decision does not attempt to enumerate initialization, capture, reset, or reattachment mutations performed by the owner — those belong to the owner's broader lifecycle, not to the composition-purity contract this ADR freezes.
- **Context:** Decoupling "compute a candidate result" from "commit it" lets a caller preview, discard, or repeatedly recompute without side effects, and lets a checkpoint prove composition correctness without needing to also prove persistence correctness in the same test.
- **Consequences:** Any future orchestrator that wants Dynamic composition to actually affect a preview must explicitly call `ApplyComposedColorsRGB` — this is a deliberate, not accidental, gap today.
- **Protected by:** `VertexMaskForge.WorkingStateOwner.ComposedColorsControlled`, and `VertexMaskForge.DynamicCompositionSourceTopologyIntegration.RealSourceTopologyBaselineAndRealStoredMaterialSlotResultProduceExpectedColorsAcrossCoveredAndUncoveredCorners`'s own explicit non-mutation assertions.
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-005 — Preserve the Previous Ready Material Slot Result Across Failed Dynamic Regeneration

- **Status:** Accepted
- **Decision:** Restricted explicitly to the Dynamic Material Slot generation path implemented by `VertexMaskForgeDynamicMaskGeneration::GenerateStoredResultForMaterialSlotInstance`: a generation call that fails never overwrites a previously-stored `Ready` result for the same `MaskInstanceId` — the store is left exactly as it was.
- **Context:** A transient or genuine failure during regeneration (e.g. a temporarily invalid parameter state) should not degrade an already-correct preview to a blank/zero-coverage one.
- **Consequences:** This contract is proven only for `GenerateStoredResultForMaterialSlotInstance`. It is **not** generalized to any other generator, present or future, and no other generation path is claimed to share this behavior. A general cross-generator FailedAfterReady policy is an open question (see Future Decision Candidates), not a decided contract.
- **Protected by:** `VertexMaskForge.DynamicMaskGeneration.FailedRegenerationPreservesPreviousReadyResult`.
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-006 — Separate Global Channel Filtering from Per-Layer Channel Participation

- **Status:** Accepted
- **Decision:** The panel's global Channel Filter (`bChannelFilterR/G/B`, legacy-only) and a Dynamic layer's own `bAffectRed/Green/Blue` are two structurally independent mechanisms with different owners, different scopes, and different fallback behavior — never treated as the same state or as convertible into one another, in the code as it exists today.
- **Context:** An earlier architectural audit in this project's history conflated the two; a corrective audit established by direct code inspection that the legacy layer struct (`FVertexMaskForgeMaskLayerParams`) has no per-layer channel field at all, and that the global filter's `CommittedColors` fallback is fused into the legacy bridge's own function, not a separate later stage.
- **Consequences:** Any future Dynamic-to-panel integration must make an explicit architectural decision about whether, where, and in what order the global filter participates alongside per-layer channel participation; the current code does not freeze that integration policy, and this decision does not presume or mandate one.
- **Protected by:** `VertexMaskForge.GeneratorLayerBridge.ChannelFilter` (global), `VertexMaskForge.DynamicLayerEvaluator.ChannelFilterSingleChannel`/`ChannelFilterAllDisabled` (per-layer).
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-007 — Preserve Stable Layer Ordering

- **Status:** Accepted
- **Decision:** Both layer models guarantee that reordering operations preserve every other layer's relative order and identity — never a destructive rebuild, never an implicit renumbering that could silently reassign one layer's configuration to another's slot.
- **Context:** The legacy model expresses order via a fixed 7-slot enum permutation (`GeneratorLayerOrder`); the Dynamic model expresses it via index position with identity-preserving move operations (`MoveLayerUp`/`MoveLayerDown`/`MoveLayer`, resolved by `LayerId`, never a cached index).
- **Consequences:** Reordering is safe to expose directly to interactive UI without risking silent data corruption on repeated moves.
- **Protected by:** `VertexMaskForge.DynamicLayerStack.MoveUpAndMoveDown`, `VertexMaskForge.DynamicLayerStack.RepeatedMovesPreservePermutation`; legacy ordering protected by the `VertexMaskForge.PanelCompositionIntegration.*` reorder tests.
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-008 — Keep the Dynamic Composition Subsystem Isolated Until Its Contracts Are Proven

- **Status:** Accepted
- **Decision:** Every Dynamic-model building block (`FVertexMaskForgeDynamicLayerStack`, `VertexMaskForgeDynamicLayerEvaluator`, `VertexMaskForgeDynamicLayerBatchCompositor`, `VertexMaskForgeDynamicMaskGeneration`, `FVertexMaskForgeInstanceResultStore`) was built and independently proven by its own dedicated test suite with **zero production composition/output caller** — no production code path connects Dynamic evaluation or composition to `WorkingColors`, preview, or Accept — one small checkpoint at a time, rather than wiring any of it into the panel's output prematurely. (The Dynamic Layers Slate UI section is itself a real production caller of the *stack's own editing API* — `AddLayer`, `SetLayer*`, reorder — but that UI's edits do not reach evaluation or composition; see [Architecture §15](VertexMaskForgeArchitecture.md#15-current-integration-status).)
- **Context:** A sequence of read-only architectural audits (culminating in M16-K.5I and its corrective rounds) established that the smallest-safe-step discipline this project already follows for generator work applies equally to composition infrastructure — integrating before every structural boundary (domain handling, identity, absence-case semantics) is independently proven would risk baking in unproven assumptions.
- **Consequences:** The Dynamic subsystem is fully real and tested but currently inert with respect to any visible output — this is intentional, not an oversight (see [Architecture §15](VertexMaskForgeArchitecture.md#15-current-integration-status)).
- **Protected by:** The entire `DynamicLayerStackTests`, `DynamicLayerEvaluatorTests`, `DynamicLayerBatchCompositorTests`, `DynamicMaskGenerationTests`, and `InstanceResultStoreTests` suites, none of which reference `SVertexMaskForgePanel`.
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-009 — Prove Dynamic Source-Topology Integration Before Panel Migration

- **Status:** Accepted
- **Decision:** Before any panel wiring is attempted, prove — with real production objects, not hand-built fixtures — that the full chain from a real `FVertexMaskForgeWorkingStateOwner` baseline through real Material Slot generation, real store move-preservation (`InstallWorkingMesh`), to real batch composition produces byte-exact, non-mutating output, restricted to the one domain (source-topology) where every underlying API is already exercised by existing tests without inventing new fixtures.
- **Context:** An audit of the render-vertex Material Slot generation path found no existing test or fixture anywhere in the codebase constructs a real `FStaticMeshLODResources` for it; inventing one for this checkpoint would have introduced untested fixture machinery. The source-topology path reuses only already-proven construction patterns.
- **Consequences:** M16-K.5J's proof is deliberately scoped to source-topology + Material Slot only; render-vertex Dynamic generation and any other generator's Dynamic integration remain unproven and are not implied by this result.
- **Protected by:** `VertexMaskForge.DynamicCompositionSourceTopologyIntegration.RealSourceTopologyBaselineAndRealStoredMaterialSlotResultProduceExpectedColorsAcrossCoveredAndUncoveredCorners`.
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-010 — Restrict Dynamic Material Slot Editing to Single-Mesh Selections

- **Status:** Accepted
- **Decision:** Editing (and any future evaluation) of a Dynamic layer's `FVertexMaskForgeMaterialSlotParams` is supported only when `SelectedMeshes.Num() == 1` — i.e. exactly one unique Static Mesh asset is represented across the current selection (`SelectedMeshes` is deduplicated by asset path; multiple selected components referencing the same asset still collapse to that one entry, per `AddOrUpdateSelectedMesh`'s own dedup key). `SelectedSlotIndex` remains the payload's sole persisted identity (a raw index, never a name); the future UI's list of selectable indices must be built from that one asset entry's own `WorkingMesh.MaterialSlotOptions`. With zero or more than one such asset entry, Material Slot editing (and generation) is unavailable — the system does not compute an intersection or union of slots across meshes, does not pick an arbitrary mesh from the selection, does not silently apply the same raw index across heterogeneous layouts, and does not clamp an out-of-range index to a fallback such as `0`. An index that is negative or outside `MaterialSlotOptions` for that one asset entry must be surfaced explicitly as invalid/unavailable, never silently accepted. No sentinel value for "no slot selected" is introduced into the payload; `Clear`/reassignment continue to follow the model's own existing defaults (`SelectedSlotIndex=0, bInvert=false`, minted fresh by `SetLayerMaskGeneratorType`/`MakeVertexMaskForgeGeneratorParams`). This decision governs UI/orchestration policy only — it does not itself implement the gate, any validation UI, or any reconciliation logic.
- **Context:** A M16-K.6C implementation attempt exposed `SelectedSlotIndex` via an open-domain (`[0, INT32_MAX]`) spin box with no reference to any mesh's actual material slot count. A corrective audit found that `SelectedSlotIndex`'s real valid domain is per-entry (`[0, WorkingMesh.MaterialSlotOptions.Num())`, confirmed directly in `VertexMaskForgeMaterialSlotGenerator.cpp`'s own `IsValidIndex` guard, which resolves to `EVertexMaskForgeScalarMaskState::Unavailable` outside that range), while `FVertexMaskForgeDynamicLayerStack` is panel-global — so a single panel-wide index has no single well-defined meaning across a heterogeneous multi-mesh selection. `SetLayerMaskParams` itself validates only that the `Params` variant's active alternative matches `GeneratorType`, never the field's semantic validity against any mesh — so the model provides no structural guard against this ambiguity on its own. The identical constraint is already implemented in the legacy path: `SVertexMaskForgePanel::IsMaterialSlotMaskAvailableForSelection() const { return SelectedMeshes.Num() == 1; }`, with `ReconcileMaterialSlotSelection()` building its slot list from `SelectedMeshes[0]`'s own `WorkingMesh.MaterialSlotOptions` and leaving the list empty for zero/multiple entries — an existing legacy constraint, not a new one invented by this decision.
- **Consequences:** The future Material Slot parameter-editing UI (and any future single-mesh-scoped Dynamic generation orchestration) must reuse this exact single-mesh eligibility gate rather than inventing a new one. No change to `FVertexMaskForgeMaterialSlotParams`, `SetLayerMaskParams`, the Material Slot generator, or any result store is required by this decision — it constrains only how a future UI/orchestrator is allowed to use the already-existing API. The M16-K.6C UI attempt was reverted before commit as a direct consequence of this decision.
- **Alternatives considered:**
  - *Raw index applied indiscriminately across multiple meshes* (the reverted M16-K.6C approach) — rejected: cannot distinguish a valid index from one that is out of range or means something different for another mesh; no domain feedback possible.
  - *Intersection of valid slot indices across all selected meshes* — rejected for now: no precedent, no test coverage, and silently narrows what indices are even offered without the user necessarily understanding why; a real design in its own right, not a small fix.
  - *Identity by material slot name instead of index* — rejected for now: `FVertexMaskForgeMaterialSlotParams` has no name field; this would be a payload/model contract change, not a UI-only change, and slot names are not guaranteed unique or stable in the way this project's existing code already treats them as "just a label."
  - *Composite identity (name + index, or per-mesh index map)* — rejected for now: same reasoning — a real payload/model contract change, out of scope for restoring a working, precedented editing gate.
  - *Per-entry (rather than panel-global) Dynamic configuration for this one parameter* — rejected for now: would special-case Material Slot's storage location differently from every other Dynamic layer property, without a broader decision about whether `DynamicLayerStack` itself should become per-entry.
  - *Single-mesh-only, mirroring the legacy precedent* — **selected**: reuses an established precedent already implemented in the current legacy flow (`IsMaterialSlotMaskAvailableForSelection`); requires no change to the payload, generator, or result-store model; keeps the fix small and reversible; gives a concrete, non-ambiguous domain (`WorkingMesh.MaterialSlotOptions` of the one asset entry) for a future UI to build against.
- **Protected by:** No automated test yet — this ADR records a policy decision for implementation in a future checkpoint, not an implemented and tested contract. The legacy precedent it mirrors (`IsMaterialSlotMaskAvailableForSelection`) has no dedicated automation test of its own either (confirmed by symbol search during the corrective audit).
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-011 — Authoritative Preview Source and Non-Persistible Dynamic Preview

- **Status:** Accepted
- **Decision:** A new explicit, panel-owned enum, `EVertexMaskForgePreviewSource` (`Legacy`, `Dynamic`), is the sole authority for which pipeline's semantically composed colors the preview currently shows. Owner: `SVertexMaskForgePanel` (member `PreviewSource`); lifetime: the panel instance's own, identical to `CurrentPreviewMode`'s lifetime. Default: `Legacy`. Only an explicit user action on a dedicated future control may change it — never inferred from Dynamic Layer Stack contents, assignment presence, generator availability, result-store contents, Accept state, Fill state, Material Slot selection, tab/section expansion, the last-edited control, or generation success/failure. **This checkpoint (M16-K.6D-1) introduces only the type and the member, both inert** — no function reads `PreviewSource` to select behavior yet, no function writes it to `Dynamic`, and no UI control exists to change it. The remainder of this ADR records the *contract* this authority establishes for future checkpoints — it is a decision about what must eventually hold, not a description of code that exists today.
  - **Legacy contract (current, unchanged):** `Legacy → WorkingColors → preview → Accept`. `WorkingColors`/`SourceTopologyWorkingColors` remain the sole semantically persistible buffer, written only by `ApplyComposedColorsRGB`/`RestoreFromBaseline`/`RestoreFromCommitted` (ADR-004), read verbatim by `BuildAcceptTargets` (unchanged). Returning from `Dynamic` to `Legacy` must trigger a full Legacy recomposition through the existing `RecomposeWorkingColors`/`ApplyPreviewToEntry` path — `WorkingColors` must never be assumed to have remained visually or semantically current while `Dynamic` was selected, because Legacy composition does not run while Dynamic is being shown (future checkpoint).
  - **Dynamic contract (future, not yet implemented):** `Dynamic → local/transitory buffer → preview visual only`. This buffer must never write `WorkingColors`/`SourceTopologyWorkingColors`, must never be reachable by `BuildAcceptTargets`, and must never become persistible merely because it was displayed. It must not be owned by `FVertexMaskForgeWorkingStateOwner` (its exact owner/shape is deliberately deferred — see Future Decision Candidates and the M16-K.6D-2 checkpoint). On generation/composition failure, or when the ADR-010 single-asset gate is not satisfied, any previously-shown Dynamic visual must be discarded (no stale result left on screen) — `PreviewSource` itself must NOT silently revert to `Legacy` on failure (no automatic fallback); the Dynamic Layer Stack's own configuration must survive unavailability untouched (indisponibilidade never mutates `SelectedSlotIndex`/`bInvert`/any other Params).
  - **Preview Mode / Channel Filter:** Preview Mode (`EVertexMaskForgePreviewMode`) is a display-only reduction that must apply to whichever pipeline `PreviewSource` currently selects — it never participates in choosing the pipeline itself. The global Channel Filter (`bChannelFilterR/G/B`) remains Legacy-only (ADR-006 is not superseded by this ADR); it must not be repurposed as a general Dynamic display filter, and must become unavailable in the UI while `PreviewSource == Dynamic` (future checkpoint — not implemented here).
  - **Fill:** Fill Black/White (`OnFillWhiteClicked`/`OnFillBlackClicked` → `RunConstantFill`) remains Legacy-only and must become unavailable in the UI while `PreviewSource == Dynamic` (future checkpoint — not implemented here). This is unrelated to `FVertexMaskForgeLayer::Fill` (the Dynamic model's own, differently-scoped per-layer field), which this ADR does not touch.
  - **Accept and Cancel:** isolation of Accept from any Dynamic-sourced buffer is a **structural** guarantee (the buffer's own owner/lifetime, per the Dynamic contract above, never intersects `BuildAcceptTargets`'s inputs) — not a runtime `if (Dynamic) { ... }` guard inside Accept. As an additional UX safeguard (not the primary barrier), Accept must become unavailable while `PreviewSource == Dynamic` in a future checkpoint, so a user is never shown an Accept affordance that would persist something other than what they are currently viewing. Cancel's contract is unaffected and must continue to work identically regardless of `PreviewSource`. Accept, Cancel, Undo, and Redo are **not modified by this checkpoint**.
  - **Future extensibility:** this contract must not block a future Render Vertex domain — extending `EVertexMaskForgePreviewSource`'s effective scope to Render Vertex, or introducing additional values, is left to whichever future checkpoint actually connects it, via explicit new values or a deliberately revisited abstraction, never by silently overloading `Legacy`/`Dynamic`'s existing meaning. Render Vertex is not implemented by this ADR.
- **Context:** M16-K.6D (rejected, see the M16-K.6D-R1 recovery) attempted to wire Dynamic Material Slot + Source Topology composition directly into `ApplyPreviewToEntry`, introducing an unauthorized `bool bDynamicPreviewActive` and writing the Dynamic composed result into the same `WorkingColors` buffer Accept reads verbatim — creating an unreviewed path by which a Dynamic preview could become persistible without any change to Accept's own code. The M16-K.6D-R1 recovery audit confirmed no authority for choosing between Legacy and Dynamic existed anywhere in the codebase (`EVertexMaskForgePreviewMode` is display-only; the Dynamic Layers UI section has no visibility gate and coexists unconditionally alongside Legacy's own "Layers" section), and that `BuildAcceptTargets` reads `WorkingColors`/`SourceTopologyWorkingColors` verbatim with no origin metadata to distinguish Legacy-produced from any other source. The M16-K.6D-R2 audit compared candidate authority models and recommended `EVertexMaskForgePreviewSource` (a preview-source/pipeline-selection enum) over an "Active Editing Surface" framing (misleading, since Legacy and Dynamic configuration UI coexist and remain simultaneously editable regardless of which pipeline is previewed) or continuing with no authority at all (which structurally cannot support any future Dynamic connection safely).
- **Consequences:** Every future checkpoint that wires Dynamic composition into any real output must read and respect `PreviewSource` rather than inferring activation from Dynamic Layer Stack state. `BuildAcceptTargets`/`VertexMaskForgeAcceptWriter` need no changes for this ADR's structural guarantee to hold, because the future Dynamic buffer is never given a code path into them. The UX-level "Accept unavailable while Dynamic" safeguard, and the Channel-Filter/Fill unavailability while Dynamic, remain explicitly unimplemented — future checkpoints, not covered by this ADR's own code.
- **Alternatives considered:**
  - *`bool bDynamicPreviewActive`* (the rejected M16-K.6D attempt) — rejected: binary bool cannot express a third state cleanly if one is ever needed (e.g. a future third pipeline), and the name conflates "is Dynamic active" with "is Dynamic even wired," inviting exactly the premature-inference bugs this ADR exists to prevent.
  - *Infer from Dynamic Layer Stack contents (layer/assignment/result-store existence)* — rejected: would silently turn a previously configuration-only edit (M16-K.6B/K.6C-2's own established contract) into one with a real preview effect, without any explicit user action; also fails the moment a user configures Dynamic layers while intending to keep previewing Legacy.
  - *Reuse `EVertexMaskForgePreviewMode`* — rejected: that enum is a display-only reduction of whichever pipeline is already selected (`OriginalMaterial`/`RGBVertexColor`/per-channel), not a pipeline selector; conflating the two would break the existing, tested, pure display-derivation contract (ADR-004's own composition-purity boundary).
  - *Automatic fallback to Legacy on Dynamic failure* — rejected: masks a real failure as if nothing happened, and risks silently showing/persisting Legacy's `WorkingColors` (last real composition) as though it were the user's current, intentional choice.
  - *Write the Dynamic result into `WorkingColors`* (the rejected M16-K.6D attempt) — rejected: makes Dynamic preview persistible via Accept without any change to Accept's own code, confirmed as a real (not hypothetical) risk by the M16-K.6D-R1 audit's direct trace of `BuildAcceptTargets`.
  - *A runtime condition inside Accept as the only barrier against persisting Dynamic* — rejected in favor of structural isolation (the Dynamic buffer's own owner/lifetime never reaching Accept's inputs at all): a condition is one omitted `if` away from silently regressing; structural non-reachability cannot regress by omission.
  - *Introduce the visual control (checkbox/tab/switcher) in this same checkpoint* — rejected: this checkpoint's own scope is the authority contract only; a control that lets the user select `Dynamic` before any real Dynamic output exists would be a dead/misleading affordance, and was explicitly out of scope per this checkpoint's own instructions.
  - *Store the future Dynamic buffer on `FVertexMaskForgeWorkingStateOwner` immediately* — rejected for this checkpoint: `WorkingStateOwner` is ADR-001's own carefully-scoped three-array (Baseline/Committed/Working) owner; adding a fourth, non-persistible array to it now, before its exact contract (per-entry vs. per-component vs. local-per-call) is decided, risks conflating two different lifetime/ownership questions in one change. Deferred to a dedicated future checkpoint (M16-K.6D-2/-3 in the current sequencing).
  - **Selected: `EVertexMaskForgePreviewSource` enum, panel-owned, default `Legacy`, written only by future explicit user action, structural (not conditional) isolation from Accept** — smallest change that establishes the authority contract without implementing any of the behavior it will eventually gate, matching this checkpoint's own single objective.
- **Protected by:** No automated test — this checkpoint introduces an inert type and a default-initialized member with zero behavioral surface (no reader, no writer, no UI); build success (the type compiles, the member initializes to `Legacy`) is the only verification this checkpoint's own scope supports. Future checkpoints that give this authority real behavior must add their own tests for those transitions.
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-012 — Layer-Owned Bounding Box as the Second Dynamic Generator, Local-Space Only

- **Status:** Accepted
- **Decision:** Bounding Box is exposed as the second selectable Dynamic Layer generator (after Material Slot), following the same layer-owned model established for Material Slot rather than any shared/panel-level control:
  - **Ownership:** each Dynamic layer's Bounding Box parameters live entirely in that layer's own `FVertexMaskForgeGeneratorParams` variant (`FVertexMaskForgeBoundingBoxParams`, the type already established for the future Recipe/Mask-Instance model, reused unchanged) — never a second copy, never panel-owned state, never the Legacy `SVertexMaskForgePanel::BoundingBoxAxisParams` member. Two Bounding Box layers hold fully independent values.
  - **Evaluator reuse:** the existing, unmodified `VertexMaskForgeBoundingBoxGenerator::GenerateBoundingBoxMaskFromDynamicMesh` is the sole evaluation path — no axis/gradient/mirror/invert/combination formula is duplicated in the orchestrator or in panel UI code, at any layer.
  - **Local-space only:** `VertexMaskForgeDynamicSourceTopologyComposition`'s Pass 1 rejects the whole composition call if any enabled axis requests World Space or if Unified Bounds is requested — deterministically, never silently reinterpreted as Local Space or per-mesh bounds. The Dynamic panel UI exposes no control capable of creating either request. This is a genuine, intentional scope boundary, not an oversight: World Space needs a per-component transform this orchestrator does not receive; Unified Bounds needs full-selection context it does not receive.
  - **Stable identity for Slate callbacks:** every per-layer, per-axis control captures the layer's stable `LayerId` (`FGuid`) by value, resolving the current layer/mask fresh at callback-execution time — the same pattern Material Slot already established, extended here rather than reinvented, since a captured array index or raw pointer cannot survive reorder/deletion/rebuild safely.
  - **Incompatible stored state is surfaced, never normalized:** if a layer's already-stored parameters request World Space or Unified Bounds (unreachable through this UI, but not otherwise structurally precluded), the UI shows a concise warning and disables that layer's axis controls rather than silently rewriting the data or pretending it is valid. Rejection remains solely the orchestrator's responsibility (see above) — the UI warning is a read-only echo of that same predicate, never a second enforcement point.
  - **Single authoritative evaluation path for preview and Accept:** both `ApplyPreviewToEntry`'s Dynamic branch and `VertexMaskForgeDynamicAcceptTargetBuilder` call the same orchestrator against the same live `FVertexMaskForgeDynamicLayerStack` — Bounding Box requires zero change to either, since both were already generator-agnostic.
  - **Extended to Directional Normal (M16-K.6D-8D-B/8D-C), proving out this ADR's own stated Consequence below:** Directional Normal became the third selectable Dynamic Layer generator, following this same model exactly rather than introducing a new pattern — layer-owned `FVertexMaskForgeDirectionalNormalParams` (reused unchanged), the existing unmodified `VertexMaskForgeDirectionalNormalGenerator::GenerateDirectionalNormalMaskFromDynamicMesh` as the sole evaluator, Local-space-only enforcement in the orchestrator's Pass 1 (World Space fails the whole call deterministically; `Space` is model-owned and never exposed by the UI), stable-`LayerId` Slate callbacks via a new `MutateDynamicDirectionalNormalParam` helper, and a read-only incompatible-state warning (`IsDynamicDirectionalNormalLayerLocalSpaceCompatible`) rather than any silent normalization. One generator-specific addition beyond the Bounding Box precedent: `Falloff <= Angle` is maintained as a UI-mutation-boundary invariant (reducing Angle clamps a larger stored Falloff; a new Falloff is clamped to the current Angle) — a small, local rule, not a new architectural pattern.
- **Context:** M16-K.6D-8A audited the existing Legacy Bounding Box UI/generator and the Dynamic Layers panel, finding: `FVertexMaskForgeBoundingBoxParams`/`FVertexMaskForgeAxisMaskParams` already existed and were already fully wired through the generic parts of `FVertexMaskForgeDynamicLayerStack` (`SetLayerMaskGeneratorType`, `SetLayerMaskParams`, `MakeVertexMaskForgeGeneratorParams`); the only genuinely new work needed was (1) the orchestrator's own masked-generator dispatch, and (2) a layer-owned Slate presentation. The audit also found a domain mismatch requiring explicit handling: Bounding Box's Source-Topology generator output is indexed by Dynamic Mesh VertexID, not corner, unlike Material Slot's already-corner-domain output — resolved in M16-K.6D-8B by tagging each generated mask with its own domain and resolving vertex-domain masks per corner through that corner's own triangle. The same audit found an unrelated, obsolete standalone `Layers` panel (a fixed Legacy-generator list with Up/Down buttons, from an earlier misunderstood implementation) that shared no code with Dynamic Layers and needed removing before adding new Dynamic UI, to avoid confusing the two — done separately in M16-K.6D-8C-B, with `GeneratorLayerOrder` (Legacy's own real composition order) explicitly retained.
- **Consequences:** Any future Dynamic generator (beyond Material Slot/Bounding Box) should follow this same layer-owned-parameter, orchestrator-dispatch, stable-`LayerId`-callback model rather than introducing a new pattern per generator. World Space and Unified Bounds support for Dynamic Bounding Box, if pursued, requires extending the orchestrator's own inputs (a per-component transform; a precomputed selection-wide bounds context respectively) — genuinely separate, larger checkpoints, not a UI-only change. No Dynamic layer-stack serialization, presets, or "Paint Layers" concept exists yet; this ADR does not introduce one.
- **Alternatives considered:**
  - *Extract a shared Legacy/Dynamic Bounding Box widget-construction helper* — rejected: the 8C-A audit found every Legacy control's callback body inline-coupled to panel-global `BoundingBoxAxisParams` and Legacy-only invalidation entry points (`InvalidateBoundingBoxRawMask`, `RunAutoUpdatePreview`); only the generic Slate *shape* (widget types, ranges, labels) was reusable, not the callback bodies — extraction would have been broader and riskier than a small, purpose-built Dynamic-only builder.
  - *Show World Space/Unified Bounds controls disabled with a tooltip* — rejected in favor of omitting them entirely: a disabled-but-visible control bound to a field that could already be `true` in stored data risks implying a false state or requiring extra defensive display logic; omission cannot be misread as "supported but greyed out."
  - *Silently reinterpret an incompatible stored layer as Local-space, or clamp `bWorldSpace`/`bUseUnifiedBounds` to false on load* — rejected: would be a silent data rewrite the user never asked for, and would hide a real, currently-inexpressible-through-the-UI state rather than surfacing it.
  - *Capture an array index or row pointer in each axis control's callback* — rejected: proven unsafe by the same reasoning ADR/precedent already established for Material Slot (M16-K.6C-2-FIX) — reorder, deletion, and rebuild all invalidate a captured index/pointer; stable `LayerId` does not.
- **Protected by:** 9 orchestrator tests (`VertexMaskForge.DynamicSourceTopologyComposition.BoundingBox*`, M16-K.6D-8B) covering Local-space X/Y/Z evaluation, invert/mirror, multi-axis combination, falloff, two independent layers with reorder, and explicit World Space/Unified Bounds rejection with output-preservation-on-failure. The per-layer Slate UI itself (selector option presence, warning display, control enable/disable, stable-`LayerId` callback wiring) has no automatable seam in this codebase — no test anywhere constructs `SVertexMaskForgePanel` or a live Preview Component (the same established boundary as every prior Dynamic Layers UI checkpoint) — validated manually instead (M16-K.6D-8C-C, PASS: independent two-layer state, reorder/deletion identity, generator-switch behavior, live preview, Accept/Cancel, no Legacy regression). Directional Normal's extension (above) is protected the same way: 5 orchestrator tests (`VertexMaskForge.DynamicSourceTopologyComposition.DirectionalNormal*`, M16-K.6D-8D-B) covering Local-space dispatch/corner-domain parity, full parameter forwarding including genuinely-nonzero Blur, explicit World-space rejection without param mutation, two independent layers with reorder, and disabled-layer no-op; its per-layer Slate UI was likewise validated manually (M16-K.6D-8D-C, PASS: items A–G — selector/defaults, live parameter behavior including the Angle/Falloff invariant, independent two-layer state with reorder, stable-`LayerId` deletion safety, generator switching, preview/Accept parity, no Legacy regression).
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-013 — Transient Within-Call Generator State for Dynamic Curvature

- **Status:** Accepted
- **Decision:** Curvature is exposed as the fourth selectable Dynamic Layer generator (after Material Slot, Bounding Box, Directional Normal), reusing ADR-012's own layer-owned-parameter/orchestrator-dispatch/stable-`LayerId`-callback model exactly, with one genuinely new architectural element this ADR records:
  - **Ownership:** each Dynamic layer's Curvature parameters live entirely in that layer's own `FVertexMaskForgeGeneratorParams` variant (`FVertexMaskForgeCurvatureParams`, reused unchanged) — never a second copy, never panel-owned state. Two Curvature layers hold fully independent values.
  - **Evaluator reuse:** the existing, unmodified `VertexMaskForgeCurvatureGenerator::GenerateCurvatureMaskFromDynamicMesh` is the sole evaluation path — no Type-selection/Multiplier/blur/Levels/Invert formula is duplicated in the orchestrator or in panel UI code, at any layer.
  - **No space concept:** unlike Bounding Box/Directional Normal, Curvature has no Local/World-space field and no `ComponentTransform` dependency at all — its Pass 1 branch has no space-rejection check, and its UI block has no incompatible-state warning or per-control disable gate.
  - **Transient within-call generator state (the new element):** `GenerateCurvatureMaskFromDynamicMesh` requires a mutable `FVertexMaskForgeGeneratorState&` for its own internal raw Convex/Concave cache (`EnsureCurvatureRawCache`, gated on `WorkingMesh.GeometryFingerprint`) — a requirement none of the three prior Dynamic generators had, since their own `*FromDynamicMesh` entry points are stateless. The orchestrator satisfies this with exactly one function-local `FVertexMaskForgeGeneratorState`, declared once outside Pass 1's per-layer loop and passed by reference to every enabled Curvature layer evaluated *within that single orchestrator invocation* — the first such layer computes the raw arrays, every subsequent one in the same call reuses them via the generator's own existing fingerprint gate. This state is never static, global, panel-owned, or retained beyond the single call; it never reads from or writes into Legacy's own real per-entry `FVertexMaskForgeGeneratorState`. `GenerateCurvatureMaskFromDynamicMesh` returns its `FVertexMaskForgeScalarMask` entirely by value (never aliased to the shared state's own cached arrays), so each layer's completed, independently-processed mask is safe to store even though later Curvature layers in the same call keep mutating the shared state's raw cache underneath.
  - **DynamicMeshVertex domain:** Curvature's Source-Topology output is indexed by Dynamic Mesh VertexID, not corner (like Bounding Box's), so it is tagged `ELayerMaskDomain::DynamicMeshVertex` and resolved through the existing `CornerToVertexID`/`TryGetValue` Pass 2 path — no new domain, no vertex remapping.
  - **Levels invariant:** `LevelsMin <= LevelsMax` is maintained as a UI-mutation-boundary invariant only (editing Min above Max raises Max to match; editing Max below Min lowers Min to match), inside one `MutateDynamicCurvatureParam` call — mirroring Directional Normal's own Angle/Falloff invariant pattern. The generator itself remains defensively robust regardless (`ApplyCurvatureLevels`'s own `Max(LevelsMax-LevelsMin, Epsilon)` denominator never divides by zero), so a malformed stored pair (unreachable through this UI) still displays and evaluates safely.
  - **Single authoritative evaluation path for preview and Accept:** both `ApplyPreviewToEntry`'s Dynamic branch and `VertexMaskForgeDynamicAcceptTargetBuilder` call the same orchestrator against the same live `FVertexMaskForgeDynamicLayerStack` — Curvature requires zero change to either, since both were already generator-agnostic. The orchestrator's own public signature is unchanged; the transient generator state is entirely internal to one call.
- **Context:** M16-K.6D-8E-A audited the existing Legacy Curvature UI/generator/cache and the Dynamic Layers panel, finding: `FVertexMaskForgeCurvatureParams` was already authoritative and already wired through the generic parts of `FVertexMaskForgeDynamicLayerStack`; the genuinely new work needed was (1) the orchestrator's own masked-generator dispatch, (2) a decision on how to satisfy the generator's own stateful cache parameter without introducing a persistent or shared-across-calls cache, and (3) a layer-owned Slate presentation. The audit distinguished raw geometry-derived Convex/Concave arrays (safely shareable across Curvature layers within one call, since they depend only on mesh geometry, never on artistic params) from per-layer artistic post-processing (Type-selection, Multiplier, Blur, Levels, Invert — always applied fresh per layer, never shared), and concluded the smallest correct architecture was a single function-local, never-escaping generator state reused only within one orchestrator call — not a new persistent cache, not a new public orchestrator parameter, not a generalized caching framework.
- **Consequences:**
  - Raw Convex/Concave values are reused among compatible enabled Curvature layers *during one composition call* — a stack with several Curvature layers pays the expensive raw geometric pass (`O(triangles)` dihedral-angle accumulation, per-vertex area normalization, a global percentile scan) only once per call, not once per layer.
  - Every layer still applies its own Type, Multiplier, Blur, Levels, and Invert independently — no cross-layer artistic contamination.
  - Each new orchestrator invocation begins with a fresh, empty local generator state — **raw Curvature is recomputed on every call**, including an Auto Update Preview recomposition triggered by editing an unrelated layer's property in the same stack, not just a Curvature layer's own parameter. Artistic-only edits do not avoid this cross-call recomputation.
  - Persistent cross-call Dynamic Curvature caching remains intentionally unresolved by this ADR — pursuing it would require either a public orchestrator signature change (threading a persistent per-entry cache reference into `ComputeComposedColorsRGBSourceTopology`) or a genuinely new caching framework; both are explicitly deferred, not implemented here.
  - Production Dynamic Curvature is now selectable in the Dynamic Layers panel and was manually validated end-to-end (M16-K.6D-8E-C, PASS: items A–G).
  - No new domain mapping, adjacency cache, blurred-result cache, or public cache API was introduced.
- **Alternatives considered:**
  - *Thread a persistent per-entry `FVertexMaskForgeGeneratorState` (or an equivalent cache reference) into the orchestrator's own public signature, sourced from the panel's real Legacy state* — rejected for this checkpoint: would change a public, already-stable signature both preview and Accept call, would risk coupling Dynamic composition to Legacy's own per-entry cache lifetime, and was judged a larger, separate architectural decision better made deliberately later (recorded as a Future Decision Candidate below) rather than folded into a UI-parity checkpoint.
  - *Introduce a new, generalized "Dynamic generator cache" framework keyed by mesh identity, shared across calls and generators* — rejected: no other Dynamic generator (Material Slot, Bounding Box, Directional Normal) needs any cache at all, since none carries persistent state; building generalized infrastructure for one generator's need, ahead of proof that a second stateful generator requires the same shape, was judged premature and speculative.
  - *Give each Curvature layer its own private `FVertexMaskForgeGeneratorState`* — rejected: defeats the entire purpose of within-call sharing, reintroducing the redundant per-layer raw-recomputation cost this design specifically avoids.
  - *Key the shared state by `LayerId`* — rejected: raw Convex/Concave values depend only on mesh geometry, never on which layer is asking; keying by `LayerId` would prevent legitimate sharing between compatible layers for no benefit.
- **Protected by:** 6 orchestrator tests (`VertexMaskForge.DynamicSourceTopologyComposition.Curvature*` and `TwoCurvatureLayersRetainDistinctMasksAndReorderChangesResult`, M16-K.6D-8E-B) covering dispatch/vertex-domain parity against a direct generator call, full parameter forwarding (all three Type options, non-default Multiplier, genuinely-nonzero fractional Blur, non-default Levels, Invert), explicit `DynamicMeshVertex`-domain correctness (two corners sharing one welded vertex resolve identically — deliberately designed to fail under a wrong Corner-domain implementation), two independent layers with reorder, disabled-layer no-op, and shared-generator-state safety (sequential evaluation against one shared state produces results byte-identical to fully independent per-config evaluation, proving no cross-layer contamination). The three pre-existing Curvature cache tests (`VertexMaskForge.WorkingMeshDomainSplit.RebuildInvalidatesCache`/`GeneratorOutputsUnchanged`/`GeneratorCacheMatrix`) remain unmodified and re-confirmed passing, continuing to protect Legacy's own separate per-entry cache. The per-layer Slate UI itself has no automatable seam in this codebase (same established boundary as every prior Dynamic Layers UI checkpoint) — validated manually instead (M16-K.6D-8E-C, PASS: items A–G — selector/defaults, all three Type options, live parameter behavior including the Levels invariant and genuinely-nonzero fractional Blur, independent two-layer state with reorder, stable-`LayerId` deletion safety, generator switching, preview/Accept parity, Auto Update Preview on/off behavior, no Legacy/other-generator regression).
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-014 — Layer-Owned Noise as the Fifth Dynamic Generator, and Ephemeral Per-Layer Lock Axes Metadata

- **Status:** Accepted
- **Decision:** Noise is exposed as the fifth selectable Dynamic Layer generator (after Material Slot, Bounding Box, Directional Normal, Curvature), reusing ADR-012's own layer-owned-parameter/orchestrator-dispatch/stable-`LayerId`-callback model, with two genuinely new architectural elements this ADR records:
  - **Ownership:** each Dynamic layer's Noise parameters live entirely in that layer's own `FVertexMaskForgeGeneratorParams` variant (`FVertexMaskForgeNoiseParams`, 17 fields, reused unchanged) — never a second copy, never panel-owned state. Two Noise layers hold fully independent values.
  - **Evaluator reuse:** the existing, unmodified `VertexMaskForgeNoiseGenerator::GenerateNoiseMaskFromDynamicMesh` is the sole evaluation path — no Type/Scale/Offset/Seed/fractal/Blur/Multiplier/Levels/Invert formula is duplicated in the orchestrator or in panel UI code, at any layer. Identity-safe callbacks (`MutateDynamicNoiseParam`) preserve stable `LayerId`/`MaskInstanceId` ownership exactly like every prior Dynamic generator's own mutation helper. Every inspector edit is live through this same authoritative Dynamic mutation path; there is no Generate button anywhere in this UI.
  - **Independent-per-layer transient state (the first new element — a deliberate divergence from ADR-013, not an extension of it):** `GenerateNoiseMaskFromDynamicMesh` requires a mutable `FVertexMaskForgeGeneratorState&` for its own raw-noise cache, the same class of requirement Curvature had (ADR-013). Unlike Curvature's *one shared* `CurvatureLocalGeneratorState`, the orchestrator gives **each enabled Noise layer its own independent, function-local `FVertexMaskForgeGeneratorState`**, declared fresh inside the per-layer loop body — Noise does **not** adopt Curvature's shared-state pattern merely because Curvature established it. The reason is Noise's own raw-cache key (`FVertexMaskForgeNoiseGenerativeParams::operator==`) includes the full generative parameter set, not just mesh geometry like Curvature's own key — two Noise layers commonly differ in Type/Scale/Offset/Seed/Octaves/Roughness/Lacunarity/Turbulence Strength/Blur, so sharing one state across them would merely thrash (invalidate and recompute for each other) rather than help. As with Curvature, no state persists across separate orchestrator calls for any Noise layer.
  - **Ephemeral per-layer Lock Axes UI metadata (the second new element):** the Dynamic Noise inspector additionally exposes a **"Lock Axes"** checkbox (tooltip "Use Scale X for all three axes.") on the Scale row, reproducing Legacy's own `bNoiseScaleAxesLocked` scale-editing rule (Scale X is the sole master; enabling copies X absolutely into Y/Z immediately, but only if a value would actually change; disabling only flips the UI state; locked X edits synchronize X/Y/Z in one mutation; Y/Z are disabled while locked, with a defensive write guard) — but **not** Legacy's single panel-global ownership, which is architecturally incompatible with multiple independent Dynamic Noise layers. Dynamic Lock Axes state is instead `SVertexMaskForgePanel::DynamicNoiseScaleAxesLockedByLayerId`, a `TMap<FGuid, bool>` panel member keyed by stable `LayerId` — never by array index, row position, or `MaskInstanceId`. A missing entry means unlocked. Reads never insert. The entry is removed on permanent layer removal, is not copied to a new or duplicated layer (no duplicate-layer operation exists in this codebase today; if one is ever added, a new layer must default to unlocked absent an established UI-preference duplication precedent, since none exists), and is preserved for the same layer across reorder, rename, and temporary generator switching away from and back to Noise within one panel session — but resets entirely when the panel is destroyed/reopened, matching Legacy's own panel-member lifetime exactly. This metadata is deliberately excluded from `FVertexMaskForgeNoiseParams` and every persistent/semantic system it touches: it is never serialized, never part of recipes/JSON (neither of which exist in this codebase today), never participates in generator equality or hashing, never enters any cache key, never reaches the orchestrator or Dynamic composition, and never affects Accept/Cancel or source-mesh persistence.
  - **Testability seam for the Lock Axes algorithm:** the lock-enable-normalization and Scale-X-edit branching logic are not left as inline, hand-duplicated code in each of the two real Slate callbacks. They are extracted into two small, production-internal, non-`static` free functions with external linkage — `VertexMaskForgePanel::NormalizeDynamicNoiseScaleForAxisLock(FVertexMaskForgeNoiseParams&) -> bool` and `VertexMaskForgePanel::ApplyDynamicNoiseScaleXEdit(FVertexMaskForgeNoiseParams&, float, bool) -> void` — declared in `SVertexMaskForgePanel.h`, defined in `SVertexMaskForgePanel.cpp`, operating only on `FVertexMaskForgeNoiseParams`/primitive values, with no Slate or `SVertexMaskForgePanel`-instance dependency. This is an internal implementation detail of one module's own `Private/` header — not a public plugin API, and not a hook that exists solely for tests: the real `OnDynamicNoiseScaleAxesLockChanged` callback and the real Scale X `OnValueChanged_Lambda` are these functions' primary, first callers; this module's own automation tests call the identical functions afterward, specifically so they exercise real production logic rather than a test-local reconstruction of it.
- **Context:** M16-K.6D-8F-A audited the existing Legacy Noise UI/generator and the Dynamic Layers panel, finding `FVertexMaskForgeNoiseParams`/`GenerateNoiseMaskFromDynamicMesh` already existed and were already wired through the generic parts of `FVertexMaskForgeDynamicLayerStack`; the genuinely new work was (1) the orchestrator's own masked-generator dispatch, (2) a decision on how to satisfy the generator's own stateful cache parameter — resolved by independent-per-layer state rather than Curvature's shared-state pattern, for the cache-key reason above — and (3) a layer-owned Slate presentation. A separate, later checkpoint (M16-K.6D-8F-C.1) added the Lock Axes convenience after Lucas requested it as a workflow parity item with Legacy; a subsequent audit (M16-K.6D-8F-D) found the first implementation's own automated tests called only a test-local reconstruction of the Lock Axes algorithm rather than real production code, and a remediation checkpoint (M16-K.6D-8F-D.1) extracted the two pure functions above specifically to close that gap honestly, without expanding the public API or the Dynamic Layer model.
- **Consequences:** Any future Dynamic generator whose evaluator needs its own stateful cache must make an explicit, evidence-based choice between Curvature's shared-within-call-state pattern (ADR-013, appropriate when the cache key is geometry-only) and Noise's independent-per-layer-state pattern (this ADR, appropriate when the cache key includes per-layer generative parameters) — neither is a universal default. Any future per-layer UI-only convenience (not part of a generator's authoritative payload) should follow this same ephemeral, panel-owned, `LayerId`-keyed `TMap` ownership model rather than a single panel-global flag, and should consider extracting its own non-trivial algorithm into a small, externally-linked, production-internal helper if honest automated coverage requires it. **Automated test coverage for Lock Axes is honestly boundary-limited**, consistent with every prior Dynamic Layers UI checkpoint (no test in this codebase constructs `SVertexMaskForgePanel` or a live Preview Component, and this ADR does not change that): automation validates `NormalizeDynamicNoiseScaleForAxisLock`'s and `ApplyDynamicNoiseScaleXEdit`'s own contracts directly, plus real `FVertexMaskForgeDynamicLayerStack` behavior via the same API shape the panel uses. It does **not** instantiate `SVertexMaskForgePanel`, click the checkbox, exercise any Slate binding, or read/write `DynamicNoiseScaleAxesLockedByLayerId` itself. The real callback-to-helper wiring, and the checkbox's exact label/tooltip/placement/enablement, are established by direct source inspection, not by automation. The integrated end-to-end workflow (selector, all 17 fields, Lock Axes enable/disable/locked-edit, multi-layer independence, Accept/Cancel, no Legacy regression) was confirmed manually in the Editor by Lucas ("funcionou").
- **Alternatives considered:**
  - *Reuse Curvature's shared `CurvatureLocalGeneratorState` pattern for Noise* — rejected: Noise's own cache key includes per-layer generative parameters, so two differently-configured Noise layers sharing one state slot would thrash rather than benefit; sharing is only a win when the key is geometry-only, as Curvature's is.
  - *A single panel-global `bool` for Lock Axes, mirroring Legacy exactly* — rejected: would incorrectly couple every Dynamic Noise layer's own Y/Z enablement to one shared flag, so locking one layer's axes would visibly (and incorrectly) disable a sibling Noise layer's own independent Y/Z controls.
  - *Transient state owned only by the currently-built inspector widget* — rejected: such state would reset on every unrelated `RebuildDynamicLayersList()` call (add/remove/reorder/rename of *any* layer, not just this one), a far more fragile lifetime than Legacy's own "resets only on tool close" contract.
  - *Add `bLockAxes`/similar directly to `FVertexMaskForgeNoiseParams`* — rejected: that struct's own existing doc comment already excludes this exact class of UI-only convenience, since the generated result is fully determined by the final Scale X/Y/Z values alone; adding it would pollute generator equality, cache keys, and any future recipe/preset serialization with a pure workflow preference.
  - *Leave the Lock Axes algorithm inline in each callback, accepting test-local mirror duplication* — rejected after M16-K.6D-8F-D's own audit: tests that call only a hand-written reconstruction of production logic can stay green even if the real callback is later broken, which is a misleading coverage claim this project does not accept.
- **Protected by:** 7 orchestrator tests (`VertexMaskForge.DynamicSourceTopologyComposition.Noise*`, `TwoNoiseLayersRetainDistinctMasksAndReorderChangesResult`, `DisabledNoiseLayerIsNoOp`, `MultipleNoiseLayersUseIndependentGeneratorState`, M16-K.6D-8F-B) covering dispatch/vertex-domain parity, full 17-field parameter forwarding, independent-per-layer generator-state safety, and disabled-layer no-op. 15 `VertexMaskForge.DynamicNoiseUI.*` tests (M16-K.6D-8F-C/8F-D.1) covering selector/default behavior, all 17 fields forwarding without sibling reset, generic-layer-property isolation, cross-layer independence, the Levels Min/Max coupled invariant, and — calling `NormalizeDynamicNoiseScaleForAxisLock`/`ApplyDynamicNoiseScaleXEdit` directly — unlocked-axis independence, enable-normalization (both the changed and already-equal cases), locked absolute (non-ratio-preserving) synchronization, disable-preserves-Scale, unrelated-field/generic-property isolation under a locked edit, and cross-layer isolation of a locked edit. The per-layer Slate UI itself (checkbox label/tooltip/placement, Y/Z enablement binding, the private metadata map's own read/write sites) has no automatable seam in this codebase (same established boundary as every prior Dynamic Layers UI checkpoint) — validated manually instead (M16-K.6D-8F-C/8F-C.1/8F-D.2, PASS — Lucas: "funcionou").
- **Supersedes:** —
- **Superseded by:** —

---

## ADR-015 — Persistent Per-Component/Per-`LayerId` Cache Ownership (Model D) for Dynamic Ambient Occlusion

- **Status:** Accepted
- **Decision:** Ambient Occlusion is exposed as the sixth selectable Dynamic Layer generator (after Material Slot, Bounding Box, Directional Normal, Curvature, Noise), reusing ADR-012's own layer-owned-parameter/orchestrator-dispatch/stable-`LayerId`-callback model, with three genuinely new architectural elements this ADR records:
  - **Ownership:** each Dynamic layer's Ambient Occlusion parameters live entirely in that layer's own `FVertexMaskForgeGeneratorParams` variant (`FVertexMaskForgeAmbientOcclusionParams`, reused unchanged — already present in the codebase before this decision, deliberately not a reuse of Legacy's own `FVertexMaskForgeAOParams`, whose default `Samples=64` mismatches the panel's real `16` default). Two Ambient Occlusion layers hold fully independent values.
  - **Evaluator reuse:** the existing, unmodified `VertexMaskForgeAmbientOcclusionGenerator::GenerateAmbientOcclusionMaskFromDynamicMesh` is the sole evaluation path — no sampling, self-hit, or Levels/Invert formula is duplicated in the orchestrator or in panel UI code, at any layer. The orchestrator translates `FVertexMaskForgeAmbientOcclusionParams` into the generator's own `FVertexMaskForgeAOParams` input 1:1 by field name; the two remain deliberately distinct types.
  - **Unconditionally World-Space, no Local-space fallback (unlike Bounding Box/Directional Normal):** Ambient Occlusion's raw sampling is inherently World-Space (Bias/Max Distance are absolute world distances; the hemisphere sampling itself assumes a world-baked normal) — unlike Bounding Box (ADR-012) and Directional Normal, which could defer World-Space support by restricting to an already-meaningful Local-space subset, Ambient Occlusion has no such subset to restrict to. This is the reason the Dynamic Source-Topology orchestrator's own public signature grew a `const FTransform& ComponentTransform` parameter (a prerequisite, separately staged checkpoint before AO's own dispatch branch existed) — the first and, as of this ADR, only generator to actually consume it; the five prior generators remain provably unaffected by its presence (verified by test, byte-exact before/after).
  - **Third mask domain — Normal Overlay Element ID (a new architectural element, not a reuse of `DynamicMeshVertex`):** unlike Bounding Box/Curvature/Noise's `DynamicMeshVertex`-domain output, Ambient Occlusion's raw mask is indexed by Normal Overlay **Element** ID — a hard-edge corner shares its Dynamic Mesh VertexID with its geometric neighbors but owns its own, independently-raycast Normal Overlay element, so reusing the `DynamicMeshVertex` domain would silently average/smooth hard edges. Resolved via a third Pass-2 domain tag (`ELayerMaskDomain::NormalOverlayElement`) and a function-local, lazily-built `CornerToElementID` map (`NormalOverlay->GetTriangle(TriangleID)[Corner]`) — the exact same lookup Legacy's own `UpdateWorkingColorsSourceTopology` already uses for this generator, never a new or invented rule, and never a source-VertexID-equals-Element-ID positional assumption.
  - **Persistent per-component/per-`LayerId` cache ownership — Model D (the central new element this ADR records):** unlike Curvature's shared-within-call state (ADR-013) or Noise's independent-per-layer-within-call state (ADR-014) — both function-local and discarded when the orchestrator call returns, because their own raw passes are cheap enough that full recomputation on every call is an accepted cost — Ambient Occlusion's raw hemisphere-raycast pass is expensive enough that discarding it on every recomposition (including one triggered only by an unrelated layer's Fill/Blend/Opacity edit) would break the "composition-only edits cost zero raycasts" contract Legacy AO's own per-component cache already guarantees, and that every other Dynamic generator's own cheap-enough-to-recompute design never had to promise. `FVertexMaskForgePreviewComponentState` therefore owns a new, **persistent**, additive field — `TMap<FGuid, FVertexMaskForgeSourceTopologyAOCache> DynamicSourceTopologyAOCachesByLayerId` — alongside, never replacing or reinterpreting, Legacy's own single-slot `SourceTopologyAOCache`. Each stable `LayerId` (never `MaskInstanceId`, never stack index, never a component pointer) owns one complete, independent `FVertexMaskForgeSourceTopologyAOCache`, including its own retained acceleration tree — no cache or tree is ever shared between two AO layers, or between two components. The existing struct's own two-layer invalidation contract (tree rebuilds on mesh/fingerprint/transform mismatch; raw values rebuild only when Samples/Max Distance/Bias change or the tree itself just rebuilt; Levels/Invert always reapplied fresh, never cached) is reused verbatim, never duplicated in the Dynamic orchestrator or in Slate UI code.
  - **Cache-adapter seam:** `GenerateAmbientOcclusionMaskFromDynamicMesh`'s own public interface (never modified) takes a `TUniquePtr<FVertexMaskForgeSourceTopologyAOCache>&`, while the persistent map necessarily stores each cache **by value** (a `TMap` value type). The orchestrator bridges this narrow interface mismatch with a call-scoped adapter — move the map entry's current content into a transient `TUniquePtr` immediately before the call, invoke the unmodified generator, move the (possibly rebuilt) result back into the same map entry immediately after, on every reachable return path (success and internal-generator-failure alike) — rather than changing the generator's interface or the map's own value type to fit around it.
  - **Lifecycle events reuse the already-established, generic-over-generator-type mutation points (no new mechanism):** layer removal and a genuine generator-type change (both introduced generically in the immediately preceding checkpoint, before Ambient Occlusion's own backend existed) erase exactly the affected `LayerId`'s own entry from every applicable component's map, mirroring the pre-existing `DynamicNoiseScaleAxesLockedByLayerId.Remove(LayerId)` precedent (ADR-014) exactly; a same-type Ambient Occlusion reselection is detected as a non-genuine transition (via a pre-mutation read-compare, since the stack's own mutators are deliberately idempotent) and retains the entry; reorder never touches the map.
  - **Single authoritative evaluation path for preview and Accept:** `ApplyPreviewToEntry`'s Dynamic branch passes `State.DynamicSourceTopologyAOCachesByLayerId` (the same `FVertexMaskForgePreviewComponentState&` already resolved for that component's own loop iteration); `VertexMaskForgeDynamicAcceptTargetBuilder::BuildSourceTopologyAcceptTargets` passes `StateOwner->GetVisualSessionStateMutable().DynamicSourceTopologyAOCachesByLayerId` — the SAME owning component state's own map, never a fresh/temporary one, never Legacy's cache. Accept continues to recompose from `GetBaselineColors()` only (never copying preview colors) and computes correctly on a cache miss, since a `FindOrAdd` lookup miss transparently creates a cold entry the generator then populates via a real raycast pass.
- **Context:** the M16-K.6D-8G-A/8G-A.1 read-only audits established that Ambient Occlusion's authoring layer (enum, payload, factory, stack validation) was already fully Dynamic-ready from earlier, incidental work, but its composition/generation layer was entirely missing, and that its raw cache's expense (unlike Curvature's/Noise's) made Curvature's/Noise's own within-call-only patterns architecturally unsuitable — reusing either verbatim would have silently regressed AO's own established "composition-only edits are free" cost contract the moment a second AO layer or any interleaved unrelated-layer edit occurred. A dedicated ownership-model audit (M16-K.6D-8G-A.1) evaluated persistent per-component/per-layer full-cache ownership (this decision, "Model D") against three alternatives before selecting it — see Alternatives Considered below. The transform-threading prerequisite (World-Space support) and the persistent-cache-map field were each staged as their own separate, independently buildable/testable checkpoints (M16-K.6D-8G-B, M16-K.6D-8G-C) before the backend dispatch itself (M16-K.6D-8G-D) consumed them, followed by focused regression coverage (M16-K.6D-8G-E) and live selector/inspector exposure with manual Editor validation (M16-K.6D-8G-F).
- **Consequences:**
  - Ambient Occlusion's own composition-only-edit cost contract now matches Legacy AO's — editing Levels, Invert, Fill, Blend Mode, Opacity, Channel Filter, reordering, or editing an unrelated layer costs zero raycasts and zero tree rebuilds once a layer's cache is warm, unlike Curvature's/Noise's own accepted "always recomputes raw data every call" cost model.
  - Each AO layer currently owns a fully independent acceleration tree, even when multiple AO layers on the same component share identical geometry/transform — tree duplication across such layers is an accepted, deliberately deferred cost, not a correctness issue (see the "Tree sharing" alternative below).
  - `FVertexMaskForgePreviewComponentState` now owns two structurally different kinds of AO cache state side by side (Legacy's single-slot field; Dynamic's per-`LayerId` map) — both remain independently valid, neither reads from or writes into the other, and this is intentional, not a transitional state to be later unified.
  - Any future Dynamic generator whose own raw pass is expensive enough that within-call-only sharing would regress an already-established "cheap composition-only edits" contract should evaluate Model D (persistent per-component/per-stable-identity full cache ownership) as a third pattern, alongside ADR-013's shared-within-call and ADR-014's independent-per-layer-within-call patterns — the choice must remain evidence-based per generator, never a default.
  - Production Dynamic Ambient Occlusion is now selectable in the Dynamic Layers panel and was manually validated end-to-end (M16-K.6D-8G-F, PASS).
- **Alternatives considered** (evaluated in M16-K.6D-8G-A.1 against the existing `FVertexMaskForgeSourceTopologyAOCache` struct's own real two-layer shape, without inventing a new cache-struct design):
  - *Model A — fully independent cache per AO layer, function-local (mirroring Curvature's/Noise's own within-call-only pattern)* — rejected: correct, but discards Legacy AO's own "composition-only edits are free" cost contract entirely — every recomposition, including one triggered only by an unrelated layer's own edit, would rebuild the tree and rerun every raycast for every enabled AO layer. This was the earliest candidate considered and was found, on closer inspection, to rest on a claim ("shared tree, independent raw results" from a single function-local instance) the existing cache struct cannot actually represent, since it holds exactly one `RawValues` buffer — a second AO layer with different raw parameters would silently overwrite the first layer's own raw result within that shared instance.
  - *Model B — persistent, one instance shared across all AO layers on a component (tree AND raw results shared)* — rejected: the existing cache struct's `RawValues`/`CachedSamples`/`CachedMaxDistance`/`CachedBias` fields describe exactly one parameter configuration; two AO layers with different raw parameters sharing one instance would thrash (each layer's own evaluation invalidating and overwriting the other's), the same class of mistake Noise's own ADR-014 already reasoned through and rejected for its own generator.
  - *Model C — persistent, shared tree + independently-retained per-layer raw results* — rejected for the initial implementation: the existing cache struct bundles tree and raw-value ownership into one non-separable shape; achieving true tree sharing would require either splitting that struct (a change to a protected, unmodified type) or introducing a new, narrower cache-struct design — a larger, riskier change than the smallest correct step, and explicitly deferred as a future optimization once real multi-AO-layer usage demonstrates the tree-duplication cost actually matters (see the Consequences above).
  - **Model D — persistent, one complete independent cache (tree + raw values) per component and per stable `LayerId`, reusing the existing cache struct's own shape verbatim inside a `TMap` — selected:** the smallest change that restores Legacy's own composition-only-edit cost contract without splitting or redesigning the existing, protected `FVertexMaskForgeSourceTopologyAOCache` struct, without a second AO algorithm, and without any new invalidation policy — the struct's own existing comparisons are reused exactly as-is, just addressed via `LayerId` instead of held as one bare field. Tree duplication across same-geometry AO layers is accepted now and can be revisited (Model C) later without changing this ADR's own per-layer raw-cache correctness contract.
  - *Keying the persistent map by `MaskInstanceId` instead of `LayerId`* — rejected: `MaskInstanceId` changes on certain parameter-edit paths within the same conceptual layer identity (per ADR-003); keying the raw-cache-reuse contract to it would risk orphaning a still-valid, still-warm cache for no correctness reason, exactly the failure mode ADR-003 itself already reasons through for result-store identity.
- **Protected by:** 11 orchestrator tests (`VertexMaskForge.DynamicSourceTopologyComposition.AmbientOcclusion*`, M16-K.6D-8G-E) covering real dispatch with byte-exact oracle parity, hard-edge Normal-Overlay-Element-ID domain resolution (two elements at one shared vertex, proven genuinely distinct via the oracle's own raycast output before asserting per-corner correctness — never assumed), cold/warm cache reuse via the strongest safely observable cache invariants (no pointer-identity accessor exists or was added), Levels/Invert raw-value reuse without double-processing, composition-only-edit (Fill/Blend/Opacity/Channel Filter) raw preservation, Samples/Max Distance/Bias raw invalidation with tree retention (`WorldMesh` pointer identity unchanged), a non-identity (rotation+translation+non-uniform-scale) component transform matching the oracle and differing from an Identity-transform result, independent cache entries for two AO layers in one map and across two separate component-owned maps, reorder cache retention with order-sensitive output, disabled-AO no-entry-created, and a transactional missing-Normal-Overlay failure that also proves the cache adapter restores ownership without corruption on that internal-failure path. 2 Accept tests prove cold-cache/warm-cache parity against a direct orchestrator call. The per-layer Slate UI itself has no automatable seam in this codebase (same established boundary as every prior Dynamic Layers UI checkpoint — no test anywhere constructs `SVertexMaskForgePanel` or a live Preview Component) — validated manually instead (M16-K.6D-8G-F, PASS: selector position/defaults, live raw and post-processing parameter edits on concave/self-occluding geometry, ordinary composition controls, independent multi-layer and multi-component/non-identity-transform behavior, generator-switch reset behavior, Source Mesh Accept, Instance Override, Undo/Redo, Cancel, no Legacy/other-generator regression).
- **Supersedes:** —
- **Superseded by:** —

---

## Future Decision Candidates

The following are open questions this project has identified but not yet decided. Listed without a preferred solution.

- Migration strategy for connecting the Dynamic composition subsystem to the production panel (and what happens to the legacy path afterward).
- A representable policy for a `Pending` (in-progress) generation status, if one is ever needed.
- Cross-generator FailedAfterReady and failed-regeneration preservation policy — whether ADR-005's Material-Slot-only contract should extend to other generators/generation paths, and if so, how.
- Per-generator domain/store choice for Dynamic generation beyond Material Slot. **Partially resolved:** Dynamic Ambient Occlusion's own cache ownership is decided (Model D, per-component/per-`LayerId` — see ADR-015); **Legacy** Ambient Occlusion's own separate, still-unresolved per-entry-vs-per-component ownership tension (noted in the architecture doc's Known Limitations) is untouched by that decision and remains open.
- Whether/how the Dynamic Layer Stack should be persisted (today it is session-only, panel-global, non-serialized).
- Whether/how Recipes/Layers should be serialized for reuse across sessions or assets.
- Final removal (or permanent retention) of the legacy fixed `GeneratorState` composition path.
- Whether/how Dynamic Bounding Box gains World Space support (requires threading a per-component transform into the orchestrator).
- Whether/how Dynamic Bounding Box gains Unified Bounds support (requires a precomputed, selection-wide bounds context reaching the orchestrator).
- Whether/how Dynamic Directional Normal gains World Space support (requires threading a per-component transform into the orchestrator — the same class of gap as Dynamic Bounding Box's own World Space candidate above).
- Whether a reusable "Paint Layers" / saved-preset concept is ever built on top of the Dynamic Layer Stack.
- Whether/how Dynamic Curvature gains persistent cross-call raw-cache reuse (requires either a public orchestrator signature change threading a persistent per-entry cache reference into `ComputeComposedColorsRGBSourceTopology`, or a new caching mechanism — see ADR-013's own "Alternatives considered"). Today's within-call-only sharing (M16-K.6D-8E-B) recomputes raw Curvature on every orchestrator invocation, including ones triggered by unrelated layer edits.
- **Resolved by implementation:** Noise was the Dynamic generator exposed after Curvature (M16-K.6D-8F-B/8F-C; see ADR-014) — its larger, 17-field parameter surface did not require revisiting ADR-012's layer-owned-parameter/orchestrator-dispatch model, and its evaluator's stateful caching needed a genuinely new shape (independent per-layer transient state, ADR-014) distinct from ADR-013's shared-within-call pattern, exactly as this candidate anticipated might be necessary.
- **Resolved by implementation:** Ambient Occlusion was the Dynamic generator exposed after Noise (M16-K.6D-8G-C through 8G-F; see ADR-015) — its raw raycast cache needed a genuinely new, third caching shape (persistent per-component/per-`LayerId` full-cache ownership, "Model D," ADR-015) distinct from both ADR-013's shared-within-call and ADR-014's independent-per-layer-within-call patterns, exactly as this candidate anticipated might be necessary. **Resolved by implementation (M17-TH-DL-B): Thickness gained a Dynamic path, Source Topology only.** Its own Asset-Local-Space-always design (never `ComponentTransform`) turned out not to require a new caching-shape decision at all — its raycast cost profile (expensive tree/raw-distance pass, cheap Min/Max/Invert remap) matches Ambient Occlusion's own exactly, so it reuses ADR-015's Model D verbatim (`FVertexMaskForgePreviewComponentState::DynamicSourceTopologyThicknessCachesByLayerId`, the same persistent per-component/per-`LayerId` full-cache-ownership shape, its own field, its own tree — never shared with AO's own map). No new ADR was required; this is a reuse of ADR-015's already-decided model, not a new caching decision. Thickness is now the last of the seven generator types to gain a Dynamic path — every `EVertexMaskForgeGeneratorType` value is dispatched. Render Vertex support for Thickness (and every other non-Material-Slot generator) remains future work, generator-agnostic and undecided. **Corrected by M17-TH-DL-E:** real Editor validation found M17-TH-DL-B's `NumValidValues == ExpectedCornerCount` requirement (zero tolerance for any single missed corner) failed the whole Dynamic composition call on ordinary real meshes, which the panel's preview code then surfaced as a silent fallback from RGB Vertex Color preview to the plain material. This is a Thickness-specific correction, not a new architectural decision or a change to the global Corner-domain/`TryGetValue` sparse-mask contract: a genuinely unmeasurable corner (even after M9) is now filled with a fixed, documented substitute (0.0f, darkest) rather than failing the whole call, and only a genuine TOTAL no-hit (`NumValidValues == 0`) still fails, unchanged. The same checkpoint added `LevelsMin`/`LevelsMax` to `FVertexMaskForgeThicknessParams`, reusing Ambient Occlusion's/Curvature's/Noise's own already-established Levels field names/defaults/range — no new ADR required for Levels either, since it is the fourth generator to adopt an identical, already-decided pattern; the only proven deviation is processing order (Levels applied after Invert for Thickness, since Invert is already resolved by the protected backend before the orchestrator's own postprocess step runs, unlike AO/Curvature/Noise where one file owns both steps).
- Definitive Accept/Cancel behavior once/if the Dynamic path gains a real caller (today's Accept/Cancel contracts are defined only in terms of the legacy path's `WorkingColors`). **Resolved by implementation (M18):** Layers (the artist-facing rename of Dynamic Layers) is now the SOLE workflow — Accept always builds targets via `VertexMaskForgeDynamicAcceptTargetBuilder::BuildSourceTopologyAcceptTargets`, and Cancel's own established restoration contract (already mode-agnostic, no `PreviewSource` read) is unchanged. This is a UI-consolidation decision, not a new caching or composition-architecture decision: no ADR was opened. The Legacy fixed-generator UI, the Legacy/Dynamic Preview Source selector, the global Channel Filter, and Fill Black/Fill White were all removed; `EVertexMaskForgePreviewSource` itself was deliberately RETAINED (not deleted) as documented technical debt, since its one remaining read guards a large, deeply-nested, dual-domain Legacy composition block inside `ApplyPreviewToEntry` that could not be safely excised within this checkpoint's own narrow UI-cleanup scope — see `Docs/VertexMaskForgeArchitecture.md`'s M18 paragraph for the full justification and the proof that this retained state can never diverge from `Dynamic` again. **User manual validation status: M18 interface consolidation — validated. Material Slot behavior — validated (functioning) after M18.1's correction.** The user initially validated the M18 interface consolidation and every other generator ("lindamente"), then discovered the Material Slot layer never responded to Preview Mode changes — a blocking defect, not a cosmetic one, so M18 was withheld from commit. **Corrected by M18.1** (see `Docs/VertexMaskForgeArchitecture.md`'s own M18.1 paragraph): the Dynamic Layers Material Slot widget never surfaced `bMaterialSlotResolutionValid` (an established, pre-existing, unchanged backend validity condition), so a mesh with ambiguous/duplicate Material Slot names silently failed composition every time, on every Preview Mode, with zero user-visible feedback. The fix adds the missing diagnostic gating; it does not change the generator, the composition dispatch, or any validity policy. The user subsequently validated Material Slot as functioning. **M18.2 (layer priority / Copy composition — investigated, resolved as a false positive, no production defect):** the user separately reported that two enabled Copy/Opacity=1 layers writing the same channel appeared not to give the visually upper layer complete authority. This checkpoint traced and empirically tested (two production-seam regression tests, both passing on the unmodified evaluator) the full array-order/reorder-mapping/sequential-fold/Copy-formula/channel-gating path and found it already correct — see `Docs/VertexMaskForgeArchitecture.md`'s own M18.2 paragraph. The user subsequently confirmed the actual cause was the order of `Lerp` operations in the user's own material/shader, external to this plugin — not a Vertex Mask Forge defect. No production code was changed; the two regression tests are retained as permanent contract coverage (sequential fold order, `Copy` full/partial-opacity behavior, reorder, multi-layer priority, channel gating), not as evidence of a bug fix. M18, M18.1, and M18.2 are now finalized as one coherent, user-validated change set.
